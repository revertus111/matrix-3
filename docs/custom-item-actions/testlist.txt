Matrix3 custom item action runtime test list

1. Pull current `main`, optionally delete the old `Server/data/logs/custom-item-actions-debug.txt`, then restart both Client and Server through the normal Eclipse/Java 8 workflow.
2. Confirm `Server/data/logs/custom-item-actions-debug.txt` is created and contains both `[CLIENT] CONFIG loaded` and `[SERVER] CONFIG loaded` lines. If either side reports `CONFIG missing` or `read-failed`, stop and use that path/error as the primary evidence.
3. Right-click Rambler's backpack 21445 in the normal NIS inventory and click `Open` once. VERIFIED prior evidence showed packet 87 for configured Inventory Option 4; confirm the log resolves that click as `context=INVENTORY option=4`, followed by `DISPATCH ... action=OPEN handler=BACKPACK`, `EXECUTE backpack-open ... openAfter=true`, and `RESULT consumed=true`.
4. VERIFIED: clicking inventory `Open` opens the player-owned backpack interface without requiring 21445 to be equipped.
5. Open the backpack and confirm interface 671 displays `BACKPACK` through component 14 instead of the old Beast-of-Burden/Familiar title text hook.
6. Confirm backpack contents render in the main interface 671 grid using native client container key 530.
7. Withdraw one item from a normal/main-grid slot that already worked in the prior test.
8. Withdraw one item from the formerly non-interactive rightmost visible column. It must now respond to the same Withdraw action; this verifies the switch from a 0..29 interaction mask to Matrix3's native 0..530 mask for 671:27.
9. Store one harmless inventory item into the backpack and withdraw it again. Confirm the correct slot updates immediately with no stale or duplicate sprites.
10. Click the existing component-29 take-all control once with enough free inventory space. Confirm it still performs the backpack take-all behavior and was not broken or repurposed by the title/mask patch.
11. If a real Beast of Burden familiar is available, close the backpack and open the familiar BoB. Confirm its own contents replace the backpack display normally despite both interfaces using client protocol key 530. Then reopen the backpack and confirm its independent player-owned contents return; server storage must not mix between the two systems.
12. Put 21445 in the inventory, open the normal bank, and right-click it in bank inventory pane 762:7 once. Confirm `[CLIENT] BANK_NATIVE` lines identify the item/option slots and show the explicit allowlist decisions. Confirm configured `Open`/`Empty to bank` insertion produces `[CLIENT] BANK_ADD` lines for options 6 and 10. If the visible menu still lacks either action, preserve the log before changing code.
13. If the bank menu still fails, copy/share the complete `custom-item-actions-debug.txt` from this single reproduction. Do not enable broad packet logging first; this targeted file should establish the next patch boundary.
14. Confirm the Server console still prints a `CustomItemActions` line showing that it loaded the canonical `Server/data/items/custom-item-actions.properties` file.
15. Right-click Rambler's backpack 21445 in the normal NIS inventory. Confirm `Open` appears from configured Inventory Option 4 while existing Wear/Use/Drop/Examine behavior remains available.
16. Equip 21445 and right-click it in the equipment interface. Confirm configured Equipment Option 1 displays `Open` and opens the same player-owned backpack storage.
17. Put 21445 in the inventory, open the normal bank, and right-click it in bank inventory pane 762:7. Confirm the backpack shows only these five intended actions: Deposit, Wear, Open, Empty to bank, Examine. Confirm Deposit-5, Deposit-10, Deposit-X/last amount, and Deposit-All are not present for 21445.
18. Check the visual order of the five backpack actions. Record whether Matrix3 presents them as `Deposit / Wear / Open / Empty to bank / Examine`. Ordering is runtime verification; do not treat a different order as a routing failure if the exact five actions and packet behavior are otherwise correct.
19. Click `Deposit`. Confirm it still performs Matrix3's normal one-item bank deposit and the trace records `RESULT fallthrough reason=stock` before Matrix3's stock handler runs.
20. Click `Wear`. Confirm it still equips the physical backpack through Matrix3's normal bank-inventory wear handler and the configured STOCK entry does not consume it.
21. Click `Examine`. Confirm it still performs Matrix3's normal inventory-item examine behavior and the configured STOCK entry does not consume it.
22. Click bank-inventory-pane `Open`. Confirm it opens the same player-owned backpack storage while validating the physical backpack from the clicked inventory slot.
23. Put several items in backpack storage, return to the bank inventory pane, and click `Empty to bank`. Confirm only backpack contents move into the bank, the physical 21445 stays in inventory, and successfully banked quantities are removed from backpack storage.
24. Repeat `Empty to bank` with insufficient bank space. Confirm the existing partial/full-bank handling remains intact and no successfully unbanked quantity is deleted.
25. Put 21445 in the main bank item container and right-click it. Confirm the existing configured Bank Option 7 `Open` path still opens the same player-owned backpack storage and is unaffected by `bank_inventory.mode=EXPLICIT`.
26. Right-click ordinary, unconfigured items in the bank inventory pane. Confirm their normal Matrix3 Deposit-5/10/X/All variants remain present and no custom filtering applies to them.
27. Close/reopen the bank repeatedly. Confirm the five configured backpack entries do not duplicate and removed stock variants do not intermittently reappear on 21445.
28. Temporarily add a second harmless test item with `bank_inventory.mode=EXPLICIT`, selected `STOCK` entries, and one custom action to a local copy of the properties file. Confirm the explicit menu remains data-driven without another item-ID hardcode.
29. Run the backpack smoke path: logout/login persistence, real familiar BoB isolation, death persistence, and main-game keep-all death behavior remain unchanged.
