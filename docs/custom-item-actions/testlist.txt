Matrix3 custom item action runtime test list

1. Pull current `main`, restart both Client and Server from the normal Eclipse/Java 8 workflow, and confirm the Server prints a `CustomItemActions` line showing that it loaded the canonical `Server/data/items/custom-item-actions.properties` file and reports the configured action count.
2. Right-click Rambler's backpack 21445 in the normal NIS inventory. Confirm `Open` appears from configured Inventory Option 4 while existing Wear/Use/Drop/Examine behavior remains available.
3. Click inventory `Open`. Confirm the player-owned backpack interface opens without requiring 21445 to be equipped, and Store/Withdraw behavior still works. This specifically retests the prior runtime failure where the client label appeared but the server action did nothing.
4. Equip 21445 and right-click it in the equipment interface. Confirm configured Equipment Option 1 displays `Open` and opens the same player-owned backpack storage.
5. Open the normal bank with 21445 in the bank inventory pane. Record the current stock `Deposit / Wear / Examine` menu as bank-inventory-pane client/CS2 evidence. `Empty to bank` is intentionally not enabled until its per-item menu label can be presented correctly.
6. Put 21445 in the bank item container and right-click it. Confirm no stock bank option has been silently repurposed into a different hidden action. Bank item-container `Open` still needs runtime verification against the client presentation path.
7. Right-click ordinary, unconfigured items in inventory, equipment, and bank. Confirm their stock Matrix3 options and behavior are unchanged.
8. Close/reopen interfaces and invalidate/reload item definitions where practical. Confirm configured inventory/equipment labels are reapplied to a newly decoded ItemDefinitions object without duplicating or corrupting options.
9. Temporarily add a second harmless test item entry to the properties file in a local test copy. Confirm the generic registry recognizes its configured option without adding an item-id hardcode to ControlerManager.
10. Keep the `EMPTY_TO_BANK` bank-menu config entry disabled until a verified per-item bank inventory/menu label hook exists. Confirm clicking any stock bank option on 21445 does NOT empty backpack contents in this build.
11. Run the backpack smoke path: persistence, real familiar BoB isolation, death persistence, and main-game keep-all death behavior remain unchanged.
