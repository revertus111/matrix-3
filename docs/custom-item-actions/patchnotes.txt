2026-09-03
- Added shared custom item action data authority for configurable item context actions.
- Moved the canonical config to `Server/data/items/custom-item-actions.properties`, matching Matrix3's existing server data layout.
- Client and Server now walk parent directories to resolve the same canonical config from normal Eclipse launch working directories; the duplicate repo-root `data/items/custom-item-actions.properties` was removed.
- Server now logs the resolved config path and number of loaded custom actions on first use so presentation-only success cannot hide a server config loading failure.
- Added server `CustomItemActions` registry with config-driven inventory/equipment/bank option routing; unconfigured items fall through to normal Matrix3 handlers unchanged.
- Added client `CustomItemActionConfig` presentation hook that applies configured inventory option labels and equipped-item option params to already-decoded Matrix3 ItemDefinitions, plus configured option entries for the bank inventory pane.
- Option numbers are 1-based in config so a future editor can expose Option 1..N directly.
- Handler/action identifiers are stable data values (`BACKPACK`, `OPEN`, `EMPTY_TO_BANK`); Java method names are intentionally not stored in config.
- Rambler's backpack 21445 is the first configured item: Inventory Option 4 = Open, Equipment Option 1 = Open, Bank Option 7 = Open.
- Runtime evidence from the first test showed the client displayed `Open` while the server action did nothing; the split config location was corrected before changing Matrix3's normal inventory packet owner.
- Added a distinct `bank_inventory` context for interface 762 component 7 so bank inventory actions do not collide with the main bank item-container context.
- verified-static: 762:7 keeps stock options 1-5 for deposit amounts, option 7 Wear, option 8 Examine, and option 9 Deposit-All; native option 6 and option 10 are available without repurposing a stock action.
- Client bank-inventory presentation reads configured `bank_inventory` entries and adds missing native option slots for the clicked configured item.
- The client resolves the clicked item from Matrix3's dynamic interface child metadata; no Rambler item ID is hardcoded into the bank-menu hook.
- Server routing recognizes `bank_inventory` options 1-10. Configured Open validates the physical item from the player's inventory slot, while Empty to bank uses the existing player-owned backpack storage transfer.
- Added optional `bank_inventory.mode=EXPLICIT`. In explicit mode, a configured item's bank-inventory menu becomes an allowlist and unlisted stock option slots are hidden for that item only.
- Added stable `STOCK` action entries. These are presentation allowlist entries that deliberately return control to Matrix3's original ButtonHandler instead of replacing Deposit/Wear/Examine behavior.
- Explicit menu suppression now occurs in Matrix3's existing `Class592.method7012` menu-entry construction path before entries are grouped/inserted, avoiding stale grouped-menu bookkeeping while keeping the hook narrow.
- Rambler's explicit bank-inventory menu now allows only native slots 1, 6, 7, 8, and 10: Deposit, Open, Wear, Examine, and Empty to bank. Deposit-5, Deposit-10, Deposit-X/last amount, and Deposit-All are hidden only for the configured backpack item.
- The exact visual ordering of those five entries remains runtime verification because Matrix3's decompiled menu sorter groups interface option opcodes internally.
- Backpack contents remain player-owned persistent storage; this patch does not move storage ownership onto the physical item or change death persistence.
- Added a persistent targeted trace at `Server/data/logs/custom-item-actions-debug.txt`; both Client and Server append timestamped lines to the same ignored runtime log.
- Client trace records config loading, inventory/equipment label application, 762:7 native bank-menu option detection/suppression, and configured bank-menu entry insertion.
- Server trace records the raw item-context pipeline entry, resolved context/packet option/item, STOCK/no-match fallthrough reasons, configured dispatch, Backpack execution, and whether the action was consumed.
- verified-static: custom actions are invoked from `ControlerManager.processButtonClick(...)` after the open Backpack storage-interface handler and before normal controller/preset routing; they are not called directly from `ButtonHandler`.
- Added a pre-Backpack pipeline trace call so a raw item click can still be seen even if Backpack interface ownership consumes it before `CustomItemActions.processButtonClick(...)`.
- The runtime log path resolves back to repository `Server/data/logs` from normal Client/Server Eclipse working directories; `Server/.gitignore` already excludes `data/logs/`.
- Tightened the trace so detailed CLIENT bank-menu and SERVER pipeline output is emitted only for items present in the custom-item-action config, avoiding normal inventory/bank click spam.

2026-09-06
- VERIFIED from `custom-item-actions-debug.txt`: configured inventory option 4 for Rambler's backpack reaches the server as packet 87, which is `ACTION_BUTTON6_PACKET`, not `ACTION_BUTTON4_PACKET`.
- Fixed the generic inventory config-slot mapping in `CustomItemActions`: option 1 -> BUTTON1, option 2 -> BUTTON2, option 3 -> BUTTON3, option 4 -> BUTTON6, option 5 -> BUTTON7.
- This is a framework-level fix; no 21445-specific routing exception was added.
- Bank-inventory menu behavior was not changed by this patch and remains the next runtime evidence target if Open/Empty to bank are still missing there.
- VERIFIED runtime: configured inventory Open now reaches the Backpack handler and opens interface 671/665 successfully.
- Runtime screenshot then showed backpack contents rendering in the narrow right-side strip while the main Beast of Burden grid remained empty.
- verified-static: Matrix3's native `BeastOfBurden` also uses interface 671 component 27, so the storage component was not changed.
- verified-static: native `BeastOfBurden` binds the 671:27 grid to client item-container key 530; Backpack had diverged to key 531 while reusing the same interface.
- Changed Backpack's UI/protocol item-container key from 531 to the native BoB key 530. The server-side `Backpack.items` container remains independent player-owned storage; only the transient client interface binding is shared.
- VERIFIED runtime: key 530 now renders backpack contents in the main interface 671 grid.
- Runtime screenshot exposed that only the narrow right-side strip accepts withdraw clicks; the large visible grid does not. The prior 0..530 interaction-mask change did not fix this, so component ownership is still unresolved.
- Runtime also confirmed interface 671 component 14 is not the visible `BEAST OF BURDEN` heading; the attempted `BACKPACK` text override on 671:14 has no visible effect.
- Added focused `Server/data/logs/backpack-debug.txt` tracing. While Backpack is open, server lines record every 671/665 click with exact interface, component, slot, packet, and resolved slot item.
- Added a one-shot client dump of interface 671 non-empty string fields for components 0..63 into the same `backpack-debug.txt`. This is intended to identify the real title component without another guessed text override.
- No new interaction component or title component is assumed in this diagnostic patch; the next fix must be driven by the runtime trace.
- VERIFIED runtime: the initial client component probe crashed at component 52 because this interface revision exposes fewer than 64 direct components and `Class512.method6083(...)` throws past the array boundary.
- Made `BackpackInterfaceTrace` bounds-safe: `ArrayIndexOutOfBoundsException` now records the boundary and stops probing instead of crashing the client. Other runtime probe failures are logged and stop the one-shot dump safely.
- The trace still records every valid component reached before the boundary, so the title/component evidence remains useful without hardcoding a guessed component count.
- VERIFIED runtime from the focused trace: valid backpack item clicks are `671:27`, the external inventory pane is `665:0`, and clicks on the large visible but non-item grid surface hit `671:16/17` with `slot=65535`. This moves the defect boundary to client layout/presentation rather than server withdraw routing.
- The same trace exposes static text `671:3 = Backpack` and `671:6 = Familiar`, while the visible `BEAST OF BURDEN` heading does not appear in the probed string fields; its exact presentation owner remains UNKNOWN.
- Expanded `BackpackInterfaceTrace` with bounded geometry snapshots for interface 671. Each changed snapshot records decoded component type, parent, base X/Y/W/H, alignment modes, runtime layout X/Y/W/H, scroll offsets, content/item IDs, child counts, sprite IDs, and text/font data.
- Geometry snapshots are signature-gated and capped at four, so opening/resizing the interface can reveal the misaligned item component without per-frame log spam or another broad client scan.
- VERIFIED runtime geometry: interface root `671:9` decodes at 485x299, then is mutated to 290x299 at runtime. That shrinks parent `671:11` to 274px and causes its two 225px child panes to overlap; `671:27` consequently lands at x=209 as the exposed thin right-side strip.
- Added `BackpackInterfaceLayout` as a narrow client repair. It only triggers for the exact verified bad `671:9 = 290x299` state and restores the cache-defined 485px root width before the next layout pass.
- The repair does not change Backpack storage, item key 530, component 27 routing, familiar storage, or server withdraw behavior. Runtime verification is required for full-width rendering and slot alignment.
- RETIRED before runtime acceptance: the hardcoded `BackpackInterfaceLayout` width repair was removed once the user chose a generic live Interface Editor workflow. The previous 485px assumption remains evidence/history, not an active fix.
- Interface 671 layout discovery now belongs to the reversible Client Console Interface Editor; `CustomItemActionConfig` no longer mutates interface geometry each cycle.
