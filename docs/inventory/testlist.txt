Matrix3 28-slot inventory restoration runtime test list

1. Log in on a fresh/new character and confirm the normal inventory has exactly 28 usable slots and the stock NIS 4x7 layout is unchanged.
2. Fill all 28 slots with non-stackable items and attempt to add another. Confirm normal full-inventory handling occurs at 28 slots.
3. On a character saved during the temporary 36-slot build, place known items in slots 29-36 before updating. After the first login on this patch, confirm slots 1-28 are unchanged and every former slot 29-36 item appears in the persistent backpack with no loss or duplication.
4. Log out after the migration and log back in. Confirm the inventory remains 28 slots and the migrated backpack contents remain intact rather than migrating again.
5. Bank/deposit/withdraw items through the normal inventory and confirm ordinary 28-slot Matrix3 behavior is unchanged.
6. Confirm the client no longer applies InventoryInterfacePatch to interfaces 1473:34 or 1474:15 and no extra blank/expanded inventory rows appear after interface reload or resize.
7. Run the normal Matrix3 inventory/bank/preset smoke path and confirm existing content once again sees the original 28-slot normal inventory authority.
8. Safety case: if a legacy larger inventory cannot be migrated because backpack capacity is already exhausted, confirm the serialized larger container is retained rather than silently deleting overflow items; report this state before continuing gameplay because only the standard 28 slots are intentionally unlocked.
