Matrix3 36-slot inventory runtime test list

1. Log in with an existing character whose serialized inventory was created before this patch. Confirm all existing items remain in the same slot positions and no item is lost or duplicated.
2. Use Item Browser or another safe owner spawn path to fill inventory slots beyond the original slot 28. Confirm the server accepts up to 36 occupied slots before reporting a full inventory.
3. Open and resize the NIS inventory interface 1473:34. Confirm the client now exposes two additional visual rows for a 4x9 / 36-slot grid rather than stopping at the original 4x7 / 28-slot layout.
4. Confirm rows 8-9 are not clipped by the NIS parent at normal usable inventory-window sizes. Resize the inventory narrower/taller and confirm the extra rows remain reachable without corrupting nearby NIS layout.
5. In slots 29-36, test selection, examine, item switching/reordering, dropping/deleting where safe, and normal inventory interactions. Confirm the server and client agree on the extra slot indexes.
6. Open any flow that uses alternate inventory interface 1474:15 and confirm its extra slots also remain visible/reachable rather than truncating at slot 28.
7. Bank/deposit and withdraw items involving the extra inventory slots. Confirm no slot truncation, packet error, or item loss occurs.
8. Log out normally with items stored in slots 29-36, restart the server/client as appropriate, and log back in. Confirm the extra-slot contents persist exactly and the visual grid still shows all 36 slots after interface reload.
9. Fill all 36 slots and attempt to add another non-stackable item. Confirm the normal full-inventory handling occurs only after all 36 slots are occupied.
10. Close/reopen or otherwise unload/reload the inventory interface where practical. Confirm the runtime layout override is not applied twice to the same component and is correctly reapplied once if a new component instance is loaded.
11. Run the normal Matrix3 inventory/bank/preset smoke path after the expansion and confirm existing 28-slot-era content does not assume or corrupt the old final slot index.
