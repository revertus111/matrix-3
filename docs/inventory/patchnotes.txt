2026-09-03
- Expanded Matrix3 inventory authority from 28 to 36 slots.
- Added load-time migration for existing serialized inventories smaller than 36 slots; existing slot positions are copied unchanged and larger containers are never shrunk.
- Extended inventory component settings to the live container size instead of hard-coding slot 27 as the final usable slot.
- Kept client interface hardcoding out of the initial slice pending runtime evidence: the item update protocol already transmits the container length.
- Runtime verification confirmed slots 29-36 exist and function but stock NIS only renders the original 28 visual slots.
- Added a narrow client runtime layout override for inventory components 1473:34 and 1474:15, extending their raw grid height by two 36px row pitches (4x7 -> 4x9) without modifying cache data.
- The override is applied from the normal logged-in client cycle and tracks component object identity so an unloaded/reloaded interface group can be patched again exactly once.
