2026-09-16 - Matrix3 Construction Revamp preservation/reset
- Established `revertus111/matrix-3` as the only implementation repository for this workstream.
- Preserved the approved Construction settlement design in a Matrix3-native workstream document.
- Explicitly rejected the legacy 718 Construction implementation as architecture authority; it remains reference-only.
- Returned tracked repository content to the Sept. 12 Matrix3 runtime-reset checkpoint, removing the later Steam Deck runtime/docs additions from the active tree.
- Phase 1 Bundle 1.1 is ACTIVE: identify Matrix3-native Construction, instance, persistence and placement ownership before runtime code is added.

2026-09-16 - Runtime feature-stack recovery
- Restored unrelated pre-reset Matrix3 Client/Server systems from `8c524fa5949dc436484e49144e736215b42586d4` without changing the approved Construction roadmap or implementation scope.
- Construction remains Matrix3-native; restored historical systems are not Construction architecture authority.
- Steam Deck files remain excluded from the active tree.
- Phase 1 Bundle 1.1 state is unchanged.

2026-09-16 - Construction Editor prototype
- Added a lazy Client Console Construction Editor panel for owner/admin development placement.
- Added provisional wooden wall, floor and doorway presets with editable object type and fixed rotation controls.
- Reused Matrix3 Dev Mode tile resolution and the existing server-authoritative `itembrowser devspawn` path; no new obfuscated scene-picking or rendering hook was added.
- Added Paint/Continuous placement controls plus R, Shift+R and mouse-wheel fixed-rotation re-arming while placement is active; Escape/Cancel continues through existing Dev Mode cancellation.
- Starter object ids/types are HYPOTHESIS until visually verified against the active Matrix3 cache.
- This editor slice does not restore persistent settlement state/instance ownership and does not claim the final 3D ghost preview.

2026-09-16 - Custom Construction palette foundation
- Added a custom-drawn in-game Construction object palette launched from the Construction Editor.
- Added Walls/Floors/Doors category tabs, text search/filter, build-piece cards, Paint/Continuous mode controls, rotation controls, cancel/close handling and keyboard/mouse-wheel rotation while the palette is active.
- Added shared Construction selected-piece state that arms the existing server-authoritative Dev placement path instead of creating a second world-placement owner.
- Added read-only hovered-world-tile tracking from Matrix3's verified action-23 menu-entry seam for future preview/validation use.
- Preserved the verified runtime starter mappings: 13450 Wooden fence (temporary wall test), 13684 Floor decoration, 13344 Door.
- Traced `ObjectDefinitions.method6061(...)` as an object-model factory, but the safe non-authoritative scene draw/insertion seam remains UNKNOWN; no fake 3D ghost was added.
- Source and workstream documentation were reconciled without force-pushing or changing branches; runtime verification is the next gate.

2026-09-16 - Construction palette flicker fix
- Replaced direct `Canvas.getGraphics()` painting with a small owned `JWindow` and Swing-painted palette surface positioned over the Matrix3 game canvas.
- The palette now has a persistent native surface instead of racing the game renderer, eliminating the known flash/flicker mechanism.
- Only the palette rectangle owns mouse input; the rest of the heavyweight game canvas remains untouched for normal Matrix3 world interaction.
- Preserved global search/rotation keyboard handling and world-canvas mouse-wheel rotation without creating a second scene/input owner.
- Runtime verification is required to confirm stable visibility, window tracking and unchanged placement passthrough.

2026-09-17 - Construction 3D ghost preview v1
- Added `ConstructionGhostPreview` as a client-only direct renderer for the selected Construction piece on the hovered world tile.
- Uses Matrix3's verified-static object-model path (`ObjectDefinitions.method6057(...)` -> `Model.method1375(...)`) with live scene terrain height and selected rotation.
- Hooked the preview into the existing per-scene-frame `Class110.method2071(...)` seam; the preview is never attached to or removed from `Class523`.
- Preserved server/world authority: the ghost has no collision, persistence, placement ownership, or server state; confirmed placement still uses the existing devspawn path.
- Kept v1 visually literal: no guessed translucency, tint, validity color, or occupancy rules were added.
- Runtime verification is required for model alignment, hover following, rotation, cancellation, and scene/render stability.

2026-09-17 - Construction ghost render-order correction
- Runtime test showed the first direct-render hook produced no visible ghost while palette, hover tracking, rotation and confirmed placement remained functional.
- Removed the preview draw from pre-scene `Class110.method2071(...)`; that seam runs before `Class523.method6257(...)` performs the main scene/object render pass.
- Moved the preview draw to `Class343.method4302(...)` immediately after `Class523.method6240(...)` completes the live scene render.
- Added the same per-tile `Class86` renderer environment setup used by `Class523.method6280(...)` before drawing normal scene objects.
- Preview remains client-only and unregistered: no `Class523` attach/remove, collision, persistence or server ownership was added.
- Corrected post-scene path requires runtime retest before ghost v1 is accepted.

2026-09-17 - Construction ghost draw-contract correction
- Second runtime test still showed no visible ghost, confirming render-order correction alone was insufficient.
- Verified the real Matrix3 object draw path passes a `Class90` render-bounds object to `Model.method1375(...)` for ordinary definitions instead of `null`.
- Updated `ConstructionGhostPreview` to mirror that contract with a reusable `Class90` bounds object.
- For definitions with `aClass326_5684`, the preview now mirrors Matrix3's special path: draw with null bounds and immediately call `Class106.method1738(...)` with the definition bounds.
- Preview remains direct-rendered and unregistered; no scene ownership, collision, persistence or server state was added.
- Runtime retest is required before the ghost-preview slice is accepted.

2026-09-17 - Construction ghost pipeline diagnostics
- Runtime retest with a valid displayed target tile still produced no visible ghost after the draw-contract correction.
- Added transition-only `[ConstructionGhostPreview]` console diagnostics for every render guard, object-definition/model-build failure, and successful draw submission.
- Successful diagnostics include object id/type/rotation plus world/local/scene coordinates without changing placement or scene ownership.
- Diagnostic output is intentionally state-change-only to avoid per-frame console spam.
- Next runtime check is to capture the single diagnostic state while placement is armed over a valid target tile.

2026-09-17 - Construction ghost white-tint pass
- Added Matrix3's verified whole-model tint capability (`Model.method1396(...)`) to the Construction preview path.
- Requested model flags now include `0x80000`, matching Matrix3's own tint-capable model path.
- Preview tint targets zero saturation / maximum signed-byte lightness with a 160 override weight for a washed-out white ghost appearance.
- True global model translucency remains UNKNOWN and was not guessed; preview remains client-only and unregistered.
- Existing pipeline diagnostics remain active until the preview is runtime visible.

2026-09-17 - Construction ghost active-scene-pass hook
- Added a narrow hook in `Class578.method6834(...)`, which Matrix3 calls directly after immediate real scene-object draw submissions.
- The hook only runs for non-owned immediate `Class531` recycling while the normal `aClass174Array5838` world terrain is active, avoiding queued cleanup and alternate scene passes.
- Added a one-render-per-`client.cycles` guard in `ConstructionGhostPreview` so repeated scene-object submissions cannot duplicate the ghost in one frame.
- Existing `Class343` post-scene call remains as a fallback; the per-cycle guard prevents double submission when the in-pass hook fires first.
- No `Class523` scene attach/remove, collision, persistence or server ownership was added.

2026-09-17 - Construction ghost staged debug instrumentation
- Added once-only `[ConstructionGhostDebug]` milestone logging through the live `Class578.method6834(...)` hook and `ConstructionGhostPreview` render pipeline.
- Milestones identify hook entry, immediate post-object path, render dispatch, valid Construction state, model-build entry/return, white-tint entry/return, and draw-call entry/return.
- Existing terminal `[ConstructionGhostPreview]` guard/failure diagnostics remain intact, so one runtime launch can identify the exact method boundary where the invisible preview stops progressing.
- Diagnostic logging is intentionally one-shot per client launch to avoid per-frame scene-render console spam.
- No renderer behavior, scene ownership, placement authority, collision, persistence or server state was changed by this diagnostic patch.

2026-09-17 - Construction ghost on-screen diagnostic bridge
- Runtime capture showed valid Construction placement commands reaching the server but no `[ConstructionGhostDebug]` / `[ConstructionGhostPreview]` lines in the Gradle console, leaving hook execution vs client stdout routing ambiguous.
- Added a temporary client-owned `GHOST DEBUG:` JWindow that starts when the Construction palette opens and displays the latest ghost-pipeline milestone directly in the game client.
- The debug bar begins at `PALETTE_OPENED - waiting for render hook` and advances through hook/model/tint/draw states; palette close ends the diagnostic session and hides the bar.
- This patch changes diagnostics only; ghost rendering, placement authority, scene ownership, collision, persistence and server state remain unchanged.

2026-09-17 - Construction ghost footprint decoder fix
- On-screen diagnostics runtime-verified that the active ghost render hook is executing with a valid hovered tile.
- The preview stopped at `SKIP_BOUNDS` because `ObjectDefinitions.sizeX/sizeY` were being multiplied by their obfuscated storage multipliers a second time, producing impossible billion-scale footprints.
- Corrected the reads to use the 32-bit modular inverse decode multipliers (`sizeX * -876498849`, `sizeY * 1922784011`), which decode the default stored values back to a 1x1 footprint.
- No render hook, placement authority, scene ownership, collision, persistence or server behavior changed in this fix.
- Runtime retest should now advance past bounds validation into model build/tint/draw diagnostics.

2026-09-17 - Construction ghost base render runtime verified
- Runtime retest after the footprint decoder fix visibly rendered the selected Wooden fence preview on the hovered world tile.
- The on-screen diagnostic reached `DRAW_SUBMITTED object=13450 type=0 rot=0 specialBounds=false`, runtime-proving the active hook, object-definition lookup, model build, tint call and `Model.method1375(...)` submission all complete successfully.
- The preview remains client-only and unregistered; no scene ownership, collision, persistence or server placement authority was introduced.
- Current tint is visibly red rather than the intended white and no translucency is present, so visual ghost styling remains pending.
- Rotation, hover-follow behavior, piece switching, terrain alignment, cancel/stale-hover cleanup and duplicate/flicker behavior remain targeted runtime checks before ghost v1 is accepted.

2026-09-17 - Construction ghost white/translucent styling correction
- Verified-static `AbstractModel.method1396(...)` interpolates packed colour components with `weight >> 7`; the previous weight 160 overshot the target and explains the runtime red result. The ghost now uses weight 128 for an exact zero-saturation / maximum-lightness target.
- Verified-static `AbstractModel.alpha` is annotated as face alpha and `Model.method1467(byte, byte[])` sets the model face-alpha values; with a null array it applies one value to every face.
- Added `Model.method1467((byte) 96, null)` after the colour override, targeting roughly 62% rendered opacity (`255 - 96`) for a visible but clearly translucent placement ghost.
- Added one-shot alpha start/done diagnostics and included tint weight/face alpha in the final `DRAW_SUBMITTED` state.
- Render hook, footprint decoding, placement authority, scene ownership, collision, persistence and server behavior are unchanged.
- Runtime retest is required to accept the white/translucent appearance and then verify hover following, rotation, piece switching, cancellation and scene stability in one bundled pass.

2026-09-17 - Construction ghost alpha-isolation correction
- Runtime retest verified that the Door ghost renders with the intended white/translucent styling and reaches `DRAW_SUBMITTED object=13344 type=0 rot=0 ... ghostTint=white/128 faceAlpha=96`.
- The same run exposed a regression: real objects placed after previewing inherited the ghost translucency, proving preview alpha mutation was leaking through shared model backing data.
- Verified-static `AbstractModel.method1351(...)` only deep-copies the face-alpha array when `Class368.method4561(flags, ...)` is true; `Class368.method4561(...)` is exactly `(flags & 0x100) != 0`. Without that bit, the clone shares `alpha` with its source model.
- The existing `0x80000` capability already isolates face-colour storage through `Class368.method4501(...)`; added `0x100` to the ghost model flags so `Model.method1467(...)` mutates preview-owned alpha storage instead of the cached/shared object model.
- No scene ownership, collision, persistence, placement authority or server behavior changed.
- A full client restart is required before retest because the previous run may already have contaminated cached model alpha arrays in memory.

2026-09-17 - Construction ghost alpha isolation runtime verified
- Full-client restart retest visibly shows the Wooden fence ghost as white/translucent while the placed Wooden fence remains its normal brown/opaque appearance.
- `DRAW_SUBMITTED object=13450 type=0 rot=0 ... ghostTint=white/128 faceAlpha=96` remains active while the isolated preview is visible.
- This runtime result verifies the added `0x100` model capability gives the preview private alpha storage and prevents `Model.method1467(...)` from contaminating the cached/placed object model.
- White/translucent ghost styling and preview-vs-placed alpha isolation are now runtime VERIFIED; remaining ghost v1 acceptance is limited to hover-follow, rotation, piece switching, terrain alignment, cancel/stale-hover cleanup, duplicate/flicker, confirmed-placement count and camera/scene stability.

2026-09-17 - Construction build-camera v1 foundation
- Added `ConstructionBuildCamera` as a Construction-only lifecycle bridge into Matrix3's existing Orb of Oculus free-camera mode.
- Opening/closing the Construction palette now requests build-camera enter/exit through the existing owner-only Client Console command bridge.
- Server enter uses `InterfaceManager.gazeOrbOfOculus()` and exit uses `Player.closeInterfaces()`, preserving Matrix3 camera/interface reset ownership and stopping current player actions on entry.
- No custom camera physics, player teleport, scene picker, or placement authority was added.
- Free Build is the first runtime target; RTS/top-down/orbit presets are documented but remain pending a verified camera-preset seam rather than guessed packet values.
- Runtime verification is required for current-client WASD/Q/E controls, stationary-player click behavior, ghost compatibility, and clean normal-camera restoration.

2026-09-17 - Construction build-camera Orb crash safety correction
- Runtime test proved the legacy Orb-of-Oculus interface path is incompatible with the current Matrix3 client/cache: opening the Construction palette crashed during `SET_INTERFACE` with `ArrayIndexOutOfBoundsException: 57`.
- The fatal chain is `Class104_Sub1.method9918(...) -> Class512.method6083(...) -> Class83.method1256(...)`; `InterfaceManager.gazeOrbOfOculus()` installs legacy root/component `475/57`, matching the invalid component index.
- Disabled the Construction client bridge from sending `itembrowser constructioncamera enter/exit`; palette open/close no longer requests the incompatible legacy root interface.
- Ghost debug output immediately before the exception is not the initiating failure, and the later `ConnectException` is secondary crash-report/network behavior.
- Modern Free Build camera work remains blocked on a verified current-client camera/input owner; exact `FreeMove`, `VK_W`, and camera-position searches did not expose a trustworthy current seam, so no obfuscated field or packet value was guessed.

2026-09-17 - Construction camera command hardening
- Disabled the owner-only server `itembrowser constructioncamera` entry point so manual/accidental use cannot invoke the rejected legacy Orb root/interface path.
- The command now fails closed with a compatibility message and leaves camera/interface state untouched.
- Construction camera work remains blocked on the verified current-client camera/input seam; no legacy interface or guessed camera state was reintroduced.

2026-09-17 - Native Construction Free Build camera v1
- Verified-static the current renderer camera path in `Class343.method4302(...)`: generic scene rendering consumes `Class36.anInt387`, `Class572_Sub13_Sub2.anInt11451`, `Class49.anInt490`, `Class455.anInt5187` and `Class406.anInt4765`; Matrix3 camera mode 1 instead uses the newer `Class411` camera object directly.
- Replaced the disabled Construction camera placeholder with a client-owned Free Build camera that snapshots/restores the previous camera mode/transform and never opens legacy root/interface 475.
- Free Build uses a Construction-only generic camera mode while active and writes the verified renderer camera globals at ~60 Hz.
- Added WASD horizontal movement relative to camera yaw, smooth acceleration/deceleration, Shift fast movement, Ctrl precision movement, Q/E vertical movement, and right-mouse-drag yaw/pitch with Matrix3's 1024-3072 pitch clamp.
- Left-clicking the game canvas zeros Construction camera velocity immediately.
- Free Build Paint now consumes Matrix3's already-resolved hovered tile and calls `DevSpawnPlacement.placeActive(...)`; the canvas click is consumed so the same intentional build click does not also become Walk Here.
- Real object creation remains server-authoritative through the existing owner-only `itembrowser devspawn` path; no second scene pick or client world-object owner was added.
- Palette close restores the exact previous Matrix3 camera mode and camera transform.
- Runtime verification is required for movement direction/speed, Q/E sign, right-drag feel, normal-camera restoration, ghost compatibility, one-object-per-click behavior and whether consumed AWT Paint clicks fully suppress action 23 on the active client.

2026-09-17 - Construction Free Build mode encoding correction
- Static verification caught an obfuscation mistake before runtime testing: raw `711307203` already decodes to camera mode 5 under `Class18.anInt143 * 625220759`; multiplying it by 5 would select decoded mode 25 instead.
- `Class457.method5426(...)` directly assigns raw `711307203`, source-proving it is Matrix3's mode-5 raw value.
- Free Build now assigns that raw value directly; camera position/pitch/yaw encode/decode pairs remain unchanged and statically verified.

2026-09-17 - Construction camera runtime rejection + Alt free-cam diagnostic trace
- Native Free Build v1 failed runtime acceptance: opening Construction produced invalid/empty-looking scene views and the intended WASD/Q/E/right-drag controls did not operate the live camera.
- Rejected the generic-camera-write implementation instead of tuning it further; the failure proves the renderer globals alone are not sufficient evidence for the working detached camera owner.
- Reworked `ConstructionBuildCamera` into a read-only diagnostic session. It no longer writes camera mode, XYZ, pitch/yaw, consumes movement input, or owns placement clicks.
- Added a live `CAM DEBUG [READ ONLY]` window that reports camera mode, effective render XYZ, delta from palette-open baseline, generic camera XYZ/pitch/yaw, and observed Alt/WASD/Q/E key state.
- Added a narrow `Class343.method4302(...)` probe immediately at the existing scene submission seam so diagnostics report the exact XYZ passed to `Class523.method6240(...)` for both the Class411/object-camera path and generic-camera path.
- Next runtime trace: open Construction, capture CAM DEBUG idle, then use the already-working Alt free-camera and capture CAM DEBUG while moving/rotating. That runtime evidence decides the exact owner/path to mirror; no camera values are guessed in this checkpoint.

2026-09-17 - Construction Free Build switched to proven Class24/Class411 owner
- Runtime CAM DEBUG screenshots established that the observed moving camera view remained mode 1 and rendered from the `CLASS411` family while captured generic XYZ values remained zero; source tracing, not the screenshots alone, established the separate developer-freecam owner.
- Source trace identified the existing developer freecam: Ctrl+backtick maps to internal keys 82+28 and activates `Class24.aClass411_Sub1_158` through `Class102_Sub5.method9948(...)`; `IncomingPacket.method4113(...)` exposes active state, `Class24.method711()` updates the camera, and `RSSocket.method7604(...)` closes it.
- Replaced the read-only camera diagnostic session with Construction lifecycle reuse of that exact detached camera. Opening the Construction palette auto-activates it when needed; closing the palette only closes it when Construction owns the activation, preserving any pre-existing manually active freecam.
- Extended the proven `Class24.method711()` movement path with Construction-only W/S/A/D mappings while preserving the original arrow controls and existing engine mouse-look.
- Added Construction-only Shift fast (step 60), Ctrl precision (step 8), default step 25, plus Q/E vertical movement on the same Class411 position target. Q/E direction remains runtime acceptance pending.
- Removed the temporary `CAM DEBUG` render probe/overlay. Future camera diagnostics are transition-only console lines from `ConstructionBuildCamera`.
- Smooth acceleration/deceleration is intentionally deferred until the Class411+WASD path passes runtime acceptance; no legacy Orb interface or generic renderer-global camera ownership was restored.


2026-09-17 - Construction Free Build live-tick input correction
- Runtime acceptance showed only detached-camera activation worked; Construction W/A/S/D/Q/E controls did not move the camera.
- Targeted trace found the Construction movement patch had been added to `Class24.method711()`, but that updater was not reached by the current Construction runtime path. The key mappings themselves were verified unchanged by `Class563.method6650(...)`.
- Restored `Class24.java` to its exact pre-Construction movement state so Construction no longer modifies the stock/decompiled freecam updater.
- Added `ConstructionBuildCamera.tick()` and call it from the live `Class343.method4302(...)` viewport seam immediately before the active Class411 camera transform is submitted. The tick is guarded to once per `client.cycles`.
- The live tick now updates the existing detached `Class24.aClass411_Sub1_158` camera directly with W/S/A/D, existing arrow aliases, Shift fast, Ctrl precision, Q/E vertical movement and the same Class411 mouse-look/position math used by the detached-camera code.
- Added one-shot client-to-server diagnostics through the existing owner-only command bridge. Server console now reports `ENTER`, first live `TICK`, first detected movement `INPUT`, `FAIL` when applicable, and `EXIT`; no on-screen camera debug overlay was reintroduced.
- Camera ownership, ghost ownership and confirmed server-authoritative object placement are unchanged. Runtime acceptance is required for movement direction, Q/E sign, mouse-look, player-stationary behavior and clean close/reopen lifecycle.


2026-09-17 - Construction Free Build live-tick controls runtime verified
- User runtime acceptance confirmed the live `ConstructionBuildCamera.tick()` seam now works: Construction automatically enters detached Free Build and W/S/A/D, Q/E, Shift fast, Ctrl precision and mouse-look all control the live Class411 camera.
- Closing Construction restores the normal camera and reopening Construction starts Free Build cleanly.
- This verifies the core camera control/lifecycle path and closes the previous dead-`Class24.method711()` input failure.
- Remaining Free Build v1 integration checks are intentionally separate: player-stationary build clicks, ghost hover/rotation/piece switching while detached, one authoritative placement/no Walk Here leakage, pre-existing-freecam preservation, bounded server-console diagnostics and combined render stability.
- Smooth acceleration/deceleration, click-to-stop and RTS/top-down/orbit presets remain deferred until that integration pass closes.


2026-09-17 - Construction Free Build smoothing + click-to-stop
- Replaced instant per-cycle camera steps with time-based world-space velocity on the runtime-verified `ConstructionBuildCamera.tick()` seam.
- Added normalized directional input, 1250 normal / 3000 Shift-fast / 400 Ctrl-precision units-per-second targets, exponential acceleration response 10.0 and deceleration response 7.0, plus bounded 5-50 ms delta time to avoid frame stalls causing large jumps.
- Releasing movement now coasts toward zero; near-zero velocity settles exactly to zero to prevent micro-drift.
- Implemented real `stopMovement()`: ground/build confirmation clears velocity immediately and latches movement off until all movement keys are released once, preventing a held key from restarting the camera on the next tick.
- Promoted verified Matrix3 action 23 to the Free Build click owner: while Construction camera is active it calls click-to-stop, optionally confirms the existing Paint placement, and consumes the action so the same click cannot become player Walk Here.
- Outside Construction Free Build, existing Dev Paint action-23 behavior remains unchanged.
- Added a bounded first-session `[ConstructionBuildCamera] STOP click` server-console diagnostic.
- Runtime acceptance is required for smoothing feel, drift/diagonal behavior, click-stop latch, stationary-player placement, one-object-per-click, ghost compatibility and combined render stability.


2026-09-17 - Construction Free Build smoothing runtime verified
- User runtime acceptance confirmed the smoothed Class411 controller works as intended: acceleration/deceleration, normalized diagonal movement, Shift fast and Ctrl precision all feel correct.
- Ground/build click-to-stop is runtime verified, including the held-key latch: movement stops immediately and does not resume until movement keys are released and deliberately pressed again.
- Free Build action-23 ownership is runtime verified for the tested Paint flow: the player remains planted, the same click does not become Walk Here, and exactly one server-authoritative object is created.
- The white/translucent ghost remained stable during the smoothed detached-camera acceptance run, with no observed camera/render instability or ghost duplication/flicker.
- Free Build v1 movement + placement integration is now runtime VERIFIED. Remaining camera-edge checks are limited to pre-existing-freecam preservation and the full bounded server-console diagnostic sequence; the broader ghost completeness checklist remains separate.


2026-09-17 - Phase 1 Bundle 1.2 persistent settlement foundation
- Locked the persistent ownership plan: normal Matrix3 `Player` serialization owns `SettlementState`; classic POH `House` remains separate; `SettlementInstance` owns transient dynamic-map projection only.
- Added `SettlementBuildPiece` starter definitions, `SettlementPlacedPiece` stable plot-relative records and serializable `SettlementState` with stable ids, old-save normalization, same-type slot occupancy protection and move/duplicate/rotate/remove mutations.
- Added `Player.settlementState` for new/existing saves without changing the Player serial UID or creating a parallel save system.
- Added `SettlementInstance`: allocates one 8x8-chunk / 64x64 Matrix3 dynamic region with `MapBuilder.findEmptyChunkBound`, projects the existing blank Construction land chunk, rebuilds saved pieces with `World.spawnObject`, maps live world tiles back to plot-relative state and destroys the runtime map on exit.
- Added `SettlementControler` and registered it with `ControlerHandler`; controller arguments discard the runtime instance immediately so temporary projection ownership is never serialized. Logout/teleport paths clean the runtime projection; stale-controller login recovery returns to a safe normal-world tile.
- Added owner-only `itembrowser settlement enter|exit|status` runtime harness.
- Existing Dev object spawn/move/duplicate/rotate/delete commands are intercepted only inside an active settlement and synchronized with persistent settlement state; outside-settlement Dev behavior is unchanged.
- Added load-failure unlock/cleanup protection and delayed dynamic-map destruction matching Matrix3's existing House lifecycle pattern.
- Updated `docs/rs3/SYSTEM_OWNERSHIP.md` for the new saved/runtime owner split.
- Runtime acceptance required: clean build, enter, place/edit/remove, exit/re-enter exact rebuild, logout/relog persistence, dynamic-map cleanup and classic POH/Dev regressions.


2026-09-17 - Client Console Test Console + Con Revamp harness
- Consolidated Client Console navigation so only Client Console/Home, Owner, Commands, Test Console and Settings remain on the top-level rail.
- Moved Player, Item Browser, Interface Editor, Construction Editor, Client Atlas and Boss Research into lazy-loaded Test Console sub-tabs.
- Added the new `Con Revamp` Test Console sub-tab with one-click Enter Settlement, Settlement Status, Exit Settlement and Open Build Palette controls.
- Con Revamp reuses `ClientConsoleBridge` and the existing owner-only server settlement command bridge; no gameplay/persistence logic was duplicated client-side.
- Legacy saved active-panel ids for moved tools normalize to Test Console for layout compatibility.
- Added a dedicated Test Console rail glyph and kept Java 8/Swing ownership consistent with the existing Client Console shell.
- Runtime acceptance required for rail layout, lazy sub-tabs, Con Revamp buttons and existing moved-tool behavior.


2026-09-17 - Test Console + settlement runtime acceptance verified
- User runtime confirmed the consolidated Client Console rail is live with Test Console as a top-level tool and Con Revamp / Construction / Player / Items / Interfaces / Atlas / Boss Research visible as sub-tabs.
- Con Revamp Enter Settlement successfully queues the existing server bridge and enters the new private settlement runtime.
- Server runtime reports the expected prepare/load sequence and `Settlement loaded: 0 saved build pieces.` on an empty state.
- Exit/re-enter is working at the runtime harness level.
- Placement-count, exact layout rebuild and logout/relog persistence remain pending and are the next Bundle 1.2 acceptance gate.


2026-09-17 - Bundle 1.2 persistence core runtime verified
- User runtime confirmed settlement placement/layout survives leaving and re-entering the private settlement instance.
- User runtime confirmed the same saved settlement state survives logout/relog and rebuilds after entering again.
- This runtime-verifies the core ownership chain: `Player.settlementState` persists plot-relative build state through Matrix3 player serialization while `SettlementInstance` remains transient and rebuilds the live dynamic-region projection.
- Bundle 1.2 remains ACTIVE for persistent move/rotate/duplicate/delete acceptance, occupancy/invalid-placement rules, status accounting and regression coverage.


2026-09-18 - Bundle 1.2 deterministic state self-test
- Added `SettlementStateSelfTest`, a disposable in-memory test that exercises the real persistent-state API: place, same-layer occupancy rejection, compatible floor/wall coexistence, rotate, move, duplicate, delete, Java serialize/deserialize and exact restored-record checks.
- The self-test never reads or mutates `Player.settlementState`; it is safe to run repeatedly and reports bounded PASS/FAIL output to game chat and the server console.
- Added owner-only `itembrowser settlement selftest` and `itembrowser settlement list` bridge operations.
- Added Con Revamp `State Self-Test` and `Saved Pieces` buttons so Bundle 1.2 edit/persistence checks no longer require raw commands.
- Runtime acceptance still required for the self-test PASS result and live move/rotate/duplicate/delete projection persistence across exit/re-entry.


2026-09-18 - Bundle 1.2 state self-test runtime verified
- User runtime confirmed the Con Revamp `State Self-Test` returns PASS.
- This runtime-verifies the real `SettlementState` mutation/serialization API for place, same-layer occupancy rejection, compatible-layer coexistence, rotate, move, duplicate, delete and Java serialize/deserialize.
- The self-test remains disposable and does not touch the player's real settlement save.
- Live runtime projection/edit persistence across exit/re-entry remains the next Bundle 1.2 acceptance gate.


2026-09-18 - Bundle 1.2 live edit persistence runtime verified
- User runtime confirmed live settlement object move, rotate, duplicate and delete operations persist through the real `SettlementState` owner.
- Exit/re-entry rebuild reproduces the edited layout correctly: moved/rotated pieces return in their edited state, duplicates remain present and deleted pieces remain absent.
- This closes the live edit-synchronization portion of Bundle 1.2; remaining checks are occupancy/invalid placement, status accounting and outside-settlement/classic-POH regressions.


2026-09-18 - Bundle 1.2 persistent validity hardening
- Moved plot-location validity into `SettlementState` itself: place/move/duplicate now reject X/Y outside 0-63 and any plane other than 0 before persistent mutation.
- `SettlementInstance` now derives plot size/plane from `SettlementState` and skips invalid saved records instead of projecting corrupt coordinates.
- Extended `SettlementStateSelfTest` with negative/out-of-range coordinate, wrong-plane, unknown-definition, invalid-move and invalid-duplicate rejection checks.
- Added `SettlementStateAudit`, a non-mutating audit of the player's real save for unique ids, approved definitions, valid locations/rotations and same-layer occupancy invariants.
- Added owner-only `itembrowser settlement audit` and a Con Revamp `Saved State Audit` button; updated the Con Revamp card into the final Bundle 1.2 gate.
- Runtime acceptance remains: expanded self-test PASS, real-save audit PASS, Status count, outside-settlement Dev smoke and classic POH smoke.


2026-09-18 - Bundle 1.2 final auto check
- Added `SettlementBundle12FinalCheck`, a non-mutating aggregate confidence check that runs the expanded disposable state self-test, audits the player's real saved settlement, verifies saved-piece count consistency and validates definition-registry uniqueness/lookups.
- Added owner-only `itembrowser settlement finalcheck` and a Con Revamp `Final Auto Check` button.
- Bundle 1.2 now has only three runtime completion checks: Final Auto Check PASS, ordinary Dev Mode smoke outside the settlement, and classic POH enter/build/leave smoke.


2026-09-18 - Bundle 1.2 duplicate regression cleanup
- User clarified that ordinary Dev Mode outside the settlement and classic POH enter/build/leave regressions had already been runtime tested successfully.
- Updated the Bundle 1.2 gate to stop requesting duplicate runtime work.
- Only the newly-added `Final Auto Check` remains unverified before Bundle 1.2 can close.


2026-09-18 - Bundle 1.2 complete
- User runtime confirmed `Final Auto Check` PASS on the real loaded settlement with 3 saved pieces.
- This closes expanded state validity, real-save invariants, saved-piece count consistency and definition-registry integrity.
- Previously verified persistence, live move/rotate/duplicate/delete rebuilds, outside-settlement Dev Mode and classic POH regressions remain accepted.
- Bundle 1.2 is DONE. Bundle 1.3 starter resources/storage is now ACTIVE.


2026-09-18 - Bundle 1.3 starter resource/storage foundation
- Added stable settlement-only `SettlementResource` identities for Wood, Food, Stone and Basic ore.
- Upgraded `SettlementState` to schema v2 with old-save-safe persistent resource totals and a 200-unit starter storage capacity; add/remove/capacity APIs never create normal inventory/bank items.
- Added four fixed starter `SettlementResourceNode` definitions on reserved plot tiles and transient spawning/cleanup through `SettlementInstance`.
- Added `SettlementGatherAction`; exact resource-node clicks are intercepted by `SettlementControler` and deposit directly into settlement storage after a short action/animation.
- Build place/move/duplicate rejects reserved resource-node tiles.
- Added disposable `SettlementResourceSelfTest`, owner-only resource status/self-test commands and Con Revamp Resource Status/Resource Self-Test buttons.
- Provisional node visuals: wood object 1276, food NPC 327, stone object 11933, ore object 11936. Runtime acceptance may replace art ids without changing ownership.
- Runtime acceptance required for build, node visibility/options, each gather path, storage isolation/persistence, capacity and node cleanup.


2026-09-18 - Bundle 1.3 starter resources runtime verified
- User runtime confirmed the starter resource/storage slice works in the live settlement; Resource Self-Test passes and the current four starter nodes are usable with isolated settlement storage.
- Current provisional tree/fishing/rock visuals are accepted for the prototype; final art can change independently of resource ownership.


2026-09-18 - Bundle 1.3 starter shelter milestone
- Added stable `SettlementBuildRole` semantics (WALL/FLOOR/DOOR) to keep progression rules independent from provisional object ids.
- Added generic persisted `SettlementMilestone` keys and advanced `SettlementState` to schema v3 with old-save-safe milestone normalization.
- Starter shelter requires 4 walls, 4 floors, 1 doorway and at least 1 Wood/Food/Stone/Basic ore; completion is one-time and permanently latched.
- `SettlementInstance` automatically checks completion after load, build placement/duplication and starter-resource gathering and emits a one-time completion message.
- Added disposable `SettlementShelterSelfTest`, owner-only shelter status/self-test commands and Con Revamp Shelter Status/Shelter Self-Test controls.
- Runtime acceptance required for self-test PASS, real automatic completion, latch behavior and save/load persistence.


2026-09-18 - Bundle 1.3 final one-click gate
- Added `SettlementBundle13FinalCheck`, aggregating the disposable Resource/Shelter self-tests, real saved-state audit, four live starter-node availability checks, real resource minimums and the real starter-shelter milestone.
- The final check never fabricates settlement progress. If the real state is incomplete it returns `NOT READY` with the exact shelter progress; it may latch completion only after the authoritative requirements are already satisfied.
- Added owner-only `itembrowser settlement bundle13check` and a Con Revamp `Bundle 1.3 Final Check` button.
- Bundle 1.3 remains ACTIVE until this real-state final check is runtime verified PASS.


2026-09-18 - Bundle 1.3 complete
- User runtime confirmed the real starter-shelter milestone completed automatically when the final required starter resource was gathered.
- Real accepted state at completion: 9 walls, 5 floors, 1 doorway, Wood=1, Food=1, Stone=1, Basic ore=1.
- `Bundle 1.3 Final Check` returned PASS on the live loaded settlement, covering Resource Self-Test, Shelter Self-Test, real saved-state audit, live starter nodes, real resource minimums and the completed starter-shelter milestone.
- Bundle 1.3 is DONE. Bundle 1.4 first-worker vertical slice is now ACTIVE.


2026-09-18 - Bundle 1.4 Worker #1 arrival foundation
- Added stable `SettlementWorkerDefinition.STARTER_SETTLER` and serializable `SettlementWorkerState` identity using worker id + definition key + custom name + plot-relative home tile.
- Advanced `SettlementState` to schema v4 with old-save-safe worker-list/next-id normalization and idempotent `ensureStarterWorker()` gated by `STARTER_SHELTER`.
- Added transient `SettlementWorkerNpc` on Matrix3 NPC authority; the first slice disables random walk/combat interaction until explicit worker AI is added.
- `SettlementInstance` now auto-creates/rebuilds Worker #1 after the starter milestone, guarantees one live NPC per saved worker id, reserves the arrival tile and cleans runtime workers on instance destroy.
- Added disposable `SettlementWorkerSelfTest`, real-state `SettlementWorkerArrivalCheck`, owner-only worker status/self-test/check commands and Con Revamp Worker controls.
- Runtime acceptance required for current NPC presentation, automatic arrival, 1/1 saved/runtime count and same-id rebuild after re-entry.


2026-09-18 - Worker #1 first arrival runtime verified
- User runtime confirmed entering the completed settlement automatically spawns the settler NPC.
- `Worker Self-Test` returned PASS for milestone gating, stable worker identity, duplicate suppression and serialization.
- `Worker Arrival Check` returned PASS with exactly one persistent Worker #1 and exactly one matching live NPC projection.
- Exit/re-enter same-id rebuild remains the only unfinished acceptance item for the Worker #1 arrival foundation before Allowed Jobs.


2026-09-18 - Worker #1 re-entry persistence runtime verified
- User runtime confirmed settlement exit/re-entry rebuilds the same persistent Worker #1 rather than creating a duplicate.
- Worker Status after re-entry reports saved=1/runtime=1 and Worker #1 id 1, definition `starter-settler`, home plot 24,12,0.
- Worker Arrival Check returns PASS after re-entry, closing the Worker #1 arrival foundation.
- Bundle 1.4 now advances to Allowed Jobs persistence/controls before gather/haul AI.


2026-09-18 - Bundle 1.4 Allowed Jobs foundation
- Added stable `SettlementWorkerJob` keys for Gather Wood, Gather Food, Gather Stone, Gather Basic Ore and Haul.
- Added persistent `SettlementWorkerState.allowedJobs`; all jobs default OFF, old worker saves repair a null allowlist and unknown keys are pruned during normalization.
- Advanced `SettlementState` to schema v5 for the per-worker policy slice.
- Added owner-only Worker Jobs status, single-job ON/OFF, enable-all/disable-all and disposable `SettlementWorkerJobsSelfTest` paths.
- Added a dedicated Con Revamp Allowed Jobs card with five checkbox command controls plus authoritative Jobs Status/Self-Test and all-on/all-off controls.
- Runtime acceptance required for self-test PASS, mixed allowlist readback, exit/re-entry persistence and logout/relog persistence before gather/haul AI consumes the policy.

2026-09-19 - Bundle 1.4 worker gather/haul vertical slice
- Runtime-accepted the existing Allowed Jobs controls/self-test/readback as the policy gate for Worker #1.
- Added transient worker AI on `SettlementWorkerNpc`: persistent Allowed Jobs are read live, while current target/path/gather timer/carried cargo remain runtime-only.
- Worker #1 now uses Matrix3 basic routefinding to existing starter resource nodes, performs the existing node animation, carries one unit, and deposits only after the Haul permission is allowed and the worker returns to its home/storage access tile.
- Haul-off and full-storage states retain carried output and expose readable status; re-enabling Haul/freeing storage resumes delivery.
- Multiple allowed gathering jobs are selected round-robin across available starter nodes to avoid first-job starvation.
- Added owner-only `itembrowser settlement workerai` plus Con Revamp `Worker AI Status` and a consolidated runtime acceptance checklist.
- No new persistence schema, inventory/bank path, alternate movement owner, resource-node owner, or client gameplay authority was introduced.

2026-09-19 - Bundle 1.4 worker route correction
- Runtime test showed the worker AI activated correctly but stalled at `MOVING_TO_RESOURCE | No Path to Wood node`; no gather/haul output occurred.
- Targeted source inspection verified the original `Entity.findBasicRoute(...)` choice is a greedy/basic stepper that can stop when a direct step is blocked.
- Replaced only the worker route call with Matrix3's existing intelligent `calcFollow(..., intelligent=true)` path, which delegates to `RouteFinder` / `FixedTileStrategy`.
- No worker policy, persistence, resource ownership, haul semantics, NPC ownership or force-teleport fallback changed.
- Runtime retest is required to confirm the Wood route now completes before continuing the existing haul-toggle acceptance.

2026-09-19 - Bundle 1.4 tree-target routing correction
- User runtime confirmed gather/haul works for the other starter resources while the tree remained the lone failure.
- Targeted trace found the worker converted every resource node to a synthetic adjacent `WorldTile`; intelligent `calcFollow(...)` therefore still used `FixedTileStrategy` and never received the tree object footprint/access semantics.
- Replaced the synthetic approach tile with the actual live resource target: object nodes return their live `WorldObject` and NPC nodes return the matching live resource NPC.
- Matrix3 `calcFollow(..., intelligent=true)` now selects `ObjectStrategy` for the tree/rocks and `EntityStrategy` for the food NPC.
- Corrected route completion handling so a successful strategy result with zero queued steps means the worker is already in interaction range rather than `No Path`.
- No Allowed Jobs, persistence, resource totals, haul semantics, node definitions or force-teleport behavior changed.

2026-09-19 - Bundle 1.4 tree interaction-ready correction
- Runtime screenshot confirmed Worker #1 now reaches and stands on a valid adjacent tile from which the player can cut tree id 1276; route movement itself is therefore working.
- The remaining failure was the worker state machine not recognizing that adjacent tile as interaction-ready, so it never advanced from movement into gathering.
- Updated `walkToward(...)` to treat a live target within one tile as reached whenever no movement steps remain, both before and after intelligent RouteFinder evaluation.
- This keeps Matrix3 routing ownership intact and changes only the gather-state handoff; no node ids, persistence, resource crediting, haul logic or teleport fallback changed.

2026-09-19 - Bundle 1.4 tree strategy-handoff correction
- Runtime retest showed the manual one-tile adjacency handoff still left Worker #1 idle at a tile where the player can cut tree id 1276.
- Rejected the anchor-distance check for object interaction readiness; multi-tile/object access is owned by Matrix3 routing strategy semantics, not raw target-anchor distance.
- `walkToward(...)` now trusts intelligent `calcFollow(...)`: when the live `WorldObject` / NPC route succeeds and queues zero steps, the worker immediately treats the target as interaction-ready and enters gathering.
- Added a source comment preserving this Matrix3 route contract so the same regression is not reintroduced.
- No changes to node ids, Allowed Jobs, persistence, storage accounting, haul behavior or movement ownership.

2026-09-19 - Bundle 1.4 tree gather/haul runtime verified
- User runtime confirmed the final strategy-owned handoff correction works for tree id 1276.
- Worker #1 now reaches the valid tree interaction position and proceeds into the gather/haul loop instead of stalling.
- Tree-specific routing/handoff is closed; remaining Bundle 1.4 gather/haul acceptance is limited to Haul OFF/ON hold-resume, gather-disable stop behavior and multi-job rotation.

2026-09-19 - Bundle 1.4 worker needs vertical slice
- Advanced SettlementState to schema v6 and added persistent Worker #1 Hunger/Thirst/Energy with old-save normalization to satisfied/rested defaults.
- Completed gather cycles now apply bounded need costs; critical needs gate new work after sensible carried-resource delivery.
- Critical Hunger returns home and consumes exactly 1 settlement Food; no Food reports a readable blocked state and automatically retries when Food is supplied.
- Critical Thirst uses an explicit Phase-1 starter-shelter basic water seam pending later physical wells/barrels/tanks.
- Critical Energy returns home and rests for a bounded recovery delay.
- Added disposable SettlementWorkerNeedsSelfTest plus owner-only needs status/set/reset commands and Con Revamp buttons for consolidated runtime acceptance.
- Runtime carry/action/recovery timers remain transient; persistent identity, Allowed Jobs, needs and settlement resource ownership remain server-authoritative.

2026-09-19 - Bundle 1.4 gather/haul + worker needs runtime accepted
- User confirmed the consolidated runtime pass works.
- Gather/haul behavior is accepted, including Worker AI state flow, Haul hold/resume, gather-disable stop behavior and multi-job rotation.
- Worker Needs self-test returned PASS; live work cycles changed Hunger/Thirst/Energy as expected.
- Critical Hunger, Thirst and Energy recovery paths and Reset Needs all worked in the live settlement.
- Explicit zero-Food blocking and restart-persistence checks remain carryover for the upcoming persistence/basic-XP slice to avoid another dedicated launch.
- Canonical milestone `First worker, Allowed Jobs, gathering and hauling` is now DONE.

2026-09-19 - Bundle 1.4 persistent worker progression + Construction XP
- Advanced SettlementState to schema v7 and added persistent Worker skill XP with stable Woodcutting, provisional Food Gathering, Mining and Hauling identities.
- Current gather jobs award 12 personal XP to their mapped skill; successful stored output awards 6 Hauling XP per deposited resource.
- Worker levels use the RuneScape-style 1-99 XP curve; this slice does not yet change speed/yield from skill level.
- Productive worker output now awards exactly 1 base Construction XP per resource only after successful authoritative storage deposit via Matrix3 Skills.addXp(CONSTRUCTION, ..., true).
- Idle/waiting, Haul-disabled, storage-full and failed-deposit paths award no passive Construction XP.
- Developer placement still awards no Construction XP; active-build XP is deferred until the material-consuming player build path exists.
- Added SettlementWorkerProgressionSelfTest, owner-only progression status, and Con Revamp Progress Status/Self-Test controls.
- Remaining zero-Food and restart-persistence carryover is bundled into the progression runtime acceptance pass.

2026-09-19 - Bundle 1.4 worker progression productive path runtime verified
- User confirmed the progression slice works in the live settlement.
- Progress Self-Test returned PASS for stable skills, job mapping, XP gain, level curve and serialization.
- Live worker progression matched job ownership: Food Gathering increased 0 -> 12 XP, Mining 0 -> 24 XP, and Hauling 6 -> 24 XP while productive work completed.
- Player Construction XP increased 187522810 -> 187522811 after the next successful stored output and then -> 187522814 after further productive deposits, confirming the 1 base XP per stored resource award seam.
- Productive progression/XP does not need re-testing. Remaining Bundle 1.4 runtime gate is blocked/idle no-XP behavior plus exit/re-entry and logout/relog persistence; zero-Food block/resupply remains optional carryover.

2026-09-19 - Bundle 1.4 final persistence-gate harness
- Added non-mutating SettlementBundle14FinalGate process-local snapshots keyed by username; no player/settlement save fields were added.
- Baseline capture requires a loaded settlement, Worker #1, all Allowed Jobs OFF and no critical need so Worker/Construction XP plus Needs remain stable while idle.
- Baseline comparison checks exact worker identity, Allowed Jobs, Hunger/Thirst/Energy, every personal Worker skill XP total and player Construction XP.
- The same snapshot can be checked before exit, after settlement rebuild, and after normal logout/relog while the server process stays running.
- Added Con Revamp Capture Stable Baseline / Check Baseline controls so the remaining Bundle 1.4 negative-XP + persistence gate can be completed in one short runtime session.

2026-09-19 - Bundle 1.4 first-worker gate runtime closed
- Stable-baseline final gate passed while idle, after settlement exit/re-entry, and after normal logout/relog without server restart.
- Verified no Worker skill XP or player Construction XP changes during stable all-jobs-OFF idle time.
- Verified Worker #1 identity, Allowed Jobs, Hunger/Thirst/Energy and all personal skill XP survive runtime-instance rebuild and player save/load exactly.
- Marked Bundle 1.4 DONE and advanced the Phase-1 execution target back to remaining ghost/placement edge acceptance plus the consolidated persistence/world-object smoke pass.
- Zero-Food Hunger block/resupply remains non-blocking carryover.

2026-09-19 - Phase 1 acceptance reconciliation
- Reconciled stale Construction test boxes against already-recorded source/runtime evidence; no gameplay behavior changed.
- Closed one-preview-per-cycle/no-flicker, exactly-one authoritative placement, ghost camera/render stability, worker-id logout/relog persistence and intentional preset/validity-color deferrals.
- Resume target is now only the still-unverified ghost interaction edges, followed by the consolidated persistence/world-object smoke pass.

2026-09-19 - Phase 1 Bundle 1.5 player build materials + active Construction XP
- Added definition-owned Phase-1 build costs/XP: Wooden fence 1 Wood/4 XP, Floor decoration 1 Wood/4 XP, Door 2 Wood/8 XP.
- Added server-owned `SettlementInstance.placePlayerPiece(...)` that validates placement, consumes settlement storage material, persists/projects the piece, then awards active base Construction XP exactly once.
- Added normal-player `settlementbuild` command gated by an active settlement; the client sends stable build-piece keys rather than raw object ids for Construction gameplay placement.
- Reused the verified Dev placement input/menu plumbing only on the client; owner-only `itembrowser devspawn` remains a separate no-cost/no-XP development harness.
- Invalid, occupied, reserved or underfunded player placement changes neither materials nor active Construction XP.
- Bundle 1.5 is IMPLEMENTED / NEEDS RUNTIME TEST; the two canonical main-goal areas remain In Progress.

2026-09-19 - Bundle 1.5 positive active-build path runtime verified
- User runtime confirmed Wooden fence = 1 Wood / 4 base Construction XP.
- User runtime confirmed Floor decoration = 1 Wood / 4 base Construction XP.
- User runtime confirmed Door = 2 Wood / 8 base Construction XP.
- Occupied placement returned the authoritative `That settlement slot is already occupied.` rejection; source order keeps that rejection before material consumption and XP award.
- Bundle 1.5 is PARTIAL RUNTIME VERIFIED; remaining checks are insufficient Wood, invalid/reserved rejection, exit/re-entry persistence and outside-settlement command rejection.

2026-09-19 - Bundle 1.5 final-gate harness
- Extracted atomic persistent placement + settlement-material mutation into `SettlementPlayerBuildTransaction`; runtime projection and player XP remain owned by `SettlementInstance`.
- Added disposable `SettlementBundle15FinalCheck` self-test using the exact gameplay transaction for configured costs and insufficient/reserved/invalid/occupied no-mutation behavior.
- Added process-local Bundle 1.5 build baseline capture/check for exact saved piece signatures, settlement resource totals and Construction XP across exit/re-entry.
- Added Con Revamp Bundle 1.5 Self-Test, Capture Build Baseline, Check Build Baseline and ordered Exit + Outside Rejection controls.
- Short-consume rollback now restores any partial material removal as well as removing the provisional persistent piece before failure.
- No worker AI, Phase 2 population, inventory/bank material path or devspawn authority changed.

2026-09-19 - Bundle 1.5 runtime gate complete
- `Bundle 1.5 Self-Test` returned PASS for exact configured costs and insufficient/reserved/invalid/occupied no-mutation behavior.
- Build baseline captured 19 saved pieces with Wood=81, Food=12, Stone=18, Basic ore=28 and Construction XP=187522868.
- After settlement exit/re-entry, `Check Build Baseline` returned PASS with the exact same saved layout, resource totals and Construction XP.
- `Exit + Outside Rejection` returned `You must be inside your settlement to build.` after leaving the settlement.
- Bundle 1.5 is DONE / runtime verified. Phase 1 now moves to a stale-checklist/remaining-smoke closure audit rather than new feature work.

2026-09-19 - Phase 1 closure audit
- Reconciled stale prototype/debug checklist debt against recorded runtime evidence and the user's completed edge/persistence smoke confirmations.
- Marked ghost hover/rotation/model-switch/terrain/cancel edges and resource/shelter/world-object persistence carryovers accepted; Bundle 1.4 status corrected to DONE / runtime verified.
- Historical diagnostic-only Console/Camera/Test Console checks are retained as non-blocking archaeology rather than active Phase-1 gates.
- Updated the obsolete pre-Bundle-1.5 XP note: devspawn remains no-cost/no-XP, while material-consuming player builds own active Construction XP.
- The only remaining blocking Phase-1 acceptance item is Eclipse clean/build for current Client + Server under the protected Java 8 project settings.
- Zero-Food Hunger block/resupply remains optional non-blocking carryover.

2026-09-19 - Phase 1 closed / Phase 2 queued
- Accepted the final build/startup gate from current runtime evidence: Client + Server launched and executed the newly added Bundle 1.5 code with no compile/verifier errors.
- Phase 1 MVP is DONE / runtime accepted; no blocking Phase-1 acceptance item remains.
- Canonical main-goal status now marks the freeform settlement building foundation and persistence/Construction-XP ownership complete.
- Added Phase 2 Bundle 2.1 planning for population capacity + additional-worker recruitment; status is READY / NOT STARTED and no Phase-2 gameplay code changed.
- Zero-Food Hunger block/resupply remains optional non-blocking carryover.

2026-09-19 - Phase 2 Bundle 2.1 population capacity + Worker #2
- `SettlementState` now derives starter population capacity as 0 before shelter / 2 after the completed starter shelter; existing worker list + nextWorkerId remain the only persistent worker owner.
- Added stable `RECRUITED_SETTLER` definition/home slot and shelter/capacity-gated Worker #2 recruitment with default Allowed Jobs OFF; third-worker recruitment is rejected at starter capacity.
- `SettlementInstance` now projects every saved worker exactly once by stable worker id, while preserving automatic Worker #1 arrival and existing Worker AI/needs/progression behavior.
- Updated Worker #1 Arrival Check so legitimate additional workers do not invalidate the Phase-1 starter-worker regression check.
- Added disposable/live `SettlementPopulationCheck` coverage for capacity, recruitment, unique ids, serialization and saved=2/runtime=2 projection ownership.
- Added Con Revamp Population Status / Recruit Worker #2 / Population Self-Test / Population Check controls.
- Bundle 2.1 is IMPLEMENTED / NEEDS RUNTIME TEST; housing/beds and broader production remain the next Phase-2 work after multi-worker persistence is accepted.

2026-09-19 - Phase 2 Bundle 2.1 runtime verified
- Population Self-Test returned PASS for capacity/recruitment/unique-id/default-job/serialization ownership.
- Live Worker #2 recruitment succeeded; population reached workers=2/2 / recruitment=FULL.
- Population Check returned PASS with saved=2/runtime=2 and unique Worker #1/#2 identities/projections.
- User completed logout/relog + settlement re-entry; the two-worker state rebuilt without duplicate runtime NPCs and Worker #2 remained idle under default-OFF Allowed Jobs.
- Bundle 2.1 is DONE / runtime verified.
- Workflow decision: batch future compatible Phase-2 work into larger 4–6 patch bundles and consolidate runtime verification into one launch when safe.

2026-09-19 - Phase 2 Bundle 2.2 multi-worker control + concurrent work
- Extended owner-only worker commands with optional stable worker-id targeting for Jobs, Needs, AI and Progress while preserving legacy Worker #1 syntax.
- Added Con Revamp selected Worker ID control; Allowed Jobs, Needs, AI and Progress controls now target the selected persistent worker, and worker changes clear local checkbox visuals to avoid stale cross-worker UI state.
- Worker #2 uses the existing SettlementWorkerNpc gather/haul/needs/progression loop through its own persistent policy; no duplicate AI owner was introduced.
- Added workerallstatus for bounded all-worker jobs/needs/skills/live-AI/shared-storage readback.
- Added SettlementBundle22FinalGate disposable targeting/serialization self-test plus read-only process-local two-worker persistence baseline/check.
- Bundle 2.2 runtime target is one consolidated launch: W1 Food+Haul + W2 Wood+Haul concurrent work, stop-one/other-continues, then exit/re-entry and logout/relog baseline PASS.
- No housing/capacity expansion, resource-node ownership, save-schema format or Phase-1 building behavior changed.
- Post-review: synchronized the fallback settlement command help string with the active Bundle 1.5/2.1/2.2 command surface; no runtime ownership changed.

2026-09-19 - Con Revamp active-workflow cleanup + scroll fix
- Removed completed Phase-1 Bundle 1.4/1.5 and Phase-2 Bundle 2.1 cards from the Con Revamp tab; retained server commands for future regression use.
- Slimmed Settlement controls to Enter/Exit/Status/Open Build Palette and removed already-proven state/resource/shelter/Worker #1 self-test clutter.
- Removed proven Jobs/Needs/Progress self-test buttons while keeping active selected-worker status/control actions needed by Bundle 2.2.
- Made active panel buttons non-focusable and preserve/restore JScrollPane viewport position around Test output updates to stop click-induced downward scrolling.
- No settlement gameplay, worker AI, persistence, command authority or save data changed.

2026-09-20 - Bundle 2.2 separated resource storage
- Replaced active shared 200-cap deposit logic with per-resource starter capacities: Wood/Food/Stone/Basic ore = 100 each; existing stored totals are preserved without truncation.
- Kept the legacy serialized shared-cap field for old-save compatibility, but runtime capacity now comes from `SettlementResource` through `SettlementState.getStorageCapacity(resource)`.
- Worker hauling now checks the carried resource's remaining capacity; full Wood no longer blocks Food/Stone/Ore hauling.
- Resource summaries now report amount/capacity per resource and aggregate total/total-capacity.
- Updated Resource Self-Test and Bundle 2.2 Self-Test to prove full Wood storage does not block Food.
- Updated Con Revamp Bundle 2.2 instructions for the separated-storage acceptance path.

2026-09-20 - Bundle 2.2 storage-aware gather selection
- Workers now skip gather jobs whose target resource storage is already full.
- In-flight gather is canceled before cargo creation if that resource storage fills while the worker is moving/gathering.
- Multi-job workers may continue with another allowed gather resource that still has capacity; all-full workers report a clear storage-full idle reason.
- Added `SettlementState.hasStorageSpace(resource)` and reused it through SettlementInstance/WorkerNpc; Resource Self-Test now asserts the per-resource availability helper.
- Updated Bundle 2.2 runtime instructions to expect `carried=none` after Wood reaches 100/100.

2026-09-20 - Bundle 2.2 in-flight storage reservation race fix
- Runtime screenshot proved a final-slot concurrency race: Wood reached 100/100 while Worker #2 was already hauling Wood x1, leaving that worker stopped with undepositable cargo.
- Added transient per-worker/per-resource storage reservations owned by SettlementInstance; reservations are not persisted.
- Workers reserve capacity before gathering, retain it while carrying, and consume/release it through the authoritative deposit path.
- Competing workers cannot overbook the same final resource slot; reservations remain resource-specific so Wood never blocks Food/Stone/Ore.
- Gather cancel/job disable/need interruption/runtime recreation/instance teardown release stale reservations.
- Strengthened Bundle 2.2 Self-Test with the exact final-slot contention scenario.

2026-09-20 - Bundle 2.2 persistent worker Pause/Resume
- Added persistent per-worker paused policy independent of Allowed Jobs; existing saves default false without a schema migration.
- Paused workers stop productive gather/haul work, cancel active gather reservations, retain carried cargo without holding a storage reservation, and continue needs/recovery processing.
- Resume reuses the unchanged saved Allowed Jobs policy.
- Added worker-id-targeted workerpause command plus Con Revamp Pause Worker / Resume Worker controls and pause readback in all-worker/live-AI status.
- Extended Bundle 2.2 disposable serialization and live baseline/check to cover independent worker pause state.

2026-09-20 - Construction RTS camera mode
- Extended the verified detached Class24/Class411 Construction camera with explicit Free Build and RTS modes; the live Class343 viewport hook and Matrix3 camera ownership remain unchanged.
- RTS preserves the current horizontal heading on mode entry, applies a deterministic 52-degree downward pitch, pans on the ground plane with WASD/arrow keys, rotates with Q/E, and retains Shift fast / Ctrl precision speed modifiers.
- Added bounded mouse-wheel zoom along the RTS viewing line; world wheel is consumed for zoom only in RTS mode.
- Preserved Free Build controls and wheel-to-piece rotation; R / Shift+R continues to rotate build pieces in either camera mode.
- Added Free/RTS selector buttons and mode-specific control hints to the Construction palette.
- Static changed-file checks passed; RTS camera feel/orientation/zoom and Free Build regression remain pending one consolidated client runtime pass.
2026-09-20 - Added RADIAL_WORKER_SELECTION.md design spec for world-space drag-scaled worker selection and linked it from PROJECT.md.
2026-09-20 - RWS-1 reticule scale proof: added client-only GFX 4171 direct-render preview with per-instance X/Z scaling, world-hover anchoring, Con Revamp controls and targeted runtime checks; combat/cache/server ownership remains unchanged.
2026-09-20 - RWS-1 runtime verified: GFX 4171 direct-rendered outside combat and the same reticule model scaled cleanly at 25%, 200% and 725% while remaining world/terrain anchored; single-asset radial selection foundation is proven.
2026-09-20 - RWS-2 radial drag input: replaced the RWS-1 proof UI with configurable Left/Right Worker Control; ground press captures the action-23 world origin, live AWT drag scales the proven GFX 4171 radius with world-distance calibration, release commits, Escape cancels while preserving the prior commit, and Disable returns mouse ownership.

2026-09-20 - RTS camera ground-clipping correction
- First runtime showed RTS pitch activating at player/terrain height, causing near-plane terrain triangle artifacts.
- RTS entry now backs the detached camera 3600 units opposite the actual Class411 look vector before normal input begins.
- Replaced guessed RTS pan/zoom axis signs with the real Class658_Sub2.method7736(...) view direction.
- Zoom is constrained to a safe -5600..-1400 relative travel band so normal wheel input cannot drive the camera back into the terrain plane.
- Free Build ownership and controls are unchanged; RTS requires one focused runtime retest.
2026-09-20 - RWS-2 release cleanup: the large radial reticule now renders only during an active mouse drag; releasing commits origin/radius data for later worker resolution but hides the area ring immediately.

2026-09-20 - RTS vertical sign correction
- Runtime clarification confirmed the RTS camera was below the map looking through terrain, not merely intersecting the near plane at ground height.
- Corrected the fixed RTS pitch sign for Class658_Sub2.method8927(...), which negates its Y target internally.
- Existing real-view-vector backoff/zoom correction remains in place; RTS should now initialize above the terrain and look downward.

2026-09-20 - RTS camera runtime verified
- User confirmed the corrected RTS camera works after the Class658_Sub2 vertical-sign fix.
- RTS now initializes above the terrain with the intended downward view; the prior under-map/underside rendering failure is closed.
- Camera mode is accepted as runtime VERIFIED for the current Free/RTS Construction scope.

2026-09-20 - RTS default + pivot-orbit camera revision
- Construction palette sessions now explicitly select RTS before camera entry, so RTS is the default on every open; Free Build remains manually selectable.
- Replaced RTS Q/E in-place yaw with pivot-orbit behavior: activation stores the pre-backoff world point as the focus pivot, Q/E changes yaw and moves the camera around that pivot at the current orbit distance.
- RTS WASD/arrow pan now translates both camera and pivot together so subsequent orbit rotation follows the newly managed area.
- Mouse-wheel zoom now changes bounded orbit distance relative to the pivot instead of accumulating free travel.
- Preserved the runtime-verified Class411 camera owner, fixed RTS pitch, safe above-terrain initialization, Shift/Ctrl speed modifiers, Free Build controls and mode-specific wheel ownership.
- Static changed-file checks passed; default-open, full-orbit stability, pan-then-orbit, zoom-pivot stability and Free Build regression require one focused client runtime pass.

2026-09-20 - Bundle 2.2 selected-worker role presets
- Added server-authoritative Lumberjack, Forager, Stone Miner, Ore Miner, Hauler Only and Idle presets as exact writes into existing Allowed Jobs state; no persistent role field or second AI owner added.
- Added worker-id-targeted `workerpreset` command plus inferred Preset=<name>/Custom readback in Jobs Status and All Worker Status.
- Added Con Revamp selected-worker Role Preset dropdown + Apply button with local checkbox mirroring; server command remains authoritative.
- Strengthened Bundle 2.2 Self-Test to round-trip all six preset definitions and serialize W1 Forager / W2 Lumberjack while preserving independent pause/needs/progression.
- Static branch audit found later radial UI edits dropped the previously documented Con Revamp Pause/Resume buttons; server Pause/Resume remains intact and UI restoration is recorded as non-blocking carryover.


2026-09-20 - RWS-2 RTS midpoint drag geometry
- Changed radial selection from press-point-centered radius growth to RTS edge-A/edge-B geometry: center is the live world-space midpoint and radius is half of the calibrated A-to-B span.
- Reused the existing action-23 world-hover direction plus AWT pixel/world calibration so midpoint movement remains smooth between tile-boundary recalibrations; no new scene picker was added.
- Reticule rendering now accepts fractional world-tile centers and samples terrain at that moving midpoint.
- Replaced the artificial 12-tile radius stop with a 64-tile safety cap, which does not constrain normal use across the 64x64 starter settlement.
- Release commits edge A + final center/radius internally and still hides the large area ring; Escape/Disable ownership behavior is unchanged.
- Client-only scope retained; worker detection/highlighting remains RWS-3.

2026-09-20 - RTS default + pivot orbit runtime verified
- User confirmed the revised RTS camera works with RTS as the default Construction view.
- Q/E pivot-orbit behavior, pan-moving-pivot behavior and pivot-relative zoom are accepted for the current Construction camera scope.
- Free Build remains available as the alternate close-up camera mode.

2026-09-20 - RTS Q/E orbit direction swap
- Swapped the RTS orbit key mapping so Q and E rotate around the pivot in the opposite directions requested at runtime.
- Orbit pivot ownership, radius, pitch, zoom, pan and Free Build behavior are unchanged.

2026-09-20 - Bundle 2.2 selected-worker Pause/Resume UI restoration
- Restored Con Revamp Pause Worker / Resume Worker controls lost during later radial UI edits.
- Controls target the existing server-authoritative `workerpause [workerId] on|off` path and do not modify Allowed Jobs, presets, Needs or progression.
- Kept buttons non-focusable through the existing panel styling so the current scroll-preservation behavior remains intact.
- Runtime verification remains deferred to the consolidated Bundle 2.2 pass.


2026-09-20 - RWS-2 live edge-B hover correction
- Runtime video showed the midpoint revision still scaling around a nearly fixed center while the cursor moved diagonally.
- Root cause: the global RWS-2 listener consumed `MOUSE_DRAGGED`, preventing Matrix3's normal mouse/menu path from refreshing the action-23 ground hover used for live edge-B direction.
- Drag-motion is now passed through to Matrix3; Worker Control still consumes the configured press and release, so the owned drag cannot become a normal ground click.
- Existing midpoint/half-span math, fractional world rendering, 64-tile safety cap, release-hide and Escape behavior are unchanged.
- Client-only scope retained; requires one focused runtime retest before RWS-3.


2026-09-20 - RWS-2 exact edge-A visual pinning
- Runtime clarification showed edge A still drifted backward, meaning the reticule was still visually scaling around its center despite midpoint movement.
- Removed the arbitrary 100%-per-tile scale assumption.
- RWS-2 now reads the cloned GFX 4171 model's actual horizontal X/Z bounds and converts desired world radius (`radiusTiles * tileSize`) into the exact per-instance `Model.method1464(...)` scale.
- Because center offset from edge A and rendered visual radius now represent the same world-space distance, the starting circumference is pinned by construction instead of moving backward.
- Existing midpoint/half-span geometry, drag passthrough, 64-tile safety cap, release-hide and Escape behavior remain unchanged.
- Client-only scope retained; focused runtime acceptance still gates RWS-3.


2026-09-20 - RWS-2 pure world-space A/B geometry
- Runtime clip showed edge A pinning improved but edge B still lagged the cursor because selection span was still estimated from AWT pixel distance.
- Removed pixels-per-tile calibration and all screen-distance ownership from radial selection geometry.
- Edge A is the mouse-down Matrix3-resolved world tile; edge B is the current Matrix3-resolved world tile; center is midpoint(A,B); radius is distance(A,B)/2.
- Exact model-bounds scaling remains in place, so rendered visual radius matches the world-space geometric radius.
- Current updates occur at Matrix3 resolved-tile granularity; optional sub-tile smoothing is deferred until the A/B geometry is runtime accepted.
- Client-only scope retained; RWS-3 remains blocked on this focused acceptance.

2026-09-20 - Bundle 2.2 storage developer/test controls
- Added owner-only `storagereset` and capacity-bounded `storageset <resource> <amount>` using the existing SettlementState add/remove resource APIs.
- Added Con Revamp Storage Status, Reset All Storage, Prime Wood 99/100 and Storage Self-Test controls.
- Prime Wood 99/100 provides a fast setup for the final-slot reservation regression; deterministic use expects workers paused/idle before priming.
- Storage Self-Test reuses the existing disposable SettlementResourceSelfTest and does not mutate persistent player storage.
- Runtime verification remains deferred to the consolidated Bundle 2.2 pass.

2026-09-20 - Adjustable RTS pan speed
- Added RTS-only movement-speed presets: 0.5x, 0.75x, 1.0x, 1.25x, 1.5x, 2.0x, 2.5x and 3.0x.
- Added compact -/+ controls to the Construction palette header; the RTS button displays the active multiplier.
- Selected RTS speed persists for the current client session across Construction close/reopen and is not reset with camera orbit state.
- Normal, Shift-fast and Ctrl-precision RTS pan speeds all scale from the selected multiplier; Free Build movement remains unchanged.
- Static changed-file checks passed; runtime feel, preset clamping, modifier scaling and session retention remain to verify.


2026-09-20 - RWS-2 reticule model-origin correction
- Runtime feedback showed the A-side still moved even after switching to pure world-space A/B geometry.
- Root cause isolated to the render asset: GFX 4171's cloned model-space origin is not guaranteed to match the center of its horizontal X/Z bounds.
- RWS-2 now recenters the per-call model clone on the midpoint of its actual X/Z bounds with `Model.method1358(...)` before applying exact radius scale.
- Runtime scale now uses the centered half-width/half-depth instead of max absolute coordinates from the original off-center origin.
- This keeps cache definitions untouched and should align the visual center with midpoint(A,B), eliminating scale-induced A-edge drift.
- Pure world-space A/B geometry, release-hide, Escape and RWS-3 boundary remain unchanged.

2026-09-20 - Bundle 2.2 Pause/Resume runtime verified
- User confirmed selected-worker Pause Worker / Resume Worker works correctly in the live settlement.
- Pause/Resume preserves the worker's existing Allowed Jobs policy and is promoted from verified-static to runtime VERIFIED.
- Bundle 2.2 consolidated runtime pass now advances to Storage Self-Test, Reset All Storage and Prime Wood 99/100.

2026-09-20 - Object / Automation Asset Probe
- Added admin-only `itembrowser objectprobe` current-tile and nearby-3x3 scans using Matrix3's effective Region object slots, including runtime replacements/removals.
- Probe readback reports slot, object id/name/type/rotation/tile, definition size/clipping/animation/map metadata and cached interaction options without mutating world objects.
- Added read-only catalog logging to `Server/data/construction/object_catalog.txt`; full scans append while in-game chat remains bounded.
- Added Con Revamp Scan Current Tile / Scan Nearby 3x3 / Log Current Tile / Log Nearby 3x3 controls for track/cart/automation asset discovery.
- Static implementation complete; runtime scan/log acceptance remains pending.
2026-09-20 - GFX 4187 worker-status recolor probe
- Added a client-only RWS-4 visual preflight for the user-selected red/yellow double-ring GFX 4187.
- Probe samples the per-call clone's most frequent non-sentinel packed face colours and can recolor Color A or Color B independently through Model.method1393(...).
- Existing 0x80000 model flags isolate face-colour storage on the clone; cache definitions, combat target state, worker AI and server persistence remain unchanged.
- Con Revamp now exposes Original / Color A / Color B / Status / Hide controls; runtime must determine whether A/B map cleanly to the outer and inner rings.


2026-09-21 - RWS-2 circular-ring body calibration
- New runtime video proved the remaining drift was visual calibration, not A/B world geometry: GFX 4171's four decorative cardinal diamonds reached the geometric endpoints while the circular ring stopped short.
- Full-model X/Z bounds are therefore not a valid radius for the selectable circle.
- On AbstractModel renderer clones, RWS-2 now computes every centered vertex's X/Z radial distance, sorts those radii and detects the large outer gap separating the dense ring-body cluster from the decorative diamond cluster.
- The last radius before that outer gap becomes the ring-body base radius used for exact world-space scaling.
- Non-AbstractModel renderer models use a bounded 0.80 GFX-4171 ring/body fallback instead of the full decorative-marker extent.
- World A/B midpoint geometry, model recentering, cache isolation, release-hide and Escape behavior are unchanged.
- Acceptance now explicitly checks the circular ring circumference against A/B; decorative diamonds are allowed to extend outside that radius.

2026-09-21 - Object Lab OBJLAB-1/2
- Added Dev Inspector -> Capture in Object Lab for live object ID/name/source-tile capture without mutating normal map objects.
- Added a dedicated Object Lab viewer with explicit object type/rotation and X/Y preview-offset controls.
- Added client-only direct-render preview through Matrix3 ObjectDefinitions.method6057(...) / Model.method1375(...); previews never register with Class523 and own no collision/persistence.
- Exact live type/rotation are intentionally not guessed from the existing Dev target payload; Object Probe remains the evidence source for those fields.
- Added tagged/annotated server-side Object Lab catalog saves to Server/data/construction/object_lab_catalog.tsv plus recent-entry listing.
- Runtime capture/preview/catalog/non-mutation acceptance remains pending.

2026-09-21 - Object Lab preview coordinate helper compile fix
- Fixed OBJLAB-1/2 compile regression where preview status paths referenced omitted getPreviewX()/getPreviewY() helpers.
- Helpers now resolve Source Tile + configured preview offset, matching the existing render-path coordinate math.
- No renderer, catalog, world-object or Construction gameplay behavior changed.

2026-09-21 - Bundle 2.2 Storage Self-Test runtime verified
- Con Revamp Storage Self-Test returned PASS at runtime.
- PASS covered per-resource storage add/remove/capacity isolation plus the four starter-node definitions.
- The disposable self-test did not mutate persistent settlement storage.
- Consolidated Bundle 2.2 runtime pass advances to Reset All Storage, then Prime Wood 99/100.

2026-09-21 - Bundle 2.2 Reset All Storage runtime verified
- User confirmed the Con Revamp Reset All Storage control works correctly at runtime.
- Persistent Wood/Food/Stone/Basic ore storage clears through the authoritative SettlementState resource APIs.
- Bundle 2.2 consolidated runtime pass advances to Prime Wood 99/100.

2026-09-21 - Matrix3 Asset Studio v1 capture/session workflow
- Replaced the manual Object Lab debug-form workflow with a professional capture/session browser while preserving the proven direct-render preview and curated server catalog.
- Added client-scene capture for Current Tile, +X Tile, 3x3 and 9x9 areas without requiring object interaction or right-click options.
- Capture reads live Matrix3 Class523 cells and Interface65 identity/type/rotation, deduplicates repeated multi-tile scene references, and records slot/tile/size metadata.
- Session browser auto-populates the selected object's exact ID/type/rotation into the viewer; manual ID/type transcription is no longer required.
- Added rail-candidate filtering and automatic suggested tags for track/rail, buffer, cart/truck, signal and loading assets.
- Added one-click full-session TSV export under Client/data/construction/asset_studio/captures/; notes remain optional.
- Con Revamp now opens Asset Studio directly; server Object Probe remains a diagnostic fallback.
- Locked rail UX ownership: captured 1x1 pieces are future auto-tiling primitives; player-facing rail construction will drag Point A -> Point B rather than place rails tile-by-tile.
- Runtime capture accuracy, multi-tile dedupe, viewer preview, filtering and export remain pending.

2026-09-21 - Bundle 2.2 Prime Wood 99/100 runtime verified
- User confirmed the Con Revamp Prime Wood 99/100 control works correctly at runtime.
- Storage Self-Test, Reset All Storage and Prime Wood 99/100 are now all runtime VERIFIED.
- Bundle 2.2 consolidated runtime pass advances to Bundle 2.2 Self-Test, then role-preset/concurrent-production acceptance.

2026-09-21 - Bundle 2.2 Self-Test runtime verified
- Runtime screenshot confirms Bundle 2.2 Self-Test PASS.
- PASS covered per-resource storage isolation, in-flight reservation race protection, all worker role presets, stable worker-id targeting, independent pause/jobs/needs/progression and two-worker serialization.
- Final gate correctly remained NOT READY because Worker #1 still had jobs enabled and no Bundle 2.2 baseline existed for the login; this is expected pre-baseline state.
- Consolidated runtime pass advances to role-preset/concurrent-production acceptance, then baseline capture/persistence checks.

2026-09-21 - Bundle 2.2 zero-Food hunger bootstrap recovery
- Runtime role/concurrency setup reproduced the historical deadlock where critical Hunger + Food=0 stopped the Forager before it could gather recovery Food.
- Added emergency Food foraging only for unpaused workers with critical Hunger, Gather Food + Haul enabled, Food storage room and an available Food node.
- Emergency selection forces the Food node and ends after deposit; normal Hunger recovery then consumes stored Food on the following cycle.
- Non-food workers retain the existing no-Food stop behavior. No save/schema, role-policy, reservation or progression ownership changed.
- Runtime retest required before resuming the Bundle 2.2 concurrency gate.

2026-09-21 - Asset Studio one-click paired evidence capture
- Added Capture / Log Rail Evidence: one click performs a fresh 9x9 live-scene capture and saves matching capture_YYYYMMDD_NNN.png + .tsv evidence files.
- Evidence rows use deterministic plane/Y/X/slot/ID/type/rotation ordering and include index, exact object identity, rotation, tile, relative tile offset, size and candidate tag data.
- PNG capture reads the real Matrix3 game Canvas (Class584.aCanvas7745) without changing renderer ownership, then adds a numbered 9x9 tile-grid and row legend matching TSV indexes.
- Asset Studio hides during the desktop capture and restores afterward; direct-render preview is hidden so it cannot contaminate the evidence image.
- Capture remains read-only: no scene registration, object mutation, collision, cache, server, save-schema or rail gameplay changes.
- Runtime acceptance must verify paired filenames, screenshot visibility/scaling and index mapping on the known rail area.

2026-09-21 - Asset Studio evidence pair write hardening
- Replaced the partial atomic-move fallback with fail-clean pair finalization: if either PNG/TSV final move fails, both final outputs are removed and temp files are cleaned.
- Prevents a rare half-pair/missing-temp state while preserving deterministic capture numbering and read-only scene ownership.

2026-09-21 - Asset Studio Rail Kit Classifier
- Added Open Rail Classifier to Asset Studio.
- Classifier seeds the 27 type-22 rail candidate ids established by the 2026-09-21 evidence captures and merges any additional rail candidates present in the current Asset Studio session.
- Double-clicking a candidate spawns a client-only ObjectLabPreview beside the live player; Left/Up and Right/Down arrow keys move previous/next and immediately spawn the new selection.
- Added rotation 0-3 preview buttons plus explicit Spawn Preview / Hide Preview controls; no world object, collision, cache or server mutation is used.
- Geometry is recorded through independent Straight, Curve, Merge, Split, End/Buffer, Crossing, Not Rail and Unsure checkboxes; checking or unchecking any box saves immediately.
- Classifications persist locally to Client/data/construction/asset_studio/rail_kit.tsv with id/type/name/observed rotations, every geometry flag, last preview rotation and update timestamp.
- Merge and Split are intentionally independent flags so a visually symmetric turnout can be marked for both roles without forcing premature topology semantics.

2026-09-21 - Rail Kit Classifier static review cleanup
- Evidence-seeded ids no longer fabricate observed rotation 0; observedRotations remains '-' until a live Asset Studio session supplies actual captured rotations.
- Reopening/reselecting a previously reviewed rail restores its persisted lastPreviewRotation; unreviewed rails still default to the first live observed rotation or 0 when none is available.

2026-09-21 - Bundle 2.2 zero-Food recovery runtime verified
- User confirmed the emergency Food recovery fix works at runtime.
- A critical-Hunger Forager can now break the Food=0 bootstrap deadlock and resume productive work.
- The unchanged non-food worker path still uses the normal no-Food stop behavior.
- Bundle 2.2 runtime acceptance returns to Forager + Lumberjack concurrent production and final-slot reservation verification.


2026-09-21 - RWS-2 runtime accepted + RWS-3 live worker preview
- User confirmed the final RWS-2 ring-body calibrated RTS drag works: the circular ring keeps edge A fixed while edge B owns expansion.
- RWS-2 is promoted to RUNTIME VERIFIED.
- Added RWS-3 client-only live worker detection using Matrix3's bounded active-local NPC index list instead of a world/definition scan.
- Preview candidates are restricted to same-plane NPC definition 1 with the server-overridden name `Settler`; players and unrelated NPC definitions are ignored.
- Worker center membership uses exact world-space distance against the current live RWS-2 circle.
- Detected workers receive a temporary small GFX 4171 marker while the drag is active; Radial Status reports count + runtime NPC indexes.
- Persistent worker-ID mapping, final selection state and server validation remain deferred to RWS-5.
- No server, worker AI, combat target, persistence or cache-definition behavior changed.

2026-09-21 - Bundle 2.2 concurrent production + final-slot runtime verified
- User confirmed the Forager/Lumberjack concurrent-production scenario works correctly at runtime.
- Worker #1 Forager and Worker #2 Lumberjack operate independently against separate Food/Wood storage.
- Wood reaches 100/100 without a worker becoming stranded carrying excess Wood; Food continues independently.
- This closes role-preset/concurrent-production/final-slot runtime acceptance. Remaining Bundle 2.2 gate is baseline capture plus rebuild/relog persistence.

2026-09-21 - Dock Rail Classifier inside Client Console Test tab
- Replaced the standalone RailKitClassifierWindow with a narrow RailKitClassifierPanel owned by Client Console -> Test -> Rail Classifier.
- Added Rail Classifier as a lazy-loaded Test Console sub-tab directly after Con Revamp; no classifier JFrame opens anymore.
- Removed Open Rail Classifier from the external Asset Studio window so the classification workflow has one canonical UI owner.
- Added Refresh Live 9x9 Rails inside the docked classifier; it uses the existing read-only AssetStudioCapture path and merges exact live rail id/type/rotation evidence without opening Asset Studio.
- Preserved double-click preview, Left/Up previous+spawn, Right/Down next+spawn, R0-R3, Spawn/Hide Preview, independent geometry checkboxes and immediate rail_kit.tsv persistence.
- Layout is rebuilt for the narrow resizable Client Console dock so the game stays visible beside the classifier on smaller monitors.
- No server, collision, cache, scene-registration, save-schema or gameplay rail-placement ownership changed.

2026-09-22 - Bundle 2.2 prepared baseline capture
- Added one-click Prepare + Capture Baseline for the only remaining Bundle 2.2 persistence gate.
- The action requires saved=2/runtime=2, disables all Allowed Jobs and resets Needs for both workers, then uses the existing authoritative baseline capture.
- Pause state, worker identity, progression and Construction XP remain untouched and are still compared by the baseline.
- Pruned already-verified self-test/concurrency instructions and buttons from the Con Revamp Final Gate card; only Prepare + Capture and Check Baseline remain.
- Runtime verification required: prepared capture -> exit/re-enter PASS -> logout/relog/re-enter PASS.

2026-09-22 - A->B Rail Route Preview V0
- Added client-only RailRoutePreview using the established Class592 action-23 hover mirror and Class578 direct-render scene seam.
- Rail Classifier now includes Use Selected as Route Rail, X-then-Y / Y-then-X route order, Enable/Disable, Clear Route and Route Status controls.
- Enable automatically prefers the first rail checked Straight in rail_kit.tsv; otherwise the currently selected candidate can be configured manually, using its current preview rotation as horizontal.
- Left mouse captures Point A, live drag follows Matrix3's already-resolved ground tile as Point B, release commits the preview, and Escape cancels only the active drag while preserving the previous commit.
- V0 tiles the chosen straight object across a bounded 64-tile Manhattan path and uses horizontal rotation + 1 for vertical segments.
- The bend intentionally uses a straight placeholder in V0; curve/turnout selection is deferred until the user accepts the basic drag/path/tiling feel.
- Preview is direct-render only: no real object spawn, collision, cache mutation, persistence, resources or server rail gameplay.

2026-09-22 - A->B Rail Route Preview V0 cap hardening
- The 64-tile safety bound now counts attempted path positions rather than successful draws, so off-scene/failed model tiles cannot bypass the per-frame work cap on very long A->B drags.

2026-09-22 - Rail Route Preview V0 axis orientation fix
- Runtime video proved the route/path generation was correct but straight rail geometry was rotated 90 degrees relative to travel direction.
- Reclassified the stored classifier preview rotation as the straight rail's north/south orientation; east/west now derives as +1 quarter-turn.
- X/Y route logic is unchanged; only the direct-render orientation mapping changed.

2026-09-22 - A->B Rail Route Preview V1 curve corner
- Route preview now auto-configures the first rail checked Curve in rail_kit.tsv in addition to the first checked Straight.
- Orthogonal Manhattan routes reserve exactly one bend tile for the curve object; straight objects render only before/after that corner instead of overlapping it.
- Curve rotation is derived from the two neighboring route directions for all four east/west + north/south corner quadrants.
- Added Use Selected Curve plus a runtime-only Curve map offset R0-R3 control; the offset rotates the whole quadrant mapping without rewriting classifier evidence.
- If no Curve rail is configured, V1 safely falls back to the previous straight-corner placeholder.
- Straight-only horizontal/vertical routes remain unchanged and no server/world/collision/persistence ownership is added.

2026-09-22 - Test Console Object Explorer + same-tile overlay proof
- Added lazy-loaded Test -> Object Explorer for generic Matrix3 object research without opening another window.
- Reused DevSpawnSearchPanel object-name/ID indexing; double-click now exposes a backward-compatible activation callback so Object Explorer can spawn immediately while existing Spawn Browser consumers remain unchanged.
- Added DevDefinitionBridge.getObjectInfoAny(...) so Prev/Next Definition can browse unnamed object definitions such as many rail primitives instead of filtering them out because their cache name is blank/null.
- Object Explorer exposes arbitrary object ID, type 0-22, rotation 0-3 and preview X/Y offset, with R0-R3 quick-spawn buttons and client-only direct rendering.
- Added research tagging/export to Client/data/tools/object_explorer.tsv.
- Added ObjectCompositePreview: snapshot A and B from the current controls and direct-render both models on exactly the same world tile.
- Overlay proof is visual-only and deliberately bypasses Class523 registration/collision/persistence. Matrix3's live scene still has distinct wall/wall-decoration/floor-object slots and one ordinary floor-decoration field per tile; two type-22 floor decorations therefore cannot both occupy that normal slot simultaneously.
- Rail implication: if two stock fragments visually form a compact one-tile curve in overlay mode, a later settlement-owned logical rail tile can render the composite while preserving parallel one-tile lane spacing, avoiding an immediate custom-cache-model requirement.

2026-09-22 - Bundle 2.2 final persistence gate runtime verified
- Prepare + Capture Baseline succeeded for exactly two workers.
- Two-worker baseline passed after settlement exit/re-entry with saved=2/runtime=2.
- The same baseline passed again after logout/relog/re-entry, preserving worker identities, pause state, Allowed Jobs, Needs, progression and player Construction XP.
- Bundle 2.2 multi-worker control + concurrent work is DONE / RUNTIME VERIFIED.
- Persistent-runtime work advances to Bundle 2.3 housing, beds + population capacity.

2026-09-22 - Same-tile Rail Composite Builder + A->B V2
- Promoted the runtime-verified A+B overlay proof into a reusable rail-composite authoring system.
- Added RailCompositeLibrary with named roles STRAIGHT/CURVE/TURNOUT/CROSSING/END/CUSTOM stored at Client/data/construction/asset_studio/rail_composites.tsv.
- Object Explorer can now add up to eight current ID/type/rotation snapshots, remove/reorder-by-rebuild, preview all components on one exact tile, clear the set, and save/replace a named composite role.
- ObjectCompositePreview now renders an arbitrary component list instead of only A+B.
- Documented the latest verified stock curve family from capture_20260922_001: 46360/R2, 46361/R0, 46353/R2, 46377/R0, 46379/R0, 46382/R0, 46381/R0; 46353/R3 is the north/south continuation.
- RailRoutePreview is now V2: on enable/reload it reads the first saved CURVE composite and uses that at the Manhattan bend, rotating every component together by the corner quadrant plus the existing R0-R3 calibration offset.
- The previous single-object Curve classification remains a safe fallback only; it is no longer the preferred rail architecture.
- RailRoutePreview hides Object Explorer composite previews when enabling route mode so developer visuals do not stack accidentally.


2026-09-22 - RWS-3 runtime verified
- User runtime screenshot verified the live radial worker detector with both active settlers inside the large RWS-2 ring.
- Both detected workers displayed the temporary small GFX 4171 preview marker simultaneously.
- RWS-3 is promoted to RUNTIME VERIFIED.
- RWS-2 large-ring geometry remains intact; no server selection or persistent worker-selection state was introduced.
- Active radial workstream advances to RWS-4 selected-worker visual feedback / GFX 4187 double-ring preflight.

2026-09-22 - Bundle 2.3 persistent housing-bed capacity + Worker #3 foundation
- SettlementState schema v9 now persists housingBedCount; completed starter shelter capacity stays 2 and each saved bed adds +1 population capacity.
- Bed capacity is shelter-gated and cannot be removed when doing so would place the live saved population over capacity.
- Generalized recruitment beyond Worker #2: multiple recruited-settler records keep stable ids and receive unique free plot-relative home slots; saved pieces and existing worker homes are skipped.
- Settlement runtime now reserves every actual saved worker home tile, including Worker #3+ homes.
- Added disposable SettlementHousingCheck for capacity 2->3, Worker #3 id/home/default policy and serialization, plus live saved=3/runtime=3 verification and a process-local rebuild/relog baseline.
- Replaced the completed Bundle 2.2 Con Revamp gate card with Bundle 2.3 Housing controls for status, developer bed-capacity mutation, recruitment, self-test/live check and Worker #3 baseline.
- No bed object id was guessed. Developer Add/Remove Bed Capacity is an architecture/runtime harness only; normal Construction bed placement/removal hookup is Patch 2.3.2 after asset verification.


2026-09-22 - RWS-4 layered GFX 4171 styling
- User rejected GFX 4187 for worker selection/status visuals; the 4187 probe and Test Console card were removed.
- RWS-4 now reuses only the proven GFX 4171: one large drag ring plus two independent worker-centered clones for outer and inner layers.
- Outer/inner worker scales default to 100% / 70% and are independently adjustable from 25%-300%.
- Added independent Drag Ring / Outer / Inner color pickers through Swing JColorChooser.
- Picker RGB is converted to Matrix3 H/S/L and applied to each isolated clone through Model.method1396(..., weight 128).
- Existing 0x80000 clone-colour isolation remains authoritative; no cache definition or combat-reticule mutation is introduced.
- Use Original Colors resets all three tint overrides while preserving worker-ring scale settings.
- RWS-3 detection remains the preview owner during active drag; server-authoritative persistent selection remains RWS-5.

2026-09-22 - RWS/Rail compile regression fix
- Restored ConstructionRadialSelection HOVER_STALE_MS used by the existing drag-start validity guard after the RWS-4 ring-style refactor removed its declaration.
- Removed RailRoutePreview's obsolete dependency on the retired GFX-4187 probe API; enabling rail preview still disables Worker Control and hides conflicting developer previews.
- Current ConstructionRevampTestPanel already uses the replacement layered GFX-4171 ring-style API, so pulling current main clears the stale local Reticule4187ProbeMode/getReticule4187ProbeStatus errors without reintroducing the old probe.

2026-09-22 - Bundle 2.3 scalable housing acceptance gate
- Relaxed the live housing check from exactly three workers to any valid population of three or more.
- Live acceptance now verifies population capacity equals starter 2 + persistent bed count, runtime worker count matches the full saved population, and every saved worker has a live projection with unique id/home ownership.
- Housing baseline capture/check now preserves and compares the complete current worker set, so valid populations beyond Worker #3 no longer fail the developer gate.
- No SettlementState, recruitment, worker AI or save ownership changed; this is a test-harness hardening patch after runtime evidence showed beds=3, capacity=5, workers=5/5 surviving settlement re-entry.

2026-09-22 - Rail Layout Lab + placement hotkeys
- Replaced the same-tile-only composite authoring workflow with a client-only Rail Layout Lab in Test -> Object Explorer.
- Layout pieces now retain independent relative dX/dY offsets in addition to object id/type/rotation; up to 32 pieces can be arranged, overlapped, rotated, duplicated and deleted around a stable preview anchor.
- Added live authoring hotkeys while Object Explorer is visible: [ / ] select previous/next piece, arrows move one tile, R rotates, Delete removes and Ctrl+D duplicates; text/spinner/combo editing is excluded and hotkeys can be disabled.
- Save Layout Evidence persists name/role/index/id/type/rotation/offset_x/offset_y to rail_composites.tsv. Legacy six-column rows remain readable as zero-offset layouts.
- ObjectCompositePreview now honors each component's relative tile offset while retaining same-tile overlap support.
- RailRoutePreview V2 rotates saved component offsets together with their model rotations at curve bends, so an accepted authored layout can be reused in all four corner quadrants.
- No real world objects, collision, server state, cache definitions or settlement persistence are mutated by the authoring lab.

2026-09-22 - Bundle 2.3 housing foundation runtime verified
- Five-worker housing state passed settlement rebuild and full logout/relog persistence with beds=3, capacity=5 and workers=5/5.
- Scalable housing gate returned PASS with saved=5/runtime=5 and preserved all captured worker identities/home slots.
- Bundle 2.3 Patch 2.3.1 is runtime VERIFIED; next patch is real bed asset verification plus normal Construction palette placement/removal hookup.

2026-09-22 - Bundle 2.3 physical Bed 14872 placement
- Added BED as a stable settlement build role and BASIC_BED/basic-bed using runtime-identified object 14872, provisional standard object type 10, 3 Wood cost and 12 base Construction XP.
- Added Furniture -> Bed to the normal in-game Construction palette; placement reuses the existing ghost/rotation/settlementbuild path.
- SettlementState now centrally couples persistent BED place/duplicate/remove to the existing housingBedCount owner: physical placement adds +1 capacity and successful removal subtracts it.
- Bed removal is blocked when the resulting capacity would be below the saved worker population; the persistent object remains in place.
- Player build transaction rejects housing-bed placement before the starter shelter/no available housing slot and preserves atomic piece/material/capacity rollback.
- Bundle 2.3 Self-Test now exercises pre-shelter physical-bed rejection, real transaction placement, capacity 2->3, unoccupied removal, occupied-removal guard and serialization.
- Static review removed recursive normalize/helper re-entry from addHousingBed/removeHousingBed; canAdd/canRemove remain the normalized guard owners.
- Existing Add/Remove Bed Capacity controls remain developer harnesses only. Bed object type 10 requires live ghost/model acceptance before being promoted from provisional mapping.
- Bunk bed object 24339 is retained as a future space-efficient housing upgrade candidate after the single-bed path is accepted.

2026-09-22 - Bundle 2.3 physical Bed placement/removal runtime pass
- Runtime screenshot verified two normal Bed 14872 placements through the player build path.
- Each placement consumed 3 Wood, awarded 12 base Construction XP and increased housing beds/capacity by one: beds 3->4->5, capacity 5->6->7, workers stayed 5.
- Spare-capacity Bed removal succeeded and reduced beds 5->4 / capacity 7->6 while workers remained 5.
- Remaining Patch 2.3.2 acceptance is physical-bed rebuild persistence, occupied-capacity live removal rejection, logout/relog persistence and explicit visual/type-10 acceptance if still unconfirmed.

2026-09-22 - Bundle 2.3 physical housing runtime closure
- User confirmed all remaining Bed 14872 acceptance checks work.
- Furniture palette/ghost/type-10 rendering, physical placement, rotation, 3 Wood cost, 12 base Construction XP and +1 housing capacity are runtime VERIFIED.
- Physical Bed placement/capacity survives settlement exit/re-entry and full logout/relog.
- Spare-capacity Bed removal decreases housing capacity; required-bed removal at occupied capacity is rejected without deleting the Bed or reducing capacity.
- Bundle 2.3 housing, beds + population capacity is DONE / RUNTIME VERIFIED.

2026-09-22 - Bundle 2.4 RWS-5 radial multi-worker control
- Promoted the proven radial drag/detection path into a real multi-worker management seam.
- Client release snapshots only live settlement-worker runtime NPC indexes inside the world-space circle and immediately commits them to the active settlement.
- SettlementInstance validates those transient NPC indexes against its own SettlementWorkerNpc projections, converts them to persistent Worker IDs, and owns the selection only for that instance lifetime.
- Added server-owned selection status/clear plus batch role-preset and Pause/Resume commands; batch preset rewrites only existing Allowed Jobs and batch pause leaves Allowed Jobs unchanged.
- Con Revamp RWS-5 now exposes Selection Status, Clear Selection, Apply to Selection, Pause Selected and Resume Selected.
- No new persistent worker identity, role, selection or save-schema owner was added. Exit/re-entry drops the transient selection and requires a fresh drag.


2026-09-23 - RWS-4 textured 4171 recolor correction
- Runtime screenshot verified the layered GFX 4171 worker visual across multiple detected settlers and confirmed independent ring scaling works.
- Color-picker state changed but produced no visible recolor; source inspection established GFX 4171's visible material is texture-driven, so face-HSL changes alone were being visually overridden.
- Added clone capability flag 0x8000, which Matrix3 uses to deep-copy the per-face texture-ID array.
- Custom-color mode now gathers the clone's active texture IDs and retextures those faces to -1 on that isolated clone only before applying the selected HSL through Model.method1396(..., 128).
- Original-color mode leaves textures untouched.
- Existing 0x80000 face-colour isolation remains in place; cached GFX 4171 and combat reticules are not mutated.
- Focused runtime retest is now only the three color controls: drag ring, outer worker ring and inner worker ring.

2026-09-23 - RWS-5 release selection handoff fix
- Runtime test showed the existing drag/ring preview still worked but mouse release produced no useful committed worker selection.
- Removed the release-time live NPC rescan from the AWT event handler.
- The proven render-thread worker preview now caches the exact runtime NPC indexes it detects/rings each frame.
- Mouse release copies that cached selection and queues the existing server-owned workerselectionset handoff.
- Cancel/start/clear paths reset the cached preview selection to prevent stale carryover.
- No worker AI, persistence, jobs, housing, or server selection ownership changed; focused runtime retest is required.

2026-09-23 - RWS-5 release handoff runtime verified
- Focused runtime retest passed after the render-thread selection-cache fix.
- Dragging around two visibly ringed workers and releasing produced server feedback: Radial worker selection committed: #1,#2.
- Release/selection handoff is runtime VERIFIED; remaining Bundle 2.4 acceptance is batch Status, role preset, Pause/Resume, clear/reselect and instance-rebuild selection reset.

2026-09-24 - RWS-5 sole worker-command ownership
- Runtime test showed a committed #1,#2 radial selection while the legacy Allowed Jobs card still targeted numeric Worker #1, causing only one worker to change.
- Made the RWS-5 committed server-owned selection the sole Con Revamp mutation target for Role Preset, Pause/Resume and Allowed Jobs.
- Added batch individual-job and all-jobs server commands over the existing selected Worker IDs.
- Renamed the numeric selector to Worker Inspector and made its role inspection-only; it no longer clears or redirects command controls.
- Persistent worker policy ownership, AI, identity and save schema are unchanged.

2026-09-24 - RWS-5 persistent selected-worker rings
- User confirmed the RWS-5 multi-worker command flow works; remaining UX gap was selected-worker rings disappearing immediately after release.
- Scene rendering now continues the existing layered GFX 4171 worker rings for the committed selection after release while keeping the large drag circle temporary.
- Persistent rings resolve the exact committed runtime NPC indexes each scene pass, so they follow selected workers as they move.
- Clear Selection or a replacement drag updates the visual selection; if the committed runtime NPC set disappears during settlement exit/rebuild, stale local rings are cleared.
- No server selection authority, worker AI, jobs, persistence, housing or radial drag geometry changed.


2026-09-24 - RWS-4 renderer-aware 4171 recolor
- Runtime retest still showed no visible color change after the first texture-isolated recolor patch.
- The failure boundary was implementation-specific: texture enumeration only handled AbstractModel.aShortArray10821.
- Matrix3 also renders models through Class89_Sub2, where face textures live in aShortArray10591 and face colors in aShortArray10592.
- RWS-4 custom-color mode now enumerates texture IDs from either AbstractModel or Class89_Sub2, retextures those isolated clone faces to -1, then applies the selected HSL through the active renderer's own Model.method1396 implementation.
- Existing 0x80000 face-color isolation and 0x8000 texture-ID isolation remain unchanged.
- Scaling logic is untouched; focused runtime retest is color-only.


2026-09-24 - RWS-4 OpenGL 4171 recolor path
- Third runtime color retest still showed no visible change.
- Source trace identified a previously unhandled renderer: Class106_Sub1 creates OpenGLModel; whether the user's active client is on this path remains a runtime hypothesis until the focused color retest.
- OpenGLModel keeps per-face texture IDs in aShortArray10306 and face colors in aShortArray10281; prior custom-color texture enumeration covered only AbstractModel and Class89_Sub2.
- RWS-4 custom-color mode now enumerates OpenGLModel texture IDs as well, retextures those isolated clone faces to -1, then applies the selected HSL through OpenGLModel.method1396(...).
- Clone flag 0x8000 is verified-static to deep-copy OpenGLModel texture IDs through Class140.method2374(...); 0x80000 continues to isolate face colors.
- Scaling and worker-detection logic remain untouched; focused runtime retest is color-only.

2026-09-24 - Rail Layout Lab curve-scan recall + screenshot IDs
- Added a large ACTIVE OBJECT ID banner and changed layout rows to lead with OBJECT ID/T/R/dX/dY so screenshots identify the selected stock piece unambiguously.
- Added Load Last Curve Scan using the verified 2026-09-22 capture geometry: 46360/R2@(-4,0), 46361/R0@(-3,0), 46377/R0@(-1,0), 46379/R0@(0,0), 46382/R0@(-1,+1), 46381/R0@(0,+1).
- Added Load 2x2 Bend Core for only 46377/46379/46382/46381; 46353 remains a separate straight connector (R2 east/west, R3 north/south).
- Seeded layouts stay CUSTOM so reference/testing geometry cannot become the A->B CURVE automatically until the user explicitly promotes a saved layout.

2026-09-24 - Accepted three-piece rail curve routing
- Promoted uploaded CURVE_RAIL_LAYOUT_01 evidence into the A->B route path: 46377/T22/R0@(2,-1), 46379/T22/R0@(3,-1), 46381/T22/R0@(3,0).
- RailCompositeLibrary now prefers that exact saved name even while its local role remains CUSTOM, then falls back to a saved CURVE role, then to the same built-in three-piece definition.
- RailRoutePreview detects 46379 as the authored elbow by cardinal neighbor geometry, normalizes offsets around it, and rotates the complete pattern to all four route quadrants.
- X-then-Y and Y-then-X straight legs now skip any tile occupied by the curve pattern, so the two approach components replace rather than overlap straight rails.
- Same-tile overlay is no longer required for the accepted curve; all three accepted type-22 origins occupy distinct neighboring tiles. Overlay remains research-only for future geometry.
- Single-object curve fallback and the existing whole-pattern Curve map offset remain available.

2026-09-24 - RWS context orders + self selection
- Added a 6px selection-drag threshold so ordinary clicks preserve the committed RTS group instead of replacing it with an empty selection.
- Local player can now be included in the radial drag, receives the same layered GFX 4171 selection ring, and is tracked as transient self membership in the active SettlementInstance without save-schema changes.
- Reused Matrix3 context actions instead of adding a second menu system: Walk Here mirrors transient Move orders to selected workers; selected self continues through vanilla player movement.
- Starter tree object 1276 first action mirrors a transient Gather order to selected workers; selected self continues through vanilla player Chop.
- Added runtime-only worker manual Move/Gather orders that resume normal worker policy after completion.
- Hardened worker arrival checks so gathering requires explicit physical interaction range; calcFollow success with zero queued steps outside range no longer starts remote gathering.
- Persistent selected rings no longer clear on one empty render frame; scene exit remains the explicit stale-selection cleanup boundary.


2026-09-24 - RWS drag/order arbitration + RTS entry default
- A completed radial selection drag now consumes its release-side Matrix3 Walk Here once, preventing the newly selected player/workers from immediately moving when the user was only expanding/replacing selection.
- A fresh mouse press clears the one-shot suppression latch, so ordinary post-selection clicks still issue the existing RTS Walk Here order.
- Fresh Construction camera sessions now force RTS mode on entry; Free Build remains an explicit in-session palette switch.


2026-09-24 - Settlement-entry RTS lifecycle correction
- Runtime proved the prior RTS default only covered Construction palette sessions; ordinary SettlementInstance entry never opens that palette lifecycle.
- ConstructionBuildCamera now detects the loaded 64x64 settlement scene at the already-live viewport seam, waits three stable frames, and auto-enters RTS without requiring the palette.
- Auto-owned RTS releases after three stable frames outside the settlement scene so normal Matrix3 camera ownership returns on exit; manually/palette-owned camera sessions are not claimed by this lifecycle.


2026-09-24 - Construction palette A->B rail build integration
- Added Rails to the player-facing Construction palette with a Rail route entry using straight 46353/T22 (R3 north/south, R0 east/west).
- Selecting Rail route enables the proven RailRoutePreview V2 drag owner directly from the palette; hold Left at Point A, drag to Point B, release to commit.
- RailRoutePreview now exposes the exact committed physical route pieces after its existing straight/curve/rotation/curve-footprint logic runs, avoiding a second world picker or a separate route geometry owner.
- Release queues each physical route component through the existing server-authoritative settlementbuild path using stable build keys; no client-only object registration was added.
- Added persistent server definitions for 46353 straight and accepted curve components 46377/46379/46381 under the new RAIL semantic role. Each physical rail component currently costs 1 Basic ore and awards 4 base Construction XP; balancing remains provisional.
- Palette footer identifies rail drag controls while route mode is selected. Normal Walls/Floors/Doors/Furniture Paint/Continuous behavior remains unchanged.
- Runtime acceptance required: Rails tab visibility, straight drag placement, L-route accepted three-piece curve placement/rotation, Basic ore consumption/XP, persistence after settlement rebuild/relog, and no regression to ordinary placement.


2026-09-24 - Deterministic settlement RTS lifecycle bridge
- Replaced failed scene-size inference with explicit server/client lifecycle ownership.
- SettlementInstance.finishLoad sends private CSVar 2835=1 only after the dynamic settlement is loaded and the player is moved to its entry tile.
- Settlement leave-to-return/logout/teleport paths send CSVar 2835=0.
- PacketsDecoder forwards both small/big CSVar packets to ConstructionBuildCamera; only id 2835 is consumed by the construction bridge.
- Signal 1 enters RTS automatically when no Construction camera is active. Signal 0 releases only a camera session auto-owned by settlement lifecycle.


2026-09-24 - Material-free rail placement
- Runtime test confirmed the Construction palette A->B rail route reaches the authoritative settlement build transaction, but the provisional 1 Basic ore cost blocked placement.
- Removed settlement-material cost from straight 46353 and accepted curve components 46377/46379/46381.
- SettlementPlayerBuildTransaction now permits zero-cost placement only for the explicit RAIL semantic role; all existing wall/floor/door/bed material validation is unchanged.
- Rails remain persistent server-owned settlement pieces and retain 4 base Construction XP per physical component for now.


2026-09-24 - Reuse proven RTS session at settlement start
- Removed the separate automatic camera activation experiment from settlement lifecycle handling.
- Settlement enter signal now invokes ConstructionPlacementController.beginPaletteSession(), the same runtime-proven path used by the existing Construction RTS camera.
- Settlement exit invokes the matching endPaletteSession path when the Construction camera is active.
- No second RTS implementation, scene-size inference, or delayed camera bootstrap remains.


2026-09-24 - Rail Logistics V1 vertical slice
- Added material-free Rail Loader and Rail Unloader persistent build roles/pieces to the Rails palette. V1 reuses the verified fence/door visuals as explicit placeholder endpoint art; stable definition keys own gameplay identity.
- Added SettlementRailNetwork as a server-side derived topology over persistent plot-relative RAIL pieces. V1 finds cardinal adjacency, loader/unloader endpoints and a BFS path without scanning live WorldObjects.
- Added one transient SettlementRailCartNpc runtime projection. A connected Loader -> rail path -> Unloader with stored Wood dispatches one cart and moves it through the derived rail path.
- Successful arrival performs a Wood payload round-trip through SettlementState and reports delivery. This intentionally proves physical transport ownership without inventing a second machine inventory before processing/logistics storage is designed.
- Broken/disconnected routes do not dispatch. V1 intentionally excludes switches, intersections policy, multiple carts, signals, offline simulation and polished cart art/motion.
- Fixed free-build completion messaging so material-free rail/endpoints no longer dereference a null resource while reporting successful placement.
- Runtime acceptance required before expanding rail logistics.


2026-09-24 - Settlement RTS uses full proven palette entrypoint
- Corrected settlement lifecycle startup to call ConstructionPaletteOverlay.show(), not only ConstructionPlacementController.beginPaletteSession().
- This reuses the complete existing RTS startup path including palette input-listener and timer setup.
- Settlement exit uses the matching palette hide path, with direct camera exit only as a fallback.


2026-09-24 - Fix RTS startup ordering against dynamic-region transfer
- Runtime screenshot proved the existing RTS palette path was invoked, but it was invoked before the client had consumed the settlement map-region/teleport transition.
- Settlement finishLoad no longer sends CSVar 2835 in the same tick as loadMapRegions/setNextWorldTile.
- RTS startup signal is sent after the existing two-tick settlement transfer lock has elapsed, guarded by the same active SettlementInstance still being loaded.
- The client continues to use ConstructionPaletteOverlay.show(), i.e. the existing proven RTS camera path.


2026-09-24 - Worker Needs HUD visual prototype
- RWS-4 layered GFX 4171 recolor/scaling is now runtime verified by user screenshot/confirmation.
- Added a client-only Worker Needs HUD prototype that reuses GFX 4171 for three concentric rings on every active same-plane Settler.
- Default scales: Hunger 125%, Thirst 95%, Energy 65%.
- Hunger uses orange, Thirst cyan and Energy yellow; each blends toward red as it approaches the real server critical threshold (Hunger/Thirst 80, Energy 20).
- Con Revamp Worker Needs now includes Enable/Disable/Status plus live demo value and per-need scale controls.
- Preview rendering is independent of Worker Control so needs readability can be tested without an active radial selection.
- Persistent values remain server-owned in SettlementWorkerState. This slice intentionally does not encode needs into NPC names, combat level or arbitrary configs; demo values are visual-only until a clean per-worker server->client metadata seam is added.
- Existing radial selection, committed selection rings, worker AI, persistence and server need logic are unchanged.


2026-09-24 - Rail Auto-Connect V1
- Added persistent rail-tile lookup to SettlementState so route placement can recognize already-owned track without scanning runtime WorldObjects.
- A->B route placement may now terminate on / extend from an existing identical rail tile without producing an occupied-slot failure or duplicating the persistent piece. The existing rail remains authoritative and the rest of the queued route continues normally.
- Same-axis route extension therefore connects cleanly to existing track while preserving the accepted straight/curve visuals and persistent topology.
- A conflicting rail visual on the exact same tile is deliberately not overwritten. The server reports that a junction/switch visual is required rather than creating a visually false branch/crossing.
- T junctions, crossings and switch replacement remain gated on accepting the correct cache art/model. No fake overlay junction was introduced.


2026-09-24 - Decouple always-on settlement RTS from build palette
- Settlement lifecycle now starts the existing RTS camera directly without opening ConstructionPaletteOverlay.
- Added explicit settlement camera ownership state exposed to the placement controller.
- Closing the build palette no longer exits the camera while settlement RTS owns it.
- Leaving the settlement still clears settlement ownership and exits RTS back to vanilla camera.


2026-09-24 - Palette-independent RTS wheel input + wider zoom
- Root cause: the global AWT listener returned immediately whenever ConstructionPaletteOverlay was hidden, so settlement-owned RTS never received mouse-wheel events.
- RTS settlement startup now installs the existing wheel listener without opening the build palette.
- World-wheel events are accepted while either the palette is visible or settlement RTS owns the camera; palette keyboard handling remains palette-only.
- Extended RTS orbit range from 1400..5600 to 700..10000 camera units. Existing 450-unit wheel step retained.
- NPC/scene streaming, minimap camera coupling, and RTS state persistence remain separate follow-up work; this patch does not fake them by moving the player.


2026-09-24 - Continued-endpoint rail visual auto-tiling
- Corrected the Rail Auto-Connect target after runtime/user visual review: A->B then B->C already had the desired placement UX; the missing behavior was re-authoring B's visuals when the continuation changes direction.
- RailRoutePreview now remembers the previous committed route's final travel direction. When the next drag begins exactly at the previous B on the same plane and leaves on the perpendicular axis, commit inserts the accepted CURVE_RAIL_LAYOUT_01 at B and suppresses new straight pieces under that curve footprint.
- The server now permits accepted non-straight rail components to replace existing persistent RAIL visuals on the same tile. It removes the old projected/persistent rail piece, saves the curve component, respawns it and refreshes rail logistics topology.
- Perpendicular straight-on-straight conflicts are still not faked as T/cross junctions; those remain gated on accepted switch/junction art.
- This slice intentionally changes visuals/topology only. The player's A->B then B->C drag workflow is unchanged.


2026-09-24 - Continued rail curve seam alignment fix
- Runtime screenshot confirmed continuation-turn detection fired but CURVE_RAIL_LAYOUT_01 was anchored directly on B, producing an overlapping/crossed visual.
- Continuation turns now retain the incoming travel vector and shift the accepted three-piece curve corner one tile back along that incoming leg before rotating the saved composite.
- The new B->C route suppresses its authored B straight while the shifted curve footprint owns the approach/elbow/exit seam.
- Scope is deliberately narrow: no route UX, topology, endpoint, junction or cart behavior changed.


2026-09-24 - RTS view persistence foundation
- RTS now retains its last accepted pivot, yaw and orbit distance for the lifetime of the client and restores that view when the detached RTS session is recreated.
- Pan, rotate and zoom operations update the saved RTS view; resetRtsState now resets transient controller state without erasing the saved view.
- Movement-speed selection was already persistent static client state and remains unchanged.
- Narrow source searches did not establish a verified fog/minimap/NPC-streaming ownership seam. Per AGENTS scan discipline, no speculative obfuscated-core patch was made for those systems; they remain the active follow-up rather than moving the player or globally changing renderer behavior.


2026-09-24 - Bundled settlement RTS world-view pass
- Server LocalNPCUpdate trace verified the missing-worker cause: ordinary local NPC packets are player-centered at 14 tiles, while Matrix3 already has a large-scene packet using 8-bit offsets and a 64-tile radius.
- Settlement entry now enables Player.largeSceneView and resets the local NPC list so workers within the settlement's large-scene radius can be supplied without moving the player's actual WorldTile.
- All three settlement leave paths restore ordinary largeSceneView=false and reset the local NPC list.
- This stacks with the same bundle's already-landed palette-independent wheel input, 700..10000 orbit range, and RTS pivot/yaw/zoom persistence.
- Renderer trace confirmed detached RTS world rendering already receives detached-camera coordinates through Class343 -> scene method6240; fog remains environment/renderer-owned and is not changed until its exact distance control is verified.
- Minimap heading is still separately vanilla-coupled; no guessed obfuscated angle write was added.


2026-09-24 - Rail continuation correction after worsening runtime result
- Reverted the one-tile-back continuation seam offset experiment; runtime video showed it made the visual connection materially worse.
- Restored B as the accepted CURVE_RAIL_LAYOUT_01 elbow anchor. The accepted composite was authored/elbow-normalized around 46379, so arbitrary seam offsets are no longer applied.
- Removed the old server conflict guard from rail-on-rail visual updates. When an A->B / B->C auto-tile footprint reaches an existing persistent RAIL tile with a different rail component or rotation, that rail visual is now replaced in place instead of rejected or stacked.
- Replacement is persistent and projected immediately; a defensive rollback restores the old rail if the replacement save fails.
- No new route geometry or guessed offsets were introduced in this correction.


2026-09-24 - Replace worker needs concentric rings with arch HUD
- Runtime screenshot proved the first Worker Needs HUD prototype rendered, but the user rejected the three-concentric-ring layout as the wrong visual direction.
- Replaced the three full needs circles with three partial GFX 4171 arches on one shared circumference.
- Hunger, Thirst and Energy each own a separate ~100-degree slot with gaps; current slot centers are 30/150/270 degrees.
- Arc length now encodes wellbeing remaining: Hunger=(100-hunger)%, Thirst=(100-thirst)%, Energy=energy%.
- Existing identity/severity colors remain: Hunger orange->red, Thirst cyan->red, Energy yellow->red.
- Added clone alpha capability 0x100 and OpenGL per-face arc masking through Model.method1473(...). OpenGL render-vertex indexes are mapped back to original model X/Z coordinates through anIntArray10329/aShortArray10330, then faces outside each arc span are made fully transparent.
- Con Revamp now uses one shared needs-arch scale instead of three independent concentric-ring scales.
- Selection/drag rings, worker detection, server needs ownership, persistence and AI are unchanged.
- Needs values remain demo-only until visual acceptance; a clean per-worker server->client metadata seam is still the next production step.

2026-09-24 - RTS loaded-scene camera clamp
- Fixed stale absolute RTS pivot restoration across rebuilt dynamic settlement scenes; saved view is restored only when its pivot belongs to the currently loaded scene.
- RTS pan now clamps the world-space look pivot to the live Matrix3 scene height-grid bounds with a small edge margin, then recomputes detached camera position from that valid pivot.
- Long orbit distance remains available; the camera itself may sit outside the scene, but its look target can no longer pan into unloaded blank terrain.
- No player movement, NPC protocol, fog, minimap, or vanilla camera behavior changed.

2026-09-24 - RTS visibility anchor + Ctrl-Z wiring
- Wired Ctrl+Z during settlement RTS (palette open or closed) to the persistent SettlementInstance.undoLastBuild() path through the existing console command bridge.
- Exposed settlement undo and clearbuilds server operations; both mutate persistent SettlementState and projected runtime pieces.
- Corrected the detached RTS renderer visibility focus: Class523.method6240 no longer receives the stationary player's tile during settlement RTS and instead receives the detached camera's current local scene tile.
- This targets the visible long-range fog/scene-culling wall without changing ordinary gameplay rendering; runtime verification is still required.
- Eraser palette UX, destructive Clear Builds confirmation, and RTS minimap heading remain unfinished and must not be marked complete.


2026-09-24 - Rail route debug instrumentation
- Froze rail geometry changes after repeated continuation visual regressions. This patch adds diagnostics only; it does not change route placement, curve anchoring, replacement or topology behavior.
- RailRoutePreview now records a monotonically numbered debug report for every committed A->B operation.
- The report captures current A/B/plane, previous A/B/plane, whether the new A equals the previous B, calculated incoming/outgoing cardinal directions, continuation H/V values, accepted curve composite name, calculated seam-curve corner/anchor/layout turns, every calculated curve footprint tile, and every final physical route piece with world tile/object id/type/rotation/logical role.
- Construction -> Rails now exposes Rail Debug and Copy Debug controls. Copy Debug puts the latest tab-separated report on the system clipboard for direct paste into chat alongside a screenshot/video.
- Debug is intentionally client route-intent evidence; persistent server actual/replacement evidence is a follow-up only if this report proves the client intent is correct but runtime objects differ.

2026-09-24 - Construction eraser + destructive clear controls
- Added a persistent Erase mode to the Construction palette. While armed, normal settlement tile clicks are consumed and routed to SettlementInstance.eraseDevelopmentTile(...) instead of Walk Here/build placement.
- Tile erase removes persistent settlement pieces whose saved origin occupies the clicked tile, removes their live projected objects, and refreshes rail logistics.
- Added a compact Clear control with a 3.5-second second-click confirmation; confirmed clear routes to the existing persistent clearPlayerBuilds() authority.
- Kept rail Debug/Copy and Cancel controls available on the same bottom utility row.
- Ctrl+Z remains runtime VERIFIED by user report.
- RTS visibility/fog-anchor correction remains NEEDS RUNTIME TEST; minimap RTS heading remains unresolved and no guessed obfuscated mapping was patched.


2026-09-24 - Worker Needs HUD ring-body arc correction
- Runtime video confirmed the new Hunger/Thirst/Energy angular masking was executing, but it was cutting sectors from the entire GFX 4171 mesh.
- Because 4171 includes decorative outer diamonds/spikes, the needs HUD appeared as chunky cyan/yellow fragments rather than clean circular arches.
- Generalized the existing 4171 ring-body radius resolver to support OpenGLModel vertices in addition to AbstractModel.
- Needs arc masking now applies two filters in order: keep only face centroids inside the calibrated circular ring-body radial band, then keep only faces inside that need's angular slot.
- Added focused mask diagnostics to Needs HUD Status: visible face count, radial-body hidden count, angular hidden count and calibrated ring radius.
- Existing selection rings, need colors, demo values, server needs ownership, worker AI and persistence remain unchanged.

2026-09-24 - Full settlement RTS terrain render radius
- Runtime screenshot rejected the previous visibility-focus-only correction: objects could remain visible beyond a hard beige terrain cutoff, proving the symptom was the scene terrain visibility radius rather than merely stale camera focus.
- Verified-static Class523.method6240 builds terrain visibility from Class523.anInt5865 around the render focus. Class613 constructs that radius from the normal player graphics/view-distance preference.
- Settlement RTS now temporarily expands Class523's visibility radius to the loaded scene extents while method6240 renders, with visibility-grid buffers expanded to match, then restores the stock player radius before returning.
- This intentionally makes the detached settlement RTS camera render loaded settlement terrain like a player positioned at that camera while leaving ordinary/player rendering radius unchanged.
- Runtime gate: zoom/pan/rotate across the settlement and confirm the beige hard terrain cutoff is gone. Watch FPS because the RTS pass can now draw the full loaded settlement scene.


2026-09-24 - Tighten Worker Needs HUD arc face mask
- Follow-up review of the malformed needs arches showed that ring-body filtering still accepted faces by centroid radius only.
- GFX 4171 decorative spike/diamond triangles can straddle the circular ring band while keeping their centroid inside it, so decorative chunks survived.
- Needs arc masking now computes radial distance for all three original X/Z vertices of each OpenGL face.
- A face is eligible for an arch only when every vertex remains inside the calibrated circular annulus; faces crossing inward/outward from the ring strip are made fully transparent before angular-slot masking.
- Needs HUD Status now reports the strict ring-body band alongside visible/body-hidden/arc-hidden face counts.
- Hunger/Thirst/Energy values, colors, slot ownership, shared scale, selection visuals and server needs ownership are unchanged.


2026-09-24 - Rail diagnostic safety freeze and visible evidence panel
- Disabled broad server rail-on-rail replacement while continuation geometry is under diagnosis; previously placed track is preserved when a different rail component reaches the same tile.
- Identical rail reuse remains idempotent.
- Rail Debug now renders the latest route-intent report visibly inside the Construction palette, including route, seam, curve-footprint and final-piece rows.
- Copy Debug remains available for the complete report.
- No route geometry, curve anchoring or routing math changed in this patch.


2026-09-24 - Rail Endpoint Editing V1
- Replaced the failed B-to-C continuation model with endpoint editing: pressing the existing committed B endpoint keeps A fixed and moves B.
- The complete A-to-new-B route is recalculated through the already-accepted A-to-B router, so a 90-degree endpoint move produces the normal multi-piece curve instead of trying to splice two independent routes.
- Removed continuation-curve injection from committed route snapshots. The old continuation H/V seam system no longer owns placement geometry.
- Endpoint edits queue exact rail-only removal of the previous route pieces followed by the complete recalculated replacement route. Server removal refuses non-RAIL targets.
- Client command transaction capacity raised from 32 to 128 so the existing 64-tile route cap can replace a full old route plus full new route in one validated queue operation.
- V1 route editing identity is the currently committed client route. Physical rail pieces remain server-persistent; persistent route metadata/re-edit-after-relog is intentionally deferred until this interaction model passes runtime.
