2026-09-01 - OpenGL renderer baseline

VERIFIED / verified-static
- Toolkit id 1 maps Class106.method1888 -> Class172.method2709 -> Class106_Sub1.
- Class106_Sub1 is the existing Jagex OpenGL renderer; it loads jaclib and jaggl, creates the OpenGL context on the game Canvas, and uses the existing OpenGL model/shader/FBO paths.
- The existing developer command tk1 switches through the normal renderer transition to toolkit 1. On success it updates the preferred renderer to 1 and saves the preference.
- The existing renderer command reports the active toolkit id plus renderer/vendor/device/driver information.
- Class498 keeps the original renderer creation failure/fallback path intact.

RUNTIME VALIDATION
- OpenGL display-mode selection currently reports that it is unable to enter the requested display mode on Windows.

2026-09-01 - Native library load path fix

verified-static
- Class567 extracts platform natives from the cache-backed native archive and loads them through System.load(...).
- System.load requires an absolute library path; the loader previously passed File.getPath(), which can remain relative to the client cache root.
- Changed the native load call to File.getAbsolutePath() without changing renderer selection, extraction, preference persistence, or fallback behavior.

RUNTIME VALIDATION PENDING
- Pull the patch, restart the client, select OpenGL or run tk1, then run renderer.
- Expected success state: Toolkit ID 1 with populated renderer/vendor/device/driver information.
- If toolkit 1 still fails, the next trace is the concrete jaclib/jaggl load or OpenGL context-init exception; do not replace the renderer.

2026-09-01 - Windows JAWT preload for jaclib

verified-static
- Runtime failure showed jaclib.dll failing in System.load(...) with "The specified procedure could not be found" before jaggl or OpenGL.init(...) ran.
- Class567 now preloads the running JVM's jawt library on Windows immediately before loading jaclib.
- The change is isolated to jaclib on Windows; native extraction, jaggl loading, renderer selection, preferences, and software fallback remain unchanged.

RUNTIME VALIDATION PENDING
- Fully restart the client, select OpenGL or run tk1, then run renderer.
- Pass: the jaclib procedure error is gone and startup advances to jaggl/OpenGL initialization or successfully reaches Toolkit ID 1.
- If it still fails, capture the first new native/OpenGL exception; do not add another speculative renderer change.

2026-09-01 - Class567 native-cache loop syntax repair

verified-static
- Restored the missing closing brace for the native-cache byte comparison loop so Class567 compiles cleanly in Eclipse/Java 8 without changing the comparison behavior.

Decision
- Do not replace the built-in renderer or add LWJGL. Keep the known-good software bootstrap/fallback intact while repairing the existing Jagex OpenGL path.
