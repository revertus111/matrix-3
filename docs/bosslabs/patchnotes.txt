2026-08-31
- Added the BossLabs architecture authority under docs/bosslabs.
- Defined BossLabs as an external specialist developer window governed by the Client Console UI/lifecycle contract.
- Locked Matrix3 as the authoritative owner of combat, NPC lifecycle, world behavior, drops, persistence, and permissions.
- Established first-boss-driven development using the Volcanic Warden concept as the initial vertical-slice specification.
- Defined planned Identity, Stats, Attacks, Phases, Mechanics, Arena, Drops, and Testing responsibilities without pre-approving runtime class names or storage format.
- Explicitly deferred giant node editing, general scripting, duplicate NPC/map/model/FX editors, and speculative mechanics until real encounter needs justify them.
- Recorded verified-static architecture facts separately from unscanned runtime integration hypotheses.
- Completed the narrow Phase 1 Matrix3 runtime scan for NPC combat dispatch, NPC lifecycle, cache-backed combat definitions, and drops.
- Verified CombatScriptsHandler/CombatScript as the existing custom NPC combat extension seam; no alternate BossLabs combat dispatcher is needed.
- Verified NPC.java/NPCCombat.java retain ownership of targeting, movement, combat delay, death, respawn, XP, and NPCDrops-based rewards.
- Added inactive Java 8 BossLabs content foundations: BossAttackDefinition, BossPhaseDefinition, and BossDefinition.
- Added validation for attack styles/overrides and non-overlapping health phase ranges while keeping spawning, damage, targeting, drops, and world mutation out of the definitions.
- Added docs/bosslabs/runtime-scan.txt with verified-static findings, unresolved hypotheses, and the next narrow runtime slice.
- Added BossDefinitionRegistry as a thread-safe NPC-id to immutable BossDefinition mapping for future runtime/tool registration.
- Added BossCombatScript to resolve registered boss health phases/attacks and execute them through existing Matrix3 CombatScript damage/projectile/animation/GFX paths.
- Added one narrow CombatScriptsHandler delegation check for registered BossLabs NPC ids; unregistered NPCs retain the existing custom/default script path unchanged.
- Corrected the runtime-scan conclusion that startup CombatScript keys alone were sufficient; dynamic BossLabs definitions require the explicit registration/delegation seam but still do not create a second combat dispatcher.
- Left the registry empty until the first Volcanic Warden NPC id/assets are verified, preventing accidental takeover of an existing NPC combat script.
- Added BossNpcInspection as a read-only auto-population model for existing NPC/boss identity, cache/combat data, combat ownership, resolved script class, and current BossLabs definition.
- Added BossNpcSearchService with one automatic query path: all-digit input resolves directly as NPC id, while text performs case-insensitive name search with exact/prefix/substring ranking.
- Name search now uses Matrix3's verified Utils.getNPCDefinitionsSize()/cache index-18 boundary, builds the valid-NPC name index lazily once, and supports explicit invalidation after future cache/import changes.
- Refactored CombatScriptsHandler script lookup into one shared resolver and added read-only resolved-script/default inspection methods so BossLabs does not duplicate combat-script discovery semantics.
- Added docs/bosslabs/NPC_SEARCH.md locking the no-mode-dropdown search workflow, existing-boss auto-population behavior, combat-source display, and no-fake-conversion rule for hand-written Java bosses.
