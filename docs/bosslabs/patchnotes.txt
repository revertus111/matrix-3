2026-08-31
- Added the BossLabs architecture authority under docs/bosslabs.
- Defined BossLabs as an external specialist developer window governed by the Client Console UI/lifecycle contract.
- Locked Matrix3 as the authoritative owner of combat, NPC lifecycle, world behavior, drops, persistence, and permissions.
- Established first-boss-driven development using the Volcanic Warden concept as the initial vertical-slice specification.
- Defined planned Identity, Stats, Attacks, Phases, Mechanics, Arena, Drops, and Testing responsibilities without pre-approving runtime class names or storage format.
- Explicitly deferred giant node editing, general scripting, duplicate NPC/map/model/FX editors, and speculative mechanics until real encounter needs justify them.
- Recorded verified-static architecture facts separately from unscanned runtime integration hypotheses.
- Completed the narrow Phase 1 Matrix3 runtime scan for NPC combat dispatch, NPC lifecycle, cache-backed combat definitions, and drops.
- Verified CombatScriptsHandler/CombatScript as the existing custom NPC combat extension seam; no alternate BossLabs combat dispatcher is needed.
- Verified NPC.java/NPCCombat.java retain ownership of targeting, movement, combat delay, death, respawn, XP, and NPCDrops-based rewards.
- Added inactive Java 8 BossLabs content foundations: BossAttackDefinition, BossPhaseDefinition, and BossDefinition.
- Added validation for attack styles/overrides and non-overlapping health phase ranges while keeping spawning, damage, targeting, drops, and world mutation out of the definitions.
- Added docs/bosslabs/runtime-scan.txt with verified-static findings, unresolved hypotheses, and the next narrow runtime slice.
- Added BossDefinitionRegistry as a thread-safe NPC-id to immutable BossDefinition mapping for future runtime/tool registration.
- Added BossCombatScript to resolve registered boss health phases/attacks and execute them through existing Matrix3 CombatScript damage/projectile/animation/GFX paths.
- Added one narrow CombatScriptsHandler delegation check for registered BossLabs NPC ids; unregistered NPCs retain the existing custom/default script path unchanged.
- Corrected the runtime-scan conclusion that startup CombatScript keys alone were sufficient; dynamic BossLabs definitions require the explicit registration/delegation seam but still do not create a second combat dispatcher.
- Left the registry empty until the first Volcanic Warden NPC id/assets are verified, preventing accidental takeover of an existing NPC combat script.
- Added BossNpcInspection as a read-only auto-population model for existing NPC/boss identity, cache/combat data, combat ownership, resolved script class, and current BossLabs definition.
- Added BossNpcSearchService with one automatic query path: all-digit input resolves directly as NPC id, while text performs case-insensitive name search with exact/prefix/substring ranking.
- Name search now uses Matrix3's verified Utils.getNPCDefinitionsSize()/cache index-18 boundary, builds the valid-NPC name index lazily once, and supports explicit invalidation after future cache/import changes.
- Refactored CombatScriptsHandler script lookup into one shared resolver and added read-only resolved-script/default inspection methods so BossLabs does not duplicate combat-script discovery semantics.
- Added docs/bosslabs/NPC_SEARCH.md locking the no-mode-dropdown search workflow, existing-boss auto-population behavior, combat-source display, and no-fake-conversion rule for hand-written Java bosses.
- Extended BossDefinitionRegistry with explicit atomic live replacement and one-level rollback, including restoration to the normal Matrix3 script/default path when the first BossLabs apply is undone.
- Added BossDefinitionPublisher as the stable Apply Live / Save & Apply / Undo Last Apply / Apply Saved API for future BossLabs tooling.
- Added BossDefinitionStore with a versioned BossLabs-owned binary store at data/bosslabs/definitions.bld; existing Matrix3 NPC combat/drop/spawn/examine files are not modified.
- Save & Apply now persists the complete immutable definition successfully before publishing that same instance to the live registry; persistence failure leaves the current live definition untouched.
- CombatScriptsHandler.init() now loads persisted BossLabs definitions before combat-script use so saved bosses survive server restart without changing NPCCombat ownership.
- Added docs/bosslabs/LIVE_EDITING.md and updated BOSSLABS.md to lock DRAFT/LIVE/SAVED state separation, explicit apply behavior, one-level rollback, no per-keystroke publishing, and controlled-respawn rules for future instance-owned fields.
- Added the Phase 3 external BossLabs Swing window and an Open BossLabs launcher in the Client Console shell.
- Added dark themed Identity, Stats, Attacks, Phases, Mechanics, Arena / Tiles, Drops, and Testing tabs with local DRAFT-state tracking.
- Added the first draft-only Arena / Tile Composer canvas with mouse-wheel zoom, middle-mouse pan, hover coordinate preview, and left-click tile selection.
- Kept NPC server search/inspection, Apply Live, Save & Apply, rollback, saved reapply, and encounter testing controls disabled until the dedicated client/server BossLabs bridge is verified and implemented; no fake direct server calls were added.

2026-09-01
- Added the BossLabs client/server development bridge over Matrix3's existing command packet and type-99 panel-message path; no socket, file polling, or alternate networking stack was introduced.
- Added a narrow packet-24 observer that reads BossLabs-prefixed commands without consuming or rewriting Matrix3 packet data, leaving normal world packet framing and command processing intact.
- Kept BossLabs server actions admin-only and delegated search/inspection/publishing to the existing BossNpcSearchService and BossDefinitionPublisher authorities.
- Routed NPC name indexing/search and Save & Apply disk persistence through the existing slow executor rather than the packet/world or Swing EDT paths.
- Connected the external BossLabs window to real automatic ID/name search, result selection, NPC inspection, combat-source/script display, and existing BossLabs definition auto-population.
- Enabled Apply Live, Save & Apply, Undo Last Apply, and Apply Saved only when the inspected NPC/current BossLabs state makes each action valid.
- First-time conversion of a Matrix3 Java/default NPC remains blocked until BossLabs attack/phase authoring exists; the tool does not publish an empty or fake conversion.
- Kept encounter spawn/reset/force-phase/testing controls disabled because matching authoritative server testing APIs are not implemented yet.
- Added window-close listener cleanup so the static BossLabs reply listener does not retain a disposed Swing panel.
- Added a client-local BossLabsDraftDefinition model plus real Phases and Attacks editors for health ranges, melee/range/magic style, animation, GFX, projectile, max-hit override, and combat-delay override.
- Existing BossLabs definitions now round-trip their complete phase/attack data through a bounded versioned development-wire payload and repopulate the editors on inspection.
- First-time Matrix3 NPC conversion is now available only after the local draft contains a valid non-overlapping phase layout with at least one valid attack per phase.
- Added an atomic Client Console command batch so multi-packet BossLabs publishes are queued all-or-nothing rather than leaving a partial client-side transaction.
- Added a bounded begin/chunk/commit BossLabs publish transaction; no live or saved definition changes until the final commit reconstructs a complete immutable server BossDefinition.
- Apply Live publishes the complete phase/attack draft through BossDefinitionPublisher; Save & Apply preserves the existing persistence-before-live ordering and slow-executor disk path.
- Kept BossDefinitionStore format/version unchanged because the existing store already persists all attack and phase fields needed by this authoring slice.
- Added the first reusable BossLabs mechanic: target-centered telegraphed tile-pattern attacks with configurable warning GFX, impact GFX, warning ticks, and up to 128 relative tiles within +/-16 of the snapshotted target tile.
- Added a tile-pattern painter to the selected attack editor; left-click toggles pattern tiles while middle-drag/zoom preserve relative coordinates. The separate Arena / Tiles shell remains a later integration target rather than being rewritten in this runtime slice.
- BossCombatScript now snapshots the target origin for patterned attacks, renders warning tiles through Matrix3 World graphics, delays through WorldTasksManager, then damages only players still occupying painted impact tiles through existing CombatScript hit/accuracy helpers.
- Plain attacks with no painted tiles keep the previous single-target BossCombatScript path unchanged.
- Upgraded the BossLabs definition wire payload to v2 and BossDefinitionStore to v2 for tile mechanic fields; both retain v1 read compatibility so existing BossLabs definitions load with empty tile patterns.
- Save & Apply writes v2 only after successful validation/persistence and still preserves the existing persistence-before-live ordering.
- Added optional lingering floor hazards to painted attacks with configurable hazard GFX, duration, tick interval, and per-tick max-hit override; duration 0 keeps the previous impact-only behavior.
- BossCombatScript now keeps the snapshotted impact tiles active through Matrix3 WorldTasksManager, damages only players occupying those tiles on each configured hazard tick, and stops the hazard when its duration expires or the owning boss dies/finishes.
- Hazard damage continues through the existing CombatScript melee/range/magic accuracy/hit helpers and does not introduce a second damage or timer authority.
- Upgraded BossLabs definition wire/storage to v3 for hazard fields while retaining v1 and v2 read compatibility; v2 patterned attacks load with hazards disabled until explicitly authored and saved.
- Added hazard controls to the selected attack editor and made the right-side attack editor vertically scrollable so the expanded mechanic controls remain reachable at practical BossLabs window sizes.
- Added per-attack target selection with Current target and Random nearby player modes plus a configurable 1-32 tile target range.
- Random nearby player targeting prefers a valid nearby player other than NPCCombat's current target, falls back to the current target for solo/no-alternate cases, and never replaces NPCCombat's authoritative target state.
- Resolved alternate targets feed the existing single-target projectile/hit path and the existing target-centered tile/telegraph/hazard path, so painted mechanics snapshot around the attack's resolved player without duplicating combat logic.
- Upgraded BossLabs definition wire/storage to v4 for target mode/range while retaining v1-v3 read compatibility; older attacks load as Current target with the default 14-tile targeting range.
- Added Target and Target range controls to the existing scrollable Attacks editor; target edits remain DRAFT-only until Apply Live or Save & Apply.

2026-09-02
- Added weighted attack rotation fields: relative weight, cooldown turns, and a best-effort immediate-repeat rule on each BossAttackDefinition.
- BossCombatScript now selects only ready attacks, rolls by configured weight, prefers another ready attack when repeat prevention is enabled, and waits one normal attack cycle when every attack is cooling down rather than violating cooldowns.
- Rotation state is isolated per NPC instance with weak references and resets on phase changes or live definition replacement, while Matrix3 NPCCombat remains authoritative for actual combat scheduling and target ownership.
- Upgraded BossLabs definition wire/storage to v5 for rotation fields while retaining v1-v4 read compatibility; older attacks load as weight 1, cooldown 0, immediate repeat allowed.
- Added Weight, Cooldown turns, and Repeat rule controls to the existing scrollable Attacks editor; all rotation edits remain DRAFT-only until Apply Live or Save & Apply.
- Added reusable painted tile effect types for both impact and lingering hazards: damage players, heal players, damage boss, and heal boss.
- Player-damage tile effects continue through Matrix3 CombatScript accuracy/max-hit/hit helpers; healing uses Entity.heal(), while boss damage uses Matrix3's Hit queue with a self source so the NPC death path remains non-null without inventing player damage credit.
- Player effects require player occupancy on a painted tile; boss effects require the boss footprint to overlap at least one painted tile, including multi-tile NPC footprints.
- Impact amount / max hit and Hazard amount / max hit now act as combat max-hit values for Damage players and fixed effect amounts for healing/boss effects; -1 resolves through the NPC's existing style max hit.
- Upgraded BossLabs definition wire/storage to v6 for impact/hazard tile effect types while retaining v1-v5 read compatibility; older definitions default both effect types to Damage players and preserve prior behavior.
- Added Impact effect and Hazard effect controls to the existing scrollable Attacks editor with the same four reusable tile actions.
- Kept this slice bounded to reusable tile mechanics; phase entry/exit actions, minions, encounter context, and a general scripting engine remain separate future slices.
- Added ordered phase On Enter / On Exit actions with Play animation, Play graphic, and Heal boss as the first bounded action catalog.
- BossCombatScript now detects initial phase activation, health-based phase changes, and live definition replacement per NPC instance; old-phase exit actions run before new-phase entry actions and rotation state resets at the transition.
- Phase transitions with authored entry/exit actions reserve one normal NPC attack opportunity so transition animation/GFX are not immediately overwritten; empty legacy action lists preserve the previous attack timing with no added pause.
- Upgraded BossLabs definition wire/storage to v7 for phase action lists while retaining v1-v6 read compatibility; older phases load with empty entry/exit action lists.
- Added ordered On Enter / On Exit action lists and add/remove authoring controls to the scrollable Phases editor; actions remain DRAFT-only until Apply Live or Save & Apply.
- Phase action healing routes through Matrix3 Entity.heal(); animation/GFX route through normal NPC presentation state. No phase scripting engine, minion lifecycle, teleport, or encounter participant authority was added.
- Added BossEncounterContext/BossEncounterRuntime as runtime-only per-NPC encounter state with weak boss/player ownership, observed participant tracking, owned WorldTask tracking, and generation-based invalidation.
- BossCombatScript now registers actual attack/effect recipients as observed participants and routes delayed tile impacts plus lingering hazards through the encounter generation so stale BossLabs tasks can be invalidated safely.
- Apply Live, Save & Apply, Undo Last Apply, and Apply Saved now invalidate existing BossLabs transient tasks for matching live NPC instances without changing saved definition data or the v7 store/wire format.
- A lightweight Matrix3 WorldTasksManager lifecycle watcher finishes encounter contexts when the boss dies, finishes, or loses BossLabs registration; final cleanup clears participant/task state while leaving NPC lifecycle ownership in Matrix3.
- Participant tracking is observational only in this slice: random nearby targeting still uses its existing eligibility rules and is not restricted to the encounter participant set yet.
- Existing phase changes do not clear prior hazards solely because a phase changed; definition replacement/death/finish cleanup is added without silently changing phase-crossing hazard semantics.
- Added Spawn minions as an ordered phase action with NPC id, amount 1-8, and radius 1-8; the Phases editor accepts the compact NPC_ID,AMOUNT,RADIUS value format.
- Minions spawn through Matrix3 World.spawnNPC(..., spawned=true) on deterministic free ring slots around the boss, inherit the boss's valid current target, and retain their normal Matrix3 NPC subclass/combat behavior.
- BossEncounterContext now owns up to 32 live summoned NPCs per boss instance; boss death/finish/unregister and live-definition replacement detach and finish those minions through the Matrix3 world-task path.
- Upgraded BossLabs definition wire/storage to v8 for phase-action quantity/radius while retaining v1-v7 read compatibility; v7 phase actions load with quantity/radius defaults of 1/1.
- Nested BossLabs NPC summons are intentionally blocked in this first slice to prevent recursive encounter spawning; player-killed summoned NPCs still use Matrix3's normal XP/drop behavior, while cleanup finish() removals create no drops.
- Replaced the disabled Testing placeholder at window composition time with live admin-only controls for Spawn Boss Here, Reset Encounter, Set Boss HP %, Force Phase, Trigger Attack, Clear Hazards, and Clear Minions without rewriting the large BossLabsPanel shell.
- Added per-admin controlled test-boss ownership: every testing mutation targets only the exact NPC instance spawned by that player's Testing tab session, never an arbitrary world NPC found by id.
- BossLabs testing world mutations are admitted through the existing rights-gated command bridge and scheduled onto Matrix3 WorldTasksManager; test spawning still uses Matrix3 World.spawnNPC(..., spawned=true), NPCCombat target ownership, and NPC.finish cleanup.
- Force Phase changes the test boss to a living HP checkpoint for the authored phase so normal transition actions fire on the next combat opportunity; Trigger Attack executes one selected authored attack through the existing BossCombatScript path without changing weighted rotation/cooldown history or NPCCombat target ownership.
- Clear Hazards invalidates BossLabs-owned delayed tile work (including pending telegraphed impacts) but does not cancel Matrix3 single-target CombatScript.delayHit queues; Clear Minions finish-removes only encounter-owned minions. BossDefinition store/wire remains v8.
- Testing now enables Spawn Boss Here, Reset Encounter, and Set Boss HP % for any valid inspected Matrix3 NPC; Force Phase, Trigger Attack, Clear Hazards, and Clear Minions remain gated to live BossLabs definitions.
- Plain Matrix3 test NPCs use their existing Matrix3 combat ownership without creating a BossEncounterContext, while test-session cleanup still owns the exact spawned copy; BossLabs-spawned test copies continue to clean up if their live registration is removed.
- Added radius-driven attack tile pattern presets for Cross, horizontal line, vertical line, filled square, and ring plus Clear; applying a preset replaces the selected attack's painted offsets and remains fully hand-editable afterward.
- Pattern presets enforce the existing +/-16 offset and 128-tile limits before mutating the local DRAFT; no server combat, definition wire/storage, or v8 persistence format changed.
- Replaced the Arena / Tiles shell with a large draft-backed attack tile workspace offering phase/attack selection, direct paint/erase, zoom/pan, radius presets, Center View, and Copy Pattern/Paste Pattern reuse across attacks and phases.
- Arena edits mutate the same BossLabsDraftDefinition attack offsets used by the Attacks tab, immediately mark DRAFT modified, retain existing +/-16/128-tile validation, and persist through the unchanged v8 Apply Live/Save & Apply path; no fixed-world arena engine or second tile runtime was added.

2026-09-03
- Rewrote BOSSLABS.md around a creator-first content-authoring standard while preserving the existing Matrix3/BossLabs runtime ownership model.
- Locked two core UX rules: creators never retype values BossLabs already knows, and normal boss creation must not require BossLabs implementation knowledge.
- Defined progressive-disclosure phase/attack authoring, creator-facing names with internally managed stable IDs, context-aware testing, safe defaults, Advanced/Developer Details separation, and direct panel composition as V2 requirements.
- Formally separated relative Attack Pattern authoring from future fixed encounter Arena Layout semantics.
- Added docs/bosslabs/ux-testlist.txt with creator-workflow, usability, testing, validation, presentation, and first-boss acceptance gates while retaining the existing runtime testlist unchanged.

2026-09-05
- Installed the BossLabs V2.1 creator shell with Overview, Phases, Attacks, Attack Pattern, Arena, Drops, Testing, and Advanced navigation.
- Removed the recursive Testing-tab title/component-tree replacement; BossLabsPanel now directly owns and registers the live testing panel lifecycle.
- Reworked phase authoring around creator-facing names and HP ranges while generated internal Phase IDs remain read-only under Advanced/internal details.
- Reworked attack authoring into a Basic default view plus collapsible Rotation, Animation & FX, Area / Telegraph, Lingering Ground Effect, and Advanced/internal sections.
- Removed the duplicate inline attack tile painter so Attack Pattern remains the dedicated relative-geometry workspace.
- Replaced manual Testing Phase ID/Attack ID fields with selected editor context and one-click Enter Selected Phase / Test Selected Attack actions over the existing authoritative bridge.
- Split Spawn minions authoring into separate NPC ID, amount, and radius fields instead of compact NPC_ID,AMOUNT,RADIUS text syntax.
- Preserved the existing BossLabs definition wire/store version, Matrix3 combat/world ownership, persistence ordering, bridge protocol, and server testing authorization semantics.
- Upgraded Testing with Full/75/50/25 HP checkpoints, a combined UI action that queues the existing scoped hazard/minion cleanup controls, and a bounded 40-entry session result history with local clear.
- Upgraded Attack Pattern authoring with drag-paint/right-drag erase, one-level pattern undo, rotate/mirror/nudge transforms, and multi-row toolbars while retaining the existing +/-16 and 128-tile limits.
- Added Attack Pattern geometry/timing summaries for tile bounds, whether target 0,0 is affected or safe, warning delay, impact, and lingering-ground duration/interval.
- Creator workflow feature-pack edits continue to mutate the existing BossLabs DRAFT and persist through the unchanged v8 store/wire path; no server combat, NPC/world, packet, drop, scheduler, or cleanup authority changed.
- Added Matrix3-native BossLabs drop authoring without replacing the existing NPCDrops/Drops runtime: NPC death still reads NPCDrops and rolls through Drops.generateDrops().
- Added an isolated version-1 BossLabs drop override store at data/bosslabs/drops.bld; data/npcs/packedDrops.d is never rewritten by BossLabs.
- Saved BossLabs drop overrides re-apply after Matrix3 packed drop loading so the original packed table can be captured/restored as the live baseline.
- Added live drop Apply, Save & Apply, one-level Undo, Apply Saved, Restore Matrix3, and Delete Saved Override flows; disk I/O stays on the slow executor and live NPCDrops mutation is scheduled through Matrix3 WorldTasksManager.
- Added faithful readback of Matrix3 Rare/Very Rare gear-split entries so editing an existing table does not silently lose wearable rare drops.
- Added a creator-facing Drops tab with item ID/name search through ClientConsoleItemBridge, rarity bucket selection, min/max quantities, rare-drop-table toggle, DRAFT/LIVE/SAVED indicators, and explicit publish/restore controls.
- The editor exposes Matrix3's actual bucket semantics (Always, Common 90%, Uncommon 70%, Rare 0.6%, Very Rare 0.36%) rather than inventing unsupported per-item percentages.
- Boss combat definitions remain definitions.bld/wire v8; drop content is independently persisted/rollbackable and does not change BossCombatScript, NPC death, XP, or loot-generation algorithms.
- Corrected BossLabs drop authoring to preserve repeated item slots inside the same rarity bucket; Matrix3 selects rolled-bucket entries by array slot, so duplicates may intentionally weight selection, while repeated Always slots remain repeated guaranteed drops.
- Normalized BossLabs into docs/bosslabs/PROJECT.md and registered it in docs/rs3/WORKSTREAMS.md; the current gate is one consolidated runtime verification session, V2.3 Asset Workflow is the next implementation phase after that gate, and true Arena Layout remains content-driven carryover.

2026-09-06
- Repaired the BossLabs creator workspace state after runtime testing exposed a ghost-DRAFT workflow: with no inspected NPC, DRAFT now stays `none`, boss-name editing is locked, and all dependent authoring/testing/drop tabs remain disabled.
- Loading a different NPC now clears the previous local definition immediately, resets stale ownership/inspection values, returns to Overview, and keeps authoring locked until the matching inspection reply arrives.
- Stale inspection replies for a previously requested NPC are ignored instead of being allowed to replace the currently requested workspace.
- Successful inspection explicitly unlocks the workspace and explains the first authoring step; invalid/missing NPC inspection returns BossLabs to a clean locked state rather than leaving partially editable controls.
- No server, combat, packet, definition-store, drop-store, or Matrix3 runtime ownership changed in this creator-state repair.
