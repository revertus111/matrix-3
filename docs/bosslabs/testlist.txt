Matrix3 BossLabs - planned runtime acceptance list

Documentation-only baseline
1. Confirm docs/bosslabs/BOSSLABS.md remains subordinate to AGENTS.md, docs/rs3/PROJECT.md, and docs/client-console/CLIENT_CONSOLE.md.
2. Confirm BossLabs does not claim ownership of combat, NPC lifecycle, world state, drops, permissions, persistence, or other Matrix3 gameplay authorities.
3. Confirm new BossLabs work is driven by the first complete custom boss rather than speculative tool expansion.

Current Java foundation acceptance
4. Eclipse clean/build the Server with Java 8. Confirm BossAttackDefinition, BossPhaseDefinition, BossDefinition, BossDefinitionRegistry, BossCombatScript, BossNpcInspection, BossNpcSearchService, BossDefinitionStore, and BossDefinitionPublisher compile with the narrow CombatScriptsHandler hooks.
5. Construct a BossAttackDefinition with MELEE, RANGE, and MAGE values. Confirm valid styles are accepted and an unsupported style throws IllegalArgumentException.
6. Construct health phases such as 71-100, 31-70, and 0-30. Confirm BossDefinition accepts non-overlapping ranges and rejects overlapping phase ranges.
7. Confirm BossDefinition.getPhaseForHealth() resolves 100%, 70%, 30%, and 0% to the intended configured phase and clamps current HP above max/below zero safely.
8. Register and unregister a BossDefinition through BossDefinitionRegistry. Confirm lookup is keyed only by the definition NPC id and the immutable definition instance is returned unchanged.
9. With no BossLabs definition registered for an NPC id, confirm CombatScriptsHandler still resolves the same existing id/name/default CombatScript as before this patch.
10. With a test BossLabs definition registered for a safe test NPC id, confirm CombatScriptsHandler delegates attacks to BossCombatScript rather than the normal cached/default script.
11. Confirm BossCombatScript selects attacks only from the current health phase and preserves Matrix3 CombatScript accuracy/max-hit handling.
12. Confirm USE_NPC_DEFAULT (-1) attack animation/GFX/projectile/max-hit/combat-delay values inherit the existing NPC/cache-backed values where applicable.
13. Confirm ranged/magic BossLabs attacks use Matrix3 World.sendProjectileNew()/projectile timing and CombatScript.delayHit(), while melee uses the existing immediate delayed-hit helper path.
14. Confirm unregistering the test definition immediately returns that NPC id to the normal Matrix3 combat-script path without restarting the server.
15. Confirm BossLabs registry/adapter code does not spawn NPCs, select targets outside NPCCombat, alter pathing, replace death/respawn handling, load drop tables, or directly mutate loot ownership.

NPC search / existing-boss inspection acceptance
16. Call BossNpcSearchService.search() with a valid all-digit NPC id string. Confirm it performs direct id inspection and returns only that exact NPC without requiring the name index.
17. Search by a case-insensitive NPC name fragment. Confirm exact-name matches rank first, prefix matches second, broader substring matches after them, and duplicate-name NPC ids remain separately selectable.
18. Search for an invalid/nonexistent numeric NPC id. Confirm no phantom NPC definition is returned.
19. Confirm name indexing uses valid cache index-18 files within Utils.getNPCDefinitionsSize(), is reused after its first build, and can be deliberately invalidated through invalidateNameIndex().
20. Inspect an existing NPC and confirm id, name, combat level, size, model ids, HP, attack speed, attack/defence/death animations, respawn delay, attack GFX/projectile, aggression, aggression range, and poison immunity match Matrix3 cache/combat-definition authorities.
21. Inspect an NPC controlled by a specific Matrix3 CombatScript. Confirm combat source reports MATRIX3_SCRIPT and the resolved script class matches the same script CombatScriptsHandler would execute.
22. Inspect an NPC with no specific custom script. Confirm combat source reports MATRIX3_DEFAULT and the resolved script is Matrix3 Default.
23. Register a BossLabs definition for a safe test NPC id, inspect it, and confirm combat source reports BOSSLABS, the resolved script is BossCombatScript, and the current BossDefinition is returned for editor auto-population.
24. Confirm inspection/search never spawns a temporary NPC, applies hits, mutates target state, edits drops, or changes combat registration merely by reading an NPC.
25. Confirm docs/bosslabs/NPC_SEARCH.md remains the detailed authority for the one-field automatic ID/name search behavior; no ID-vs-name dropdown/toggle is introduced in the future UI.

Live editing / persistence acceptance
26. With no BossLabs registration for a safe test NPC id, publish definition A through BossDefinitionPublisher.applyLive(). Confirm A becomes live immediately and the NPC resolves through BossCombatScript on its next attack.
27. Undo the first live apply from item 26. Confirm the BossLabs registration is removed and the NPC returns to the same Matrix3 Java/default script path it used before the apply.
28. Register/publish definition A, then apply definition B for the same NPC id. Confirm B becomes live atomically and BossDefinitionRegistry.hasRollback() reports a rollback state.
29. Undo item 28. Confirm A is restored exactly as the live definition and the rollback history is consumed.
30. Confirm Apply Live performs no disk write to data/bosslabs/definitions.bld.
31. Call BossDefinitionPublisher.saveAndApply() with a valid definition. Confirm data/bosslabs/definitions.bld is created/updated and only after successful persistence does that exact definition become live.
32. Cause a controlled persistence failure (for example, use a test environment where the BossLabs data path cannot be written). Confirm Save & Apply throws/reports failure and the pre-existing live definition remains unchanged.
33. Save multiple BossLabs definitions. Restart the server and confirm CombatScriptsHandler.init() loads all valid saved definitions before live combat-script use.
34. After restart, inspect each saved NPC and confirm combat source is BOSSLABS and the loaded phases/attacks match the saved definition data.
35. Apply an unsaved live definition that differs from the saved version, then call BossDefinitionPublisher.applySaved(). Confirm the persisted version becomes live without rewriting the store.
36. Confirm Save & Apply preserves other saved boss definitions when updating one NPC id.
37. Confirm a malformed/unsupported BossLabs store header/version is rejected and logged rather than partially registering corrupt definitions. Preserve a known-good backup before this destructive test.
38. Confirm the store is versioned and isolated under data/bosslabs; existing data/npcs packed combat definitions, drops, spawns, and examines remain byte-for-byte untouched by BossLabs save operations.
39. Confirm definition persistence/runtime publishing does not spawn NPCs, alter NPCCombat target/movement ownership, apply hits, mutate drops, or directly change world lifecycle.
40. Run the targeted startup/combat smoke checks after this slice: server starts cleanly with no BossLabs store, server starts cleanly with a valid BossLabs store, normal non-BossLabs custom NPC scripts still resolve, and default NPC combat still resolves.
41. Confirm docs/bosslabs/LIVE_EDITING.md remains the detailed authority for DRAFT/LIVE/SAVED separation, explicit apply behavior, rollback, persistence ordering, and future controlled-respawn rules.

BossLabs external-window acceptance once implemented
42. Open BossLabs through its approved launcher path. Confirm the Matrix3 client continues rendering and receiving input normally.
43. Confirm BossLabs uses the intentional Client Console dark visual system and does not present unfinished default Swing styling.
44. Confirm the NPC selector is one search field: numeric text is interpreted as NPC id and non-numeric text as name search, with no mode dropdown/radio/toggle.
45. Select an existing NPC/boss from search results. Confirm all verified inspection fields populate automatically and the current combat source/script is visible.
46. Select an already-registered BossLabs boss. Confirm its current BossDefinition/phases/attacks populate rather than opening a blank definition.
47. Select a legacy Matrix3 Java boss. Confirm BossLabs identifies and displays its current script but does not falsely present arbitrary Java mechanics as converted editable BossLabs actions.
48. Edit a field in the BossLabs draft. Confirm no live/server change occurs until Apply Live or Save & Apply is explicitly invoked.
49. Apply a definition-backed combat change during a live boss fight. Confirm subsequent applicable attack/phase resolution uses the new live definition without requiring a server restart.
50. Confirm the UI exposes clear live-edit controls equivalent to Apply Live, Save & Apply, Undo Last Apply, and Apply Saved/Revert Live to Saved when a saved definition exists.
51. Confirm the UI clearly indicates draft/live/saved divergence so the developer can tell whether current edits are merely draft, live-only, or permanently saved.
52. Confirm fields that require concrete NPC/world-instance reconstruction do not pretend to apply instantly; present an explicit controlled respawn requirement once that bridge exists.
53. Resize BossLabs at practical non-maximized sizes. Confirm important controls remain reachable through layout reflow/scrolling and are never clipped off-screen.
54. Test common Windows display scaling targets (100%, 125%, 150%). Confirm text remains readable and controls do not overlap or disappear.
55. Type W/A/S/D and other game hotkeys in BossLabs text fields. Confirm the game does not also consume those keys. Return focus to the game and confirm normal input resumes.
56. Confirm first-time name-index building/refresh and definition disk persistence are performed away from the Swing EDT so the game/client window does not freeze.
57. Move/resize BossLabs, close it cleanly, and reopen/relaunch when geometry persistence exists. Confirm valid window geometry restores and invalid/off-screen geometry is clamped safely.
58. Repeatedly resize/move the BossLabs window. Confirm preference writes are quiet/coalesced and do not cause visible client hitching.
59. Trigger any intentionally testable panel/definition validation failure. Confirm the tool reports the problem without crashing the running game client where practical.

First-boss runtime acceptance
60. Spawn the first boss through the authoritative Matrix3 development path and confirm normal NPC lifecycle/pathing remains Matrix3-owned.
61. Verify the boss can execute its minimal Phase 1 attack loop without BossLabs directly owning target validation, movement, death, respawn, or drops.
62. Verify projectile, AoE, hazard, targeting, minion, and timed mechanics individually as they are added; classify runtime-proven behavior as VERIFIED.
63. Force common HP thresholds/phases through safe development controls and confirm phase transition/cleanup behavior matches the encounter definition.
64. Reset the encounter repeatedly. Confirm boss state, minions, hazards, timers, targets, and temporary encounter objects clean up without leaking into the world.
65. Kill the boss normally. Confirm death/cleanup and reward/drop behavior routes through Matrix3-owned systems.
66. Relaunch/reload the same saved boss definition and confirm the persisted configuration reproduces the intended encounter configuration.
67. Run the relevant Matrix3 smoke-test portion after any meaningful engine/runtime extension. Core ownership regressions are blockers.

Framework acceptance after boss #1
68. Create a second small boss/encounter using the proven reusable framework. Confirm it can reuse definitions/components without copying boss #1's encounter implementation wholesale.

Current Phase 3 UI shell checkpoint
69. Eclipse clean/build the Client with Java 8. Confirm ClientConsoleShell, BossLabsWindow, and BossLabsPanel compile without adding a second UI framework or changing the protected Java target.
70. Open Client Console -> Open BossLabs. Confirm one external BossLabs window opens, repeated launcher clicks reuse/raise the existing window, and closing/reopening it creates a clean replacement window.
71. Confirm the BossLabs shell exposes Identity, Stats, Attacks, Phases, Mechanics, Arena / Tiles, Drops, and Testing tabs and remains usable at the 900x620 minimum window size.
72. Enter an all-digit NPC query and a text NPC query. Confirm the UI automatically classifies ID versus name without any mode selector and clearly states that no server lookup occurs until the dedicated bridge is connected.
73. Edit Definition ID, Display name, or NPC ID. Confirm the DRAFT indicator changes to modified and no live/server action occurs.
74. In Arena / Tiles, confirm mouse-wheel zoom changes tile size, middle-mouse drag pans without changing relative tile coordinates, hover reports the tile coordinate, and left-click selects/highlights a tile.
75. Confirm Apply Live, Save & Apply, Undo Last Apply, Apply Saved, and encounter testing buttons are visibly present but disabled in this shell checkpoint so no fake/direct server mutation path is introduced before the bridge.

Current client/server bridge checkpoint (supersedes Phase 3 items 72 and 75)
76. Eclipse clean/build both Client and Server with Java 8. Confirm BossLabsClientBridge, BossLabsPacketBridge, BossLabsCommandBridge, BossLabsPanel, BossLabsWindow, Class118, and InputStream compile cleanly.
77. Log in as owner/admin, open BossLabs, enter a valid numeric NPC id, and confirm the tool inspects that exact NPC without an ID/name mode control.
78. Search by NPC name and confirm matching results arrive asynchronously, remain individually selectable by id, and selecting one auto-populates identity, Matrix3 stats, combat source, and combat script.
79. Inspect a normal Matrix3 Java/default NPC. Confirm LIVE reports Matrix3, BossLabs definition id stays blank, and Apply Live / Save & Apply stay disabled rather than publishing a fake conversion.
80. Inspect an existing registered/saved BossLabs NPC. Confirm definition id/display name auto-populate, LIVE/SAVED indicators match server state, and valid publish/rollback buttons enable according to saved/rollback availability.
81. Edit Definition ID or Display name for an existing BossLabs NPC. Confirm only DRAFT becomes modified until Apply Live or Save & Apply is clicked.
82. Click Apply Live for an existing BossLabs NPC. Confirm the server reports success, the next applicable BossCombatScript resolution uses the new identity-bearing definition with existing phases preserved, and no BossLabs disk write occurs.
83. Click Save & Apply. Confirm persistence runs through the BossLabs store, then the saved/live state refreshes in the tool only after success.
84. Create a rollback state with two live publishes, click Undo Last Apply, and confirm the prior live definition is restored and the Undo button disables once rollback is consumed.
85. With a saved definition and a different unsaved live definition, click Apply Saved and confirm the persisted version becomes live without rewriting the store.
86. Log in without admin rights and send/attempt a BossLabs bridge command. Confirm BossLabsCommandBridge performs no search/publish action and Matrix3 gameplay permissions remain authoritative.
87. Confirm normal non-BossLabs command packet traffic, chat, NPC combat, and type-99 panel messages still behave normally; BossLabs only observes packet 24 and only consumes type-99 client text carrying the bosslabs| protocol prefix.
88. Close BossLabs while logged in, reopen it, and confirm responses go only to the new panel instance with no disposed Swing listener retained.
89. Confirm name-index search and Save & Apply do not freeze the game or Swing EDT while the server performs cache indexing or disk persistence.
90. Confirm encounter testing buttons remain disabled because spawn/reset/force-phase server APIs are not part of this bridge checkpoint.

Current attack/phase authoring checkpoint (supersedes bridge item 79 and expands items 80-83)
91. Eclipse clean/build Client and Server with Java 8. Confirm BossLabsDraftDefinition, BossLabsDefinitionEditor, BossLabsDefinitionWireCodec, BossLabsClientBridge, BossLabsCommandBridge, BossLabsPanel, and ClientConsoleBridge compile cleanly.
92. Inspect a normal Matrix3 Java/default NPC. Confirm LIVE still reports Matrix3, the tool creates a local empty BossLabs draft with a generated definition id, and no combat ownership changes merely from inspection.
93. In Phases click Add Phase, then in Attacks choose that phase and click Add Attack. Confirm the default 0-100 phase plus NPC-default melee attack produces a valid local draft and enables Apply Live / Save & Apply.
94. Create two phases with overlapping HP ranges. Confirm the client draft remains local and Apply Live / Save & Apply stay disabled until the overlap is corrected.
95. Test invalid phase percentages, blank phase/attack ids, unsupported style data, max-hit below -1, and combat-delay below -1. Confirm invalid drafts cannot be published and the server constructors remain the final validation gate.
96. Create melee, range, and magic attacks with explicit animation/GFX/projectile/max-hit/delay values. Confirm the complete draft round-trips through the wire codec without field/style changes.
97. Inspect an existing BossLabs NPC with multiple phases/attacks. Confirm the complete server definition repopulates the Phases and Attacks editors instead of only restoring identity fields.
98. Change one attack in an existing or newly converted BossLabs definition and click Apply Live. Confirm the whole immutable definition replaces the live registration atomically and subsequent applicable BossCombatScript attacks use the new definition.
99. Click Save & Apply on a multi-phase draft, restart the server, re-inspect the NPC, and confirm the same phase ranges and attack fields reload from the unchanged BossDefinitionStore format.
100. Fill the Client Console queue enough that the complete BossLabs publish transaction cannot fit. Confirm queueConsoleCommands rejects the transaction before queuing any of its begin/chunk/commit commands.
101. Send an intentionally incomplete, missing-chunk, malformed, or mismatched BossLabs upload transaction in a controlled test. Confirm no live/saved definition changes and the server reports the transaction failure.
102. Attempt uploadbegin/uploadchunk/uploadcommit without admin rights. Confirm no BossLabs definition is reconstructed or published.
103. First-time convert a safe Matrix3 NPC with Apply Live, then Undo Last Apply. Confirm the BossLabs registration is removed and the NPC returns to its original Matrix3 Java/default combat path.
104. Confirm this authoring slice does not change BossDefinitionStore version/format, NPC spawn/death/pathing/drop ownership, or the still-disabled encounter testing controls.

Current telegraphed tile-attack checkpoint (supersedes items 99 and 104 only for BossDefinitionStore format/version)
105. Eclipse clean/build Client and Server with Java 8. Confirm BossTileOffset, BossAttackDefinition, BossCombatScript, BossDefinitionStore, BossLabsDefinitionWireCodec, BossLabsDraftDefinition, and BossLabsDefinitionEditor compile cleanly.
106. Start with a known-good version-1 data/bosslabs/definitions.bld. Confirm the server loads it successfully, every legacy attack has an empty tile pattern, and normal single-target BossCombatScript behavior is unchanged.
107. Save any BossLabs definition after this patch. Confirm definitions.bld is written as version 2, restart the server, and confirm the same boss still loads. Existing Matrix3 NPC combat/drop/spawn/examine files must remain untouched.
108. Select an attack and paint a simple cross containing 0,0 plus its four cardinal neighbors. Confirm the DRAFT becomes modified, the attack list reports its tile count, and no live behavior changes before Apply Live/Save & Apply.
109. Configure Warning GFX, Impact GFX, and Warning ticks on the painted attack. Apply Live and confirm the complete v2 definition round-trips back into the same attack fields and relative tile offsets when re-inspected.
110. Runtime-test a painted attack while standing on a warned tile. Confirm warning graphics appear on the snapshotted target-centered pattern, then impact graphics appear and the player is damaged through the configured melee/range/magic CombatScript hit path.
111. During the warning period, move from a painted tile to an unpainted tile. Confirm the impact remains at the original snapshotted world tiles and the moved player avoids the hit.
112. With two players present, have one leave the warned pattern and another enter it before impact. Confirm only players occupying painted tiles at impact are eligible for damage and each eligible player receives at most one hit from that impact.
113. Confirm an attack with zero painted tiles continues through the previous single-target BossCombatScript path, including existing projectile and NPC-default behavior.
114. Configure invalid warning ticks (<0 or >50), warning/impact GFX below -1, more than 128 tiles, duplicate offsets, or offsets outside +/-16. Confirm the client draft cannot publish invalid data and the server BossAttackDefinition/BossTileOffset validation remains the final gate.
115. Kill/finish the boss during a delayed tile warning. Confirm the scheduled impact does not damage players after the boss has died/finished.
116. Set a warning duration longer than the normal combat delay. Confirm BossCombatScript delays the boss's next attack until at least the patterned attack's warning/impact window has completed.
117. Save & Apply a patterned attack, restart the server, re-inspect it, and confirm warning/impact values and every painted tile survive persistence exactly.
118. Confirm this slice does not add persistent hazards, minions, alternate targeting, a second combat engine, or world lifecycle ownership; those remain separate future first-boss slices.

Current lingering floor-hazard checkpoint (supersedes item 118 for persistent hazards and items 107/109/117 for store/wire version)
119. Eclipse clean/build Client and Server with Java 8. Confirm BossAttackDefinition, BossCombatScript, BossDefinitionStore, BossLabsDefinitionWireCodec, BossLabsDraftDefinition, and BossLabsDefinitionEditor compile cleanly after the hazard fields/editor-scroll change.
120. Start the server with known-good BossLabs v1 and v2 definition stores in controlled tests. Confirm both load successfully; v1 attacks remain plain, and v2 painted attacks retain their patterns with lingering hazards disabled.
121. Save any BossLabs definition after this patch. Confirm definitions.bld is written as version 3, restart, and confirm existing plain/patterned attacks remain intact. Existing Matrix3 NPC combat/drop/spawn/examine data must remain untouched.
122. Select a painted attack and configure Hazard GFX, Hazard duration, Hazard interval, and Hazard max hit. Confirm duration 0 disables the lingering hazard and the draft remains local until Apply Live/Save & Apply.
123. Confirm a positive hazard duration with zero painted tiles cannot publish. Also reject hazard GFX below -1, duration below 0 or above 100, interval below 1 or above 50, interval greater than enabled duration, and hazard max hit below -1.
124. Apply Live with a painted impact plus a lingering hazard. Confirm the normal warning/impact occurs first, then the exact snapshotted impact tiles remain the hazard area for the configured duration rather than following the player or boss.
125. Stand on a lingering hazard tile and confirm damage occurs at the configured tick interval through the attack's existing melee/range/magic CombatScript accuracy/hit helpers using Hazard max hit (or NPC default when -1).
126. Move off the hazard between damage ticks. Confirm later hazard ticks no longer damage that player; move another player onto the tile and confirm that player becomes eligible on subsequent ticks.
127. Configure a hazard duration of 6 ticks and interval of 2. Confirm hazard damage checks occur every 2 ticks and stop after the configured duration without an extra late damage tick.
128. Confirm the boss can continue selecting/performing later attacks while an earlier floor hazard remains active; the lingering floor does not artificially extend the boss's attack delay beyond the warning/impact window.
129. Kill or finish the boss while its floor hazard is active. Confirm the scheduled hazard stops and does not apply further damage after the boss death/finish condition.
130. Save & Apply a hazard attack, restart the server, and re-inspect it. Confirm Hazard GFX, duration, interval, max hit, and every painted tile survive the v3 store/wire round trip exactly.
131. Resize BossLabs down to its practical minimum and open Attacks. Confirm the fixed phase/attack list remains usable and the expanded right-side attack/hazard/pattern editor scrolls vertically with no unreachable controls.
132. Confirm this slice does not add minions, alternate/random targeting, a general mechanics scripting engine, or alternate world/combat ownership. Those remain separate first-boss slices.

Current alternate-target checkpoint (supersedes item 132 for random targeting and items 121/130 for store/wire version)
133. Eclipse clean/build Client and Server with Java 8. Confirm BossAttackDefinition, BossCombatScript, BossDefinitionStore, BossLabsDefinitionWireCodec, BossLabsDraftDefinition, and BossLabsDefinitionEditor compile cleanly after target mode/range fields are added.
134. Start with known-good BossLabs v1, v2, and v3 stores in controlled tests. Confirm they still load and every older attack defaults to Current target with target range 14.
135. In Attacks, confirm Target offers Current target and Random nearby player, Target range accepts 1-32, edits mark DRAFT modified, and nothing changes live before Apply Live/Save & Apply.
136. Configure Current target and confirm single-target, painted telegraph, and lingering-hazard attacks behave exactly as before this slice.
137. With two eligible players in range, make player A the NPCCombat target and configure an attack as Random nearby player. Confirm that attack prefers player B without replacing the NPC's authoritative combat target.
138. With only player A eligible/in range, use the same Random nearby player attack and confirm it safely falls back to player A rather than cancelling, stalling, or selecting an invalid entity.
139. Put a second player outside the configured Target range and confirm that player is not selected. Move them inside range and confirm they become eligible on a later random-target attack.
140. Confirm dead, finished, not-started, or different-plane/out-of-range players are not eligible alternate targets.
141. Configure a ranged/magic projectile attack as Random nearby player. Confirm its projectile and delayed hit are aimed at the resolved alternate player while NPCCombat continues tracking its original target.
142. Configure a painted telegraphed attack as Random nearby player. Confirm tile origin 0,0 snapshots around the resolved alternate player's location, and any lingering hazard remains on those resulting world tiles rather than following either player.
143. After a random-target attack completes, allow the boss to use a normal Current target attack. Confirm it immediately uses NPCCombat's original target, proving alternate resolution did not mutate target ownership.
144. Reject target mode values outside the supported two modes and Target range below 1 or above 32. Confirm client validation blocks publish and BossAttackDefinition remains the final server validation gate.
145. Save & Apply a random-target attack, restart the server, and re-inspect it. Confirm target mode/range, projectile/tile/hazard fields, and the rest of the definition survive the v4 store/wire round trip exactly.
146. Confirm this slice does not add encounter participant registration, threat/aggro tables, minions, or a second targeting/combat engine; random targeting is a bounded per-attack selection layered over NPCCombat ownership.

Current weighted-rotation checkpoint (supersedes items 134/145 only for store/wire version)
147. Eclipse clean/build Client and Server with Java 8. Confirm BossAttackDefinition, BossCombatScript, BossDefinitionStore, BossLabsDefinitionWireCodec, BossLabsDraftDefinition, and BossLabsDefinitionEditor compile cleanly after weighted-rotation fields are added.
148. Start with known-good BossLabs v1-v4 definition stores in controlled tests. Confirm all still load and every older attack defaults to weight 1, cooldown 0, and immediate repeat allowed while retaining its earlier target/tile/hazard behavior.
149. In Attacks, confirm Weight accepts 1-1000, Cooldown turns accepts 0-100, Repeat rule offers Allow immediate repeat and Prefer another ready attack, edits mark DRAFT modified, and nothing changes live before Apply Live/Save & Apply.
150. Configure two attacks with equal weight 1, cooldown 0, and immediate repeats allowed. Confirm runtime selection remains equivalent to the previous uniform random selection behavior.
151. Configure attack A with weight 9 and attack B with weight 1, both with cooldown 0. Over a meaningful runtime sample, confirm A is selected substantially more often than B without guaranteeing a fixed sequence.
152. Configure attack A with cooldown 2. After A is used, confirm A is excluded from the next two attack opportunities and becomes eligible again on the third later opportunity.
153. Configure all currently ready attacks with cooldowns such that none are eligible. Confirm BossCombatScript performs no attack for one normal NPC attack cycle, advances cooldown turns, and resumes selection once an attack becomes ready rather than violating cooldowns or stalling permanently.
154. Set an attack Repeat rule to Prefer another ready attack. When at least one other non-cooldown attack is ready, confirm the same attack is not selected twice in a row. When it is the only ready attack, confirm it remains selectable so the boss cannot deadlock.
155. Change the boss into a different health phase after rotation state exists. Confirm last-attack and cooldown state reset for the new phase rather than leaking cooldowns/repeat history across phase boundaries.
156. Apply Live with a replacement BossDefinition while the same NPC instance is fighting. Confirm weighted rotation state resets against the new immutable definition and subsequent selections use the new weights/cooldowns immediately.
157. Reject weight below 1 or above 1000 and cooldown turns below 0 or above 100. Confirm client validation blocks publish and BossAttackDefinition remains the final server validation gate.
158. Save & Apply a weighted/cooldown attack, restart the server, and re-inspect it. Confirm weight, cooldown turns, repeat rule, target fields, projectile/tile/hazard fields, and the rest of the definition survive the v5 store/wire round trip exactly.
159. Confirm this slice does not add fixed scripted sequences, phase entry/exit events, minions, encounter participant registration, or a second combat scheduler; weighted rotation remains a bounded attack-selection layer over Matrix3 NPCCombat.

Current generic tile-effect checkpoint (supersedes items 148/158 only for store/wire version and expands the generic-mechanics foundation)
160. Eclipse clean/build Client and Server with Java 8. Confirm BossAttackDefinition, BossCombatScript, BossDefinitionStore, BossLabsDefinitionWireCodec, BossLabsDraftDefinition, and BossLabsDefinitionEditor compile cleanly after generic tile effect fields/controls are added.
161. Start with known-good BossLabs v1-v5 definition stores in controlled tests. Confirm all still load and every older attack defaults both Impact effect and Hazard effect to Damage players while retaining its previous rotation/target/tile/hazard behavior.
162. Save & Apply a definition using non-default tile effects, restart the server, and re-inspect it. Confirm definitions.bld is v6 and both tile effect types plus all earlier attack fields survive exactly.
163. In Attacks, confirm Impact effect and Hazard effect each offer Damage players, Heal players, Damage boss, and Heal boss; edits mark DRAFT modified and nothing changes live before Apply Live/Save & Apply.
164. Configure Damage players as the impact effect and confirm patterned damage retains the previous melee/range/magic CombatScript accuracy, max-hit, and hit-mark behavior rather than becoming fixed environmental damage.
165. Configure Heal players as the impact effect with a fixed explicit amount. Confirm only living/started players occupying painted tiles at impact heal by that amount (capped by Matrix3 HP rules), while players outside the pattern do not heal.
166. Configure Heal players as a lingering hazard effect. Confirm only players occupying painted tiles on each configured interval heal; leaving before a later tick stops healing and entering before a later tick makes that player eligible.
167. Configure Damage boss as the impact effect with a fixed explicit amount. Confirm the boss is damaged only when its footprint overlaps at least one painted tile at impact and no damage occurs when it does not overlap.
168. Configure Heal boss as the impact effect. Confirm the boss heals only while overlapping a painted tile and Matrix3 Entity.heal caps normal healing at max HP.
169. Configure Damage boss and Heal boss as lingering hazard effects in separate tests. Confirm each effect applies only on hazard ticks where the boss footprint overlaps the painted area; moving the boss off the area prevents later ticks from changing its HP.
170. Test a multi-tile boss/NPC. Confirm boss-targeted tile effects trigger when any tile of its footprint intersects the painted area, and a different-plane painted tile never counts as overlap.
171. For Heal players, Damage boss, and Heal boss, set the amount field to -1 and confirm it resolves to the NPC's existing max hit for the attack style; set an explicit 0 and confirm no HP change occurs.
172. Confirm a non-default Impact effect with zero painted tiles cannot publish, any enabled lingering hazard with zero painted tiles cannot publish, and tile effect type values outside the supported four are rejected by both client validation and BossAttackDefinition.
173. Use Damage boss to reduce a BossLabs NPC to zero HP while it has prior real player damage. Confirm Matrix3's normal NPC death path completes without a null-source NPE, and drop/killer ownership remains based on real player received-damage history rather than the self-sourced environmental hit.
174. Confirm this slice does not add phase entry/exit actions, minions, encounter participant registration/context, a general scripting engine, a second damage engine, or a second timer engine; generic tile effects remain a bounded mechanic expressed through Matrix3 Entity/Hit/CombatScript authorities.
