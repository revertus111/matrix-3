Gear Presets runtime test list

1. Clean/refresh the Server project in Eclipse with Java 8 and confirm it compiles.
2. Open Gear > Presets and click preset row 1 selector (component 184); confirm the preset action dialogue opens for preset 1.
3. On preset row 1, click the floppy/save icon (193), green-arrow/load icon (201), and trash/delete icon (225); confirm each action targets preset 1 specifically.
4. Repeat the same controls on preset row 2 using components 233/242/250/274 and confirm they target preset 2 specifically.
5. Verify row 1 and row 2 mapper output still reports packet 103 with slot=65535 and slot2=65535, proving componentId is the row/action discriminator for these runtime-confirmed rows.
6. Click the gear/settings control and both right-side circles for rows 1 and 2; confirm they are consumed without triggering save/load/delete or new game-chat diagnostics.
7. Click at least one matching control on rows 3 and 10 and inspect `Server/data/logs/presets/preset-map.txt`; confirm their component IDs continue the +49 row stride before treating rows 3-10 as VERIFIED.
8. Save a preset with both worn equipment and backpack items, including stack quantities.
9. Change the carried setup, click Load with no bank open, and confirm loading is rejected without item changes.
10. Open a bank, return to Gear > Presets, load the saved preset, and confirm worn/backpack slot contents and quantities match the saved setup.
11. Verify the displaced setup moves into the bank and required preset items are removed from the bank with no duplicate or missing quantities.
12. Test a nearly/full bank where a one-for-one preset swap should still fit.
13. Test a missing required bank item and insufficient final bank space; both must reject before changing items.
14. Save, logout/restart the server, login, and confirm the preset remains available through the same preset slot.
15. Clear a saved preset and confirm it no longer loads after restart.
16. Test while a controller restricts inventory/equipment changes and confirm the preset load is rejected.
17. Click Beast of Burden and other unmapped preset controls and confirm they do not route into one of the ten normal presets.
18. Confirm `[PRESET-MAP]` output remains de-duplicated per click tuple, still appears in the BAT console, and is appended to `Server/data/logs/presets/preset-map.txt`.
