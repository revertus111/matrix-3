2026-09-25
- Added Live Model Editor Bundle 1 runtime-clone proof.
- Added Dev > Edit Model Live plus an Inspector shortcut for selected world objects.
- Reused Matrix3 ObjectDefinitions and the established Class578 direct-render seam; the factory model is cloned privately before editor transforms.
- Added live whole-model scale X/Y/Z, translation X/Y/Z and yaw controls plus explicit object type/rotation and preview-tile offsets.
- Added JSON save/load under dev-model-projects with source object/model IDs and transform state.
- Bundle 1 performs no cache writes, no clipping changes and no server-world persistence writes.
- The source scene object is not replaced/suppressed yet; the editable clone defaults two tiles east and may be overlaid at 0/0 for the runtime proof.

2026-09-25
- Corrected the Bundle 1 scene-render integration: LiveModelEditorPreview is now invoked from the established Class578 developer render pass beside Object Lab/Composite previews.
- This closes the missing render-hook regression from the initial Bundle 1 commit; runtime behavior remains NEEDS TEST.

2026-09-25
- Runtime-confirmed the Bundle 1 private clone and whole-model transform path in the live Matrix3 client using the Smelter reference asset.
- Added Live Model Editor Bundle 2.1 connected-component authoring from the source Class159 triangle graph.
- Added stable mesh-part list selection, white in-world selected-part highlight, isolate, hide/show, per-part scale/translation/yaw around the component centroid, duplicate, project-delete and 64-step undo with Ctrl+Z.
- Added a private raw-geometry rebuild path that starts from fresh source model bytes and rebuilds renderer Models; shared ObjectDefinitions/cache models remain untouched.
- Mirrored object-definition rotation/recolor/retexture/scale/offset/terrain transforms onto the developer raw-model build path before the existing whole-model editor transforms.
- Added JSON project version 2 persistence for original-part edits, duplicate part instances, selected part and isolate state. Version 1 projects remain loadable with default part state.
- In-world mouse hover/click component picking remains a bounded carryover; Bundle 2.1 uses the parts list while the edited geometry itself renders live in the game scene.

2026-09-25
- Fixed Live Model Editor Bundle 2.1 compile regression: ConsoleTheme now provides the shared styleList(JList<?>) helper used by the mesh-parts list.
- The helper applies the existing ConsoleTheme fonts/colors/selection colors without changing model-editor behavior.

2026-09-25
- Added Live Model Editor Bundle 2.2 in-client overlay UX.
- Replaced the standalone JFrame-style editor experience with an owned JWindow anchored over the Matrix3 heavyweight game Canvas, following the same proven ownership approach used by the Construction palette.
- Added drag repositioning while preserving automatic client move/resize tracking.
- Rebuilt the editor layout into a compact RuneScape-style dark/brown/gold panel with target/model context, dense part list, per-part transforms/actions, whole-clone controls and JSON project controls without the previous oversized scroll-heavy form.
- Added transient part-row hover preview: moving across the parts list temporarily highlights the hovered connected component in-world; leaving the list restores the locked clicked selection highlight.
- Hover state is presentation-only and is not written into JSON or undo history. Clicked selection remains the authoritative project selection.
- True 3D world-surface component hover/click picking remains Bundle 2.3 carryover.

2026-09-25
- Started Bundle 2.3A proper live edit-session ownership.
- Editable model replacement now defaults to preview offset 0/0 so it occupies the selected source object's tile instead of appearing as a second nearby clone.
- Reopening the same object/tile/plane resumes the current authoring state instead of resetting transforms/parts or creating another editing workflow.
- Added editor input ownership: action-23 Walk Here is consumed before RTS/Construction routing, normal object/NPC interaction actions are consumed, and Canvas left-click events are consumed while the edit session is active. Overlay controls remain usable; camera-only inputs are not globally disabled.
- Escape or Exit Edit pauses the runtime editor, hides the overlay and immediately restores normal source rendering/input.
- Added client-only source-object visual suppression across Matrix3's eight object scene-render implementations. Matching is object ID + plane + occupied tile bounds for multi-tile entities, or object ID + plane + scene tile for single-tile/wall/decor entities.
- Source suppression is render-only: the original object is not deleted, moved or rewritten; server ownership, clipping, map state and cache bytes remain untouched.
- Bundle 2.3B true screen-to-connected-component hover/click picking remains the next dependency after this replacement/input gate is runtime-verified.

2026-09-25
- Added Bundle 2.3B direct in-world connected-component picking using Matrix3's existing Model.method1376(...) screen hit-test path.
- The editor builds cached component-only pick models from the same source Class159 geometry; no independent camera/raycast stack was introduced.
- Hovering actual model geometry can now drive the same temporary part highlight used by the overlay list; left-click locks the hovered component as the selected part.
- Added direct left-drag transforms with G/R/S modes and X/Y/Z/F axis constraints. Each drag creates one undo checkpoint, while intermediate mouse motion applies transient transform updates.
- Overlay selection/precision spinners synchronize back to the world-selected part so the number fields are now precision controls rather than the primary editing workflow.
- Added Bundle 2.4A Construction material replacement foundation. The current ConstructionPlacementController build-piece catalog is exposed in Live Model Editor as a shared source-object library.
- Replace Part hides the selected source component and renders the chosen Construction object's model geometry aligned to that component. Replace Matches applies the same replacement to geometrically matching repeated source parts such as repeated bars/supports.
- Replacement auto-fit centers on the source component, scales to its longest dimension and rotates 90 degrees in X/Z when dominant horizontal axes differ. Existing per-part transforms remain available for fine alignment.
- Restore removes the replacement recipe and reveals the untouched original component.
- JSON project format advanced to v3 and now persists replacement object/type metadata. No cache bytes, map objects, clipping or server persistence are written.

2026-09-25
- Fixed the Bundle 2.3B OpenGL crash in component-only world-pick models. The compact Class159 remap now sets anInt1775 to its compact used-vertex count before OpenGLModel construction; previously it remained zero, causing OpenGLModel's per-vertex face-reference array to be allocated at length 1 and then indexed by vertex 1+.
- Added face-remap range validation so a malformed component mapping is rejected before renderer construction instead of crashing the client.
- Recorded Conveyor belt object 46298 / model IDs 49717+49718 as the preferred Construction conveyor visual-source experiment. The intended workflow is isolate the belt component, duplicate it and translate copies end-to-end rather than fabricating a belt mesh from scratch.

2026-09-25
- Added Live Model Editor Bundle 2.5 professional selection/editor pass.
- Replaced single-part-only authoring state with a real persistent selection set while retaining one primary part for precision fields and legacy JSON compatibility.
- Added Whole / Part / Multi selection modes, list multi-select, additive/toggle world selection, Select All, Clear and Reset Selection.
- Group move/rotate/scale gestures now preserve each selected part's starting transform and create one undo snapshot per gesture; hide/delete/duplicate/replacement/reset actions operate on the full selected set.
- Multi-selection highlight/isolate now renders the full active selection instead of one primary part.
- Corrected direct mouse transform signs so move/rotate/scale drag direction is no longer inverted; Whole mode can now use the same G/R/S + axis workflow against clone transforms.
- Project JSON advanced to v4 with partSelection persistence while retaining partSelected for older projects.
- Added Save Selection export to dev-model-assets as matrix3-live-model-selection JSON containing source object/model IDs and selected component recipes.
- Live Model Editor overlay is now resizable with a lower-right drag grip and scroll fallback so controls remain reachable on smaller client windows.

2026-09-25
- Bundle 2.5 follow-up: multi-part isolate visibility now follows the full selected set independently of transient hover preview, preventing hover from collapsing an isolated multi-selection to one component.
- Guarded JList selection-mode transitions so switching Whole/Part/Multi cannot fire a stale Swing selection event back into the authoritative editor selection.
- Reset Sel now resets the whole-clone transform when Whole mode is active and resets component transforms in Part/Multi modes.

2026-09-25
- Added Live Model Editor Bundle 2.6 shared RTS authoring camera.
- Live Model Editor now leases the existing ConstructionBuildCamera RTS controller on open instead of creating a parallel camera implementation.
- Editor camera controls: WASD/arrows pan, Q/E yaw orbit, mouse wheel zoom, and middle-mouse drag orbits yaw plus bounded pitch around the existing RTS pivot.
- LMB remains exclusively owned by model selection/direct G/R/S transforms; non-LMB drag events are no longer consumed by the editor transform gate.
- Editor exit restores a camera mode that was already active before editing, and only closes a camera session it started when no settlement/palette owner needs it.
- RTS accepted pitch is persisted with the existing client-session yaw/pivot/orbit-distance view state.

2026-09-25
- Started Bundle 2.7 Professional Editor Shell and added EDITOR_WORKSPACE_DESIGN.md as the compact 16-inch workspace design authority.
- Replaced the page-length shell with a right-side P/T/M/C/O/S rail and one active compact drawer.
- Removed whole-editor horizontal scrolling; only the Parts list owns vertical scrolling.
- Transform is selection-context-sensitive: Whole uses clone values while Part/Multi uses selected-component values in the same drawer.
- Added RTS/FREE camera switching through the shared ConstructionBuildCamera owner; FREE_BUILD is exposed for under/inside geometry inspection.
- Editor buttons and part-list selection now return keyboard focus to the Matrix3 Canvas; MMB world press explicitly reacquires Canvas focus. Exact numeric fields remain focusable.
- Preserved resize, selection, isolate/show/hide, duplicate/delete/undo, replacement, source controls, project save/load and Save Selection export.
- Dedicated gameplay-HUD suppression is deferred to Bundle 2.7B until Matrix3's authoritative root-interface seam is verified; no hardcoded masks were added.

2026-09-25
- Bundle 2.7A focus follow-up: Load Project and Save Selection file-chooser actions are marked to retain editor/desktop focus while their modal dialogs are active; ordinary editor buttons still hand focus back to the Matrix3 Canvas.

2026-09-25
- Bundle 2.7 runtime-fix pass: implemented Editor Workspace HUD suppression through server InterfaceManager root-1477 ownership. Direct root mounts are snapshotted and hidden while component 12 (world/game viewport) stays visible; interfaces are not closed or removed.
- Added owner-only itemBrowser editorhud enter/exit/status bridge. Live Model Editor requests suppression on open and restoration on Exit/Escape; restore unhides the snapshot and reapplies normal gameframe/mode visibility.
- Fixed FREE camera backward conflict: plain S is no longer consumed as Scale. Scale moved to V so WASD remains camera movement.
- Added ConstructionBuildCamera.mapScreenDragToGround(...) using the actual detached camera view vector. Free-axis Whole/Part/Multi MOVE now follows screen direction at any RTS/FREE camera heading instead of assuming fixed world X/Z.
- Explicit X/Y/Z constraints remain world-axis transforms; only Free-axis planar MOVE is camera-relative.

2026-09-25
- Hardened Live Model Editor part rendering after a runtime ArrayIndexOutOfBoundsException surfaced in renderPartModels while editing Conveyor 46298.
- Added a renderer-facing Class159 validation gate for vertex/face table sizes and face vertex references before editor-generated main/duplicate/replacement/pick raws reach OpenGL.
- Normalizes Class159.anInt1775 to the actual used-vertex range when its decoded/generated value is inconsistent but recoverable.
- Added per-stage safe model construction for main, duplicate, replacement and world-pick models. A malformed individual authoring raw is now skipped and logged instead of crashing the entire Matrix3 client.
- Part-model cache no longer retries a failed null main model every frame for the same revision; the next editor geometry revision retries cleanly.
- Added defensive recolor/retexture array-length bounds in the custom definition-model builder.
- Crash diagnostics are rate-limited per editor revision and identify stage/part/revision.

2026-09-25
- Bundle 2.7 compact-workspace follow-up: root-1477 HUD suppression is now persistent. The server seeds known NIS/root chrome component IDs, merges current mounted root children, and reasserts hidden state while Editor Workspace is active.
- Any interface subsequently mounted into root 1477 is immediately hidden unless it is component 12 (world/game viewport).
- Live Model Editor reasserts editorhud enter at a low 1-second cadence so NIS scripts cannot permanently resurrect minimap/chat/backpack/actionbar chrome.
- Merged Parts + Transform into one larger EDIT drawer. Whole/Part/Multi, All/Clear/Reset, Move/Rotate/Scale, Free/X/Y/Z, numeric transforms, component list and common part actions are now together.
- Simplified rail to E/M/C/O/S and enlarged default editor workspace from 304x560 to 390x720 (minimum 350x560).

2026-09-25
- Fixed Editor Workspace black-screen regression from the persistent root-HUD pass.
- Root component 8 is Matrix3 SCREEN_BACKGROUND_COMPONENT_ID and is structural to the live scene; it is no longer included in the explicit developer-HUD hide set.
- Added a shared structural-component guard so both the initial root snapshot and later root-interface mounts can never hide component 8 (screen background) or component 12 (game/world viewport).

2026-09-25
- Replaced automatic broad editor HUD suppression with explicit manual HUD controls after root hiding still black-screened the live world.
- Editor now opens with normal HUD visibility and no periodic broad hide reassertion.
- Added H / HUD VISIBILITY drawer with concrete NIS root component -> interface ID rows, multi-select Hide Selected / Show Selected, Hide Mounted HUD, and Restore All.
- Structural root components 8 (screen background) and 12 (game/world viewport) are not present in the HUD list and are rejected by the server hide API.
- Server editorhud now supports safehide, restore, hide <rootComponent>, show <rootComponent>, and status. Only exact components already hidden are re-hidden on later mounts.
- Exit/Escape restores all HUD components hidden through the editor.

2026-09-25
- Removed the standalone H / HUD rail tab because H is already the Live Model Editor Hide Part hotkey.
- HUD visibility controls now live inside C / Camera/View beneath RTS/FREE camera controls.
- Camera/View keeps Hide Mounted HUD, Restore All, root-slot multi-select, Hide Selected and Show Selected.
- Reduced the embedded HUD list to a compact 9-row/190px view so the combined Camera/View drawer remains practical on the 16-inch workspace.
- Keyboard H behavior was intentionally left unchanged: it still toggles selected-part visibility.


2026-09-26
- Started Bundle 2.7C professional transform tool.
- Added authoritative TRANSFORM_GIZMO_SPEC.md for viewport gizmos, live inspector, pivots, snapping, numeric editing and gesture ownership.
- Normal transform drag remains free; Ctrl temporarily enables snapping.
- Added persistent SNAP mode; while enabled, Ctrl temporarily bypasses snap for free movement.
- Added configurable Move snap (default 16 model units) and Angle snap (default 15 degrees) in the EDIT workspace.
- Snapping is applied inside the existing drag gesture so Part/Multi transforms retain one-drag/one-undo transaction behavior.
- Scale snapping remains intentionally deferred.


2026-09-26
- Added Bundle 2.7C-B contextual live Transform Inspector.
- Replaced the visible duplicate Whole/Part transform spinner grids with one contextual Position / Rotation / Scale inspector.
- Inspector values update continuously from the authoritative Whole/Part/Multi runtime transform while viewport editing.
- Scale is displayed as decimal ratios (1.00 = internal 100) without changing project/runtime storage format.
- Exact numeric entry now supports Enter/Tab commit, Escape cancel, valid blur commit, and invalid/partial blur revert.
- Editor hotkeys no longer fire while a JTextField/JSpinner/ComboBox owns text input.
- Removed the duplicate Swing Ctrl+Z binding so inspector typing cannot accidentally undo model geometry.
- Part list now expands into remaining Edit-drawer height instead of keeping a fixed short viewport.
- Runtime verification intentionally remains batched with later 2.7C gizmo work per user request.


2026-09-26
- Added Bundle 2.7C-C first Matrix3-native Move gizmo.
- LiveModelEditorParts now exposes the real edited Part/Multi selection centroid for pivot ownership.
- LiveModelEditorPreview projects the pivot and X/Y/Z basis through Class106.method1792 and draws handles through renderer-native 2D primitives.
- Move gizmo uses fixed screen-length handles, neutral center/free handle, axis colors, yellow hover and white active-drag feedback.
- Clicking a gizmo handle takes priority over component picking; Multi selection is therefore not toggled when Ctrl is used for temporary snapping on a handle.
- Axis drag movement is projected onto the visible handle direction and then routed through the existing Move + Snap transform path.
- Part/Multi gizmo drags keep one gesture = one undo snapshot through the existing parts transaction API.
- Class578 draws the gizmo after the other developer preview models; no Canvas.getGraphics or second Swing viewport overlay was introduced.
- Mirrored-definition and terrain-contour point projection remain explicitly UNKNOWN pending runtime evidence instead of being guessed.


2026-09-26
- Runtime feedback on Conveyor belt 46298 confirmed the contextual inspector, visible pivot and native Move gizmo; snap was the reported failure.
- Added Bundle 2.7C-D corrected snapping + yaw Rotate gizmo.
- Move snap now quantizes gesture delta from the mouse-down transform instead of rounding the final absolute transform every drag frame.
- Angle snap now quantizes yaw delta from the gesture start by the configured Angle Snap increment.
- Added an editor-level Ctrl key latch alongside MouseEvent modifier state; SNAP OFF + Ctrl and SNAP ON + Ctrl no longer depend on each Canvas drag event preserving the Ctrl modifier bit.
- Added one Matrix3-native yaw rotation ring using the existing projected pivot/XZ basis, renderer line primitives, yellow hover and white active state.
- Ring drag converts pointer angle around the projected pivot into yaw delta.
- Multi yaw now orbits selected component centers around their shared pivot while rotating each component's yaw by the same delta.
- Shared-pivot yaw semantics are implemented in the common selection-delta layer so viewport rotation and exact Multi yaw edits stay consistent.
- Scale snapping remains deferred.


2026-09-26
- Runtime feedback clarified that Scale was also nonfunctional in the same Conveyor 46298 session where Snap failed.
- Added Bundle 2.7C-E repaired Scale interaction + Matrix3-native Scale gizmo.
- Scale mode now renders X/Y/Z projected handles with square endpoints plus a larger center uniform-scale handle.
- Axis scaling projects mouse movement onto the visible handle direction.
- Center uniform scaling uses right/up to grow and left/down to shrink.
- The old fallback direct-scale drag direction was normalized to the same convention.
- Scale gizmo uses the existing Whole/Part/Multi pivot, gizmo-first hit ownership, yellow hover and white active feedback.
- Part/Multi scale gestures continue to use one mouse drag = one undo snapshot.
- Existing 10–400 internal scale limits and 1.00-style inspector display are unchanged.
- Scale snapping remains intentionally deferred; Move/Rotate are the only transform modes affected by the current Snap controls.


2026-09-26
- User runtime-confirmed the corrected 2.7C-D/E Snap + Rotate + Scale core workflow on Conveyor belt 46298.
- Added Bundle 2.7C-F professional transform interaction polish.
- Added persistent visual active states for Whole/Part/Multi, Move/Rotate/Scale, Free/X/Y/Z, Isolate and the active editor tool rail.
- Added a compact live EDIT readout showing selection context, transform mode/axis and the current authoritative transform value.
- Added horizontal numeric scrubbing to Position/Yaw/Scale inspector fields with Shift fine and Ctrl coarse modifiers.
- Part/Multi numeric scrubbing reuses the existing transform gesture transaction so one scrub is one undo record.
- Added inspector mouse-wheel value adjustment.
- Added Alt+Arrow keyboard nudging with Alt+Shift coarse steps while preserving plain arrow keys for camera navigation.
- Centralized editor shortcut ownership and added Ctrl+O Load, Shift+H Show All, I Isolate and Tab collapse/expand.
- Removed obsolete duplicate createPartsPanel/createTransformPanel UI code and unused transform CardLayout state.
- Native text beside the 3D gizmo remains non-blocking CARRYOVER because no verified Matrix3 renderer-font seam was established; no second Swing viewport overlay was added.
