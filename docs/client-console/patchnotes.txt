2026-08-31
- Added the Matrix3 Client Console design authority.
- Locked V1 to a RuneLite-style sidebar shell with Owner, Commands, Player, and Debug panels.
- Defined strict ownership boundaries so the console remains a UI/tooling layer over authoritative Matrix3 systems.
- Added hard responsive-layout/no-cutoff requirements; Client Console functionality may not depend on fullscreen/maximized mode.
- Added permanent modern dark-theme requirements and centralized presentation/styling responsibilities instead of finished default Swing visuals.
- Added resizable/customizable layout, validated layout persistence, DPI/display-scaling targets, multi-resolution acceptance checks, and safe reset/clamping behavior.
- Expanded persistence to restore complete valid client/console workspace geometry after clean restart, including client size/position/state, console width/open state, active panel, and meaningful split/collapse state.
- Required one versioned Client Console preference authority with safe defaults, monitor-bound clamping, reset-layout behavior, and debounced/coalesced writes during resize.
- Added smoothness requirements for lazy panel initialization, non-blocking thread ownership, resize-without-rebuild behavior, clean game/console focus handoff, minimum game-view protection, panel failure isolation, and restrained animation.
- Implemented the Phase 2 frame-level Client Console shell around the existing RS3Applet without changing Matrix3 renderer/canvas ownership.
- Added a permanent dark shell with a thin icon rail, scrollable placeholder content, collapse/reopen behavior, and a draggable width boundary that protects a 640px minimum game view.
- Added one versioned Java Preferences workspace authority for window position/size/maximized state, console open state/width, and active-panel identity; resize writes are coalesced and invalid monitor geometry is clamped.
- Added a reset-layout action and a dedicated focus-test field; game/console keyboard focus remains runtime acceptance rather than assumed behavior.
- Added the first real Owner panel with a dedicated O rail button, lazy panel creation, read-only local display-name/rights/client-state visibility, and a small client bridge that keeps decompiled rights decoding out of UI code.
- Added reusable shell panel selection behavior: selecting another panel opens it, selecting the active panel again collapses the console, and panel-specific initialization failures are isolated to the panel host where practical.
- Finished active-panel workspace persistence so the selected shell/Owner panel restores after restart and Reset Client Console Layout returns selection to the shell panel.
- Kept the Owner slice read-only; no server command logic, permissions, gameplay behavior, or command execution path changed.
- Added the lazy Commands panel with a >_ rail entry, search, category filtering, inline arguments/preview, and a scroll-safe button for each of the 304 unique command names currently exposed by Commands.java switch handlers.
- Added a client-console command queue that hands Swing actions to the Matrix3 client cycle before building/sending the existing opcode-24 COMMANDS_PACKET with clientCommand=false; server Commands.processCommand remains authoritative for permissions and behavior.
- Added inline double-confirm protection for destructive command buttons instead of modal confirmation dialogs.
- Extended active-panel persistence and shell failure isolation to the Commands panel without changing server command implementations.

2026-09-01
- Added the lazy Client Console Item Browser behind the I rail button with automatic ID/name search over Matrix3 item definitions.
- Added real Matrix3-rendered item thumbnails through a bounded client-cycle render queue so Swing never renders cache models directly.
- Added persistent favorites, multi-category membership, custom categories, presets, and per-preset saved quantities through the Client Console preference store.
- Added Inventory spawn quantities 1/5/10/X and preset spawning through the existing Client Console queue and normal Matrix3 ::item command authority.
- Added right-click Bank spawn quantities 1/5/10/X and preset bank spawning through the isolated ItemBrowser packet/server bridge; Commands.java remains untouched.
- Bank spawning validates owner/admin rights, live player started/running/not-finished state, item definition validity, and positive quantities before calling Matrix3 Bank.addItem(...).
- Added right-click favorite, category, preset membership, preset quantity, and removal actions.
- Kept item definition indexing off the Swing event-dispatch thread and limited visible results/thumbnail work to avoid blocking normal client rendering.
- Fixed Item Browser item-row interaction: a normal left-click now selects the clicked row and queues one copy to Inventory; right-click keeps the quantity/Bank/organization menu.

2026-09-02
- Rerouted Item Browser Inventory spawning from the generic ::item command handoff to the isolated ItemBrowser bridge, matching the already-isolated Bank path while leaving Commands.java untouched.
- ItemBrowserCommandBridge now accepts inventory or bank destinations, applies the same rights/live-player/item/amount validation once, then calls Matrix3 Inventory.addItem(...) or Bank.addItem(...) and reports explicit success/failure in game.
- Preset Inventory spawning now uses the same ItemBrowser inventory bridge as single-item and quantity-button spawning.
- Moved Client Console command/thumbnail queue draining onto Class514.method6093(...), a callsite directly invoked by the logged-in client loop, and removed the earlier unverified Class572_Sub12_Sub17 hook so queued Item Browser and Commands actions have a live client-cycle sender.
- Changed Item Browser result interaction so single left-click only selects an item; spawning now requires the Inventory/Bank quantity controls, right-click spawn actions, preset actions, or a double left-click quick-spawn of one item to Inventory.
- Changed Item Browser non-preset result ordering from alphabetical name order to ascending item ID so the normal browse/search list runs from the lowest usable ID, including ID 0 when defined, upward.
- Added a read-only RoTS cache-research card to Owner with Scan RoTS, Copy All, and Clear controls plus a scrollable monospaced output box.
- Added ClientConsoleRotsBridge and narrow definition-factory registration hooks so Owner can inspect live Matrix3 NPC/animation definitions without a standalone Java main or server command path.
- RoTS scanning runs on a daemon worker thread and only returns completed text to Swing, avoiding full cache research work on the event-dispatch thread.
- The focused dump covers NPCs 18538-18551 plus animation neighborhoods 17960-18040 and 21920-21940, including models, render emotes, known params, frame durations/IDs, hand overrides, and handled-sound data.

2026-09-03
- Added a separate Owner `Deep Scan` action for focused RoTS render/BAS and GFX correlation while preserving the existing normal RoTS scan.
- Deep Scan resolves known RoTS render sets into raw cache opcode animation references and marks targeted animation candidates by render-set membership without assigning unverified mechanic semantics.
- Added a narrow GraphicsDefinition loader registration hook and read-only GFX scan for explicit candidate-animation or known RoTS-model matches.
- Deep Scan remains daemon-worker based; Copy All/Clear continue to operate on the complete generated text without gameplay/server mutation.
- Fixed Owner content clipping by making the scroll content track viewport width and wrapping long informational text instead of widening the panel off-screen.
- Added a read-only RoTS GFX bootstrap that builds the spot-animation definition loader from the already-live config archive before Deep Scan, removing the dependency on a prior runtime GFX instantiation.
- Added the lazy S/Settings rail panel with independent Legacy combat and Legacy interface toggles plus an Apply saved combination action.
- Added isolated `itembrowser settings` server actions for Legacy/EoC combat and Legacy/NIS interface selection while keeping Commands.java untouched.
- Split controls normalize Matrix3's original coupled `Player.legacyMode` master state off, then keep combat authority in `CombatDefinitions` while applying the known legacy-interface var state independently.
- Legacy combat + NIS is represented directly as Legacy combat ON / Legacy interface OFF; the opposite and matched combinations are also selectable for runtime verification.
- Saved Client Console mode selections use local Java Preferences and are queued automatically when the Settings panel becomes visible after login; the server still validates owner/admin and live-player state.
- The original in-game Legacy Mode checkbox remains the existing coupled Matrix3 control; applying the Client Console split settings re-normalizes that combined state before applying the selected pair.
- Added a Cache development card to the C shell with an Open RS3 CacheEditor button.
- CacheEditor launches as a separate Server-side process through the existing Gradle wrapper/task, avoiding a Client-to-Server compile dependency.
- Duplicate launch requests are suppressed while the CacheEditor child process remains alive, and the launcher resolves the Server project relative to the normal Client runtime/project location.
- Fixed CacheEditor project detection so the Client Gradle project cannot be mistaken for the Server; a candidate must contain the actual Server-side CacheEditorLauncher source before `runCacheEditor` is invoked.
- Hardened CacheEditor launch by invoking the absolute Server Gradle wrapper and passing `-p <Server>` explicitly; launch diagnostics now print the resolved Server project and wrapper paths before Gradle starts.
- Kept the default Item Browser result list capped at 160 rows for normal browsing, but non-empty ID/name searches now populate every matching definition while thumbnail rendering remains bounded/lazy.
- Added a 1.25-second Item Browser search debounce so partial one/two-letter input does not rebuild the full matching result model on every keystroke; indexing refreshes continue using the last applied query until typing pauses.
- Corrected the RoTS Deep Scan GFX loader path: the previous bootstrap incorrectly fabricated a spot-animation loader from the render-set loader archive, which could produce a believable count followed by widespread definition decode failures.
- The authentic Matrix3 `Class639` GFX loader now registers itself with the RoTS research bridge when constructed with `Class452_Sub1`; Deep Scan no longer guesses or substitutes a config archive.
- Simplified `ClientConsoleRotsGfxBootstrap` to readiness-only behavior so it cannot construct a mismatched loader.
- GFX correlation now aggregates decode failures, prints at most the first five examples, and explicitly marks zero-match results as incomplete whenever decoding failed instead of presenting them as evidence.
- Extended focused/Deep Scan animation coverage through 21947, including the runtime-confirmed 21941 shared melee spin and current Guthan 21944/21945/21947 mappings.
- Corrected the Owner dump label for NPCs 18546-18551 to body-only/inactive verified-static definitions that are runtime-invisible as the current subdued presentation; their authentic role remains HYPOTHESIS.
- Added the lazy P/Player V1 panel with read-only display name, rights, client state, world tile, plane, and region visibility.
- Player world position is derived from the live local-player scene transform plus Matrix3's current scene base; plane uses the same local-player field already used by Dev Mode world actions.
- Player region display is derived from the resolved world tile using Matrix3/RuneScape 64x64 region boundaries and includes both region ID and region X/Y.
- Player panel refresh runs only while the panel is showing and performs no server command, packet send, gameplay mutation, or persistence write.

2026-09-04
- Replaced the Client Console rail's permanent C/O/>_/P/I/S text placeholders with original Java2D home, owner, commands, player, items, and settings icons.
- Added shared `ConsoleIcons` rendering with consistent 22px geometry, antialiasing, theme-aware normal/hover/selected/disabled coloring, tooltips, and accessible names without external icon assets or dependencies.
- Kept the existing rail size, panel IDs, lazy panel lifecycle, selection behavior, persistence, and collapse semantics unchanged; this is presentation-only shell polish.
- Updated the shell's docking help text to describe icon-based navigation rather than the removed letter buttons.
- Reduced Item Browser search debounce from 1.25 seconds to 500ms while preserving the existing bounded empty-list behavior, full non-empty search results, lazy thumbnails, and Swing-thread refresh ownership.
