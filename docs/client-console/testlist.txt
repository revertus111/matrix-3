Matrix3 Client Console - shell + Owner + Commands + Player + Item Browser + Atlas + Settings runtime test list

1. Start the client through the normal Matrix3 client launch path. Confirm the game still renders and reaches normal login/gameplay with the Client Console attached beside the game view.
2. Confirm the shell is dark-themed, the M3 rail remains visible, the Home icon visibly indicates the open state, and the placeholder content scrolls vertically when space is limited.
3. Click the Home icon to collapse the console. Confirm only the narrow rail remains, the game expands into the freed space, and clicking Home again restores the panel immediately.
4. Drag the console's left divider repeatedly while logged in. Confirm resize is continuous, does not reload/rebuild interfaces or game state, and does not visibly stall rendering.
5. Drag the console as wide as possible. Confirm it clamps before the RuneScape game view becomes narrower than roughly 640px at supported frame sizes.
6. Resize the main client window while the console is open and collapsed. Confirm important shell controls remain reachable and the panel does not require maximized/fullscreen mode.
7. In the Focus test field, type W/A/S/D and other game hotkeys. Confirm text entry receives the keys and the game does not also consume them. Click the game view again and confirm normal game input resumes.
8. Move/resize the client, change console width/open state, then close Matrix3 normally. Relaunch and confirm the previous valid window geometry, maximized/restored state, console width, and open/collapsed state restore automatically.
9. Move the client to a second monitor, close normally, disconnect/disable that monitor, then relaunch. Confirm the saved window is clamped onto an available monitor rather than reopening off-screen.
10. Click Reset Client Console Layout. Confirm the frame returns to a safe normal-state default, the console opens at its default width, and the new state persists on the next clean restart.
11. Exercise rapid frame resizing and divider dragging for several seconds. Confirm there is no obvious disk-I/O hitching; preference writes are coalesced rather than issued for every drag event.
12. Run the normal client smoke path after the shell test: login, move, interact with the world/interface, and close normally. Any Canvas/Swing flicker, z-order issue, stuck focus, or input leak is a shell blocker and should be reported as runtime evidence.
13. Click the Owner icon on the rail. Confirm the Owner panel is created/opened on demand and shows the local display name, decoded rights label, and current client-player state without changing gameplay/server state.
14. Open Owner before logging in, then log in while leaving Owner visible. Confirm its read-only account/client-state values refresh without switching panels and without visible client hitching.
15. With Owner active, click the Owner icon again. Confirm the panel collapses to the narrow rail. Click Owner again and confirm it reopens immediately.
16. Switch between Home and Owner repeatedly. Confirm only the selected rail icon is highlighted, panel switching is immediate, and the game renderer/input remains normal.
17. Leave Owner selected, resize the console, close Matrix3 normally, and relaunch. Confirm Owner is restored as the active panel together with the saved valid console/window geometry. Repeat once with the console collapsed and confirm reopening returns to Owner.
18. Click Reset Client Console Layout while Owner is selected. Confirm selection returns to the Home/shell panel, default geometry is restored, and the reset state survives a clean restart.
19. Confirm the Owner panel contains no active gameplay/admin quick-action buttons in this slice and that normal server command permissions/behavior are unchanged.
20. Click the Commands icon on the rail. Confirm Commands is lazy-created, opens immediately, shows 304 total current command buttons, and remains scrollable at narrow/non-maximized window sizes without clipping the search/composer controls.
21. Search for item, npc, bank, tele, gfx, and shutdown. Confirm filtering is immediate, case-insensitive, and the shown-count updates without visible game/render hitching.
22. Change the category filter between All, Admin, Punishment, Moderator, Support, Spawn, and Player. Confirm only matching command buttons remain visible and no controls become unreachable.
23. Select ::bank with no arguments and click Run Command. Confirm the command is queued by the console, sent on the Matrix3 client cycle, and the existing server command behavior opens the bank.
24. Select ::item, enter 4151 1 in Arguments, confirm the preview reads ::item 4151 1, then run it. Confirm the normal server command path processes the item command and remains independent from Item Browser spawning.
25. Select an argument command, edit its arguments repeatedly, and press Enter from the Arguments field. Confirm Enter uses the same queued command path as the Run Command button.
26. While focused in Commands search/arguments fields, type W/A/S/D and other game hotkeys. Confirm the game does not also consume those keystrokes; clicking the game restores normal input.
27. Select ::shutdown or another protected command and click Run Command once. Confirm it changes to Confirm Run and DOES NOT execute. Let five seconds expire and confirm another first click still does not execute. Do not perform the second confirmation unless intentionally testing the destructive command.
28. Queue several safe commands quickly. Confirm the UI stays responsive and commands are drained on the client cycle rather than directly mutating the Matrix3 packet queue from the Swing event-dispatch thread.
29. Leave Commands selected, close Matrix3 normally, and relaunch. Confirm Commands restores as the active panel with the previous valid console/window geometry.
30. Verify normal typed ::commands still use the original server authority and that Client Console buttons do not bypass Matrix3 rights checks, command logging, or Commands.processCommand behavior.
31. Click the Item Browser icon on the rail. Confirm Item Browser is created only on first open, becomes the active panel, and opening it does not visibly freeze the game while item definitions are indexed.
32. While Item Browser indexing is in progress, continue moving/interacting in game and type in the search field. Confirm the client remains responsive and results/status update progressively rather than waiting for one blocking full-cache scan.
33. Search a known item by exact numeric ID. Confirm the matching definition appears without selecting an ID/name search mode. Then search by a partial mixed-case item name and confirm matching names filter automatically and case-insensitively.
34. Inspect several common items in the result list. Confirm each loaded preview is the real Matrix3 item render rather than a placeholder/static icon and that scrolling a long result set does not cause a sustained render hitch.
35. Select a known item and use Inventory 1, 5, 10, and X. Confirm each request queues itembrowser inventory <id> <amount>, reaches ItemBrowserCommandBridge, and calls Matrix3 Inventory.addItem(...) with the expected quantity.
36. Right-click a known item and use Spawn to Inventory 1, 5, 10, and X. Confirm the right-click actions behave identically to the Inventory quantity buttons and use the same ItemBrowser inventory bridge.
37. Right-click a known item and use Spawn to Bank 1, 5, 10, and X. Confirm the item is added directly to the player's Matrix3 bank with the expected quantity even when the bank interface is closed.
38. Repeat Inventory and Bank spawn while logged in without admin/owner rights. Confirm the server rejects both isolated Item Browser bridge actions. Confirm ordinary Commands.java behavior is otherwise unchanged.
39. Exercise an Inventory or Bank request during login/transition or while the Player is not fully started/running, where practical. Confirm ItemBrowserCommandBridge refuses to mutate either container until Player.hasStarted(), Player.isRunning(), and !Player.hasFinished() are all satisfied.
40. Attempt Inventory and Bank spawn with an invalid item ID, zero/negative quantity, and malformed X input where the UI permits. Confirm no container mutation occurs and invalid server-side item/amount requests are rejected safely.
41. Favorite two items, remove one favorite, switch to Favorites, and confirm only current favorites appear. Close Matrix3 normally, relaunch, and confirm the remaining favorite persists.
42. Create at least two custom categories. Add one item to both categories and another item to only one category. Confirm category views reflect independent multi-category membership and that all memberships survive a clean restart.
43. Create a preset, add multiple items, assign different saved quantities, change one quantity, and remove one item. Confirm the preset view displays the saved quantities and the edits survive a clean restart.
44. Run the saved preset to Inventory. Confirm each preset line uses its saved quantity and routes through ItemBrowserCommandBridge/Inventory.addItem rather than the generic ::item command path.
45. Run the saved preset to Bank. Confirm each preset line uses its saved quantity and routes through ItemBrowserCommandBridge/Bank.addItem without requiring a Commands.java item-browser command.
46. Right-click items in All/Favorites/Category/Preset views. Confirm Favorite, category membership, preset membership, preset quantity, and preset removal actions remain reachable without changing views unexpectedly.
47. Leave Item Browser selected, close Matrix3 normally, and relaunch. Confirm the Item Browser rail icon restores as active and favorites/categories/presets still contain their previous saved data.
48. Rapidly switch Home/Owner/Commands/Items, resize the console, search items, scroll thumbnails, and queue a few safe spawns. Confirm the game renderer remains responsive, no panel is rebuilt unnecessarily, and no Swing/client-thread exception appears.
49. Final regression: type and execute a normal existing ::command, use the Commands panel, spawn an item to Inventory from Item Browser, then spawn one to Bank. Confirm both Item Browser actions use ItemBrowserCommandBridge while Commands.java itself remains untouched.
50. Single-left-click directly on a visible Item Browser result row. Confirm that exact row becomes selected but no spawn is queued and no item is added. Double-left-click the row and confirm exactly one copy is queued through itembrowser inventory. Left-click empty list space and confirm no item is spawned; right-click the row must still open the quantity/Bank/organization menu without an automatic Inventory spawn.
51. For one successful Inventory spawn and one successful Bank spawn, confirm an in-game success message names the quantity/item/destination. Fill or restrict the target container where practical and confirm the bridge reports a clear failure instead of only leaving the Client Console status at "queued".
52. After login, single-click a known Item Browser row and confirm selection changes without a server spawn response. Then click Inventory 1 or double-click the selected row and confirm the sidebar reports the Inventory spawn queued, an in-game server success/failure message arrives, and the item appears when accepted. Run one safe Commands-panel command immediately afterward and confirm it also drains without any unrelated client action.
53. Clear the Item Browser search in All mode after indexing completes. Confirm visible usable definitions are ordered by ascending item ID starting with the lowest defined ID (including ID 0 when present), not alphabetically by name. Search a partial item name with multiple matches and confirm those matching rows also remain in ascending item-ID order; exact numeric ID search must still return that one definition directly.
54. Open Owner after the client has loaded NPC/animation definitions. Confirm the RoTS cache-research status reaches Ready without starting an automatic full-cache scan.
55. Click Scan RoTS. Confirm the button disables while work runs, the game/Swing UI remains responsive, and the completed text box contains NPCs 18538-18551 plus populated animation entries from 17960-18040 and 21920-21947.
56. Confirm the dump labels NPC 18539 as VERIFIED Ahrim flying, NPC 18542 as VERIFIED Guthan spearless/Impale, and NPCs 18546-18551 as verified-static body-only/inactive definitions that are runtime-invisible as the current subdued presentation; their authentic role remains HYPOTHESIS.
57. Click Copy All and paste into a plain text editor. Confirm the complete dump is copied in one operation, including text outside the visible scroll area. Click Clear and confirm only the research text/status resets; no game/cache state changes.
58. While a RoTS scan runs, move/interact in game and resize/switch Client Console panels. Confirm no visible render hitch, cache mutation, server command, or gameplay state change occurs.
59. Confirm the Owner RoTS research card now shows Scan RoTS, Deep Scan, Copy All, and Clear without clipping at normal supported console widths.
60. After normal login/loading, click Deep Scan. Confirm both scan buttons disable until completion and the game/Swing UI remains responsive during render-set/GFX scanning.
61. Confirm the Deep Scan output resolves render sets 2687, 2689, 2690, 2982, and 2987 into raw BAS/cache opcode animation references and reports opcode-52 arrays where present.
62. Confirm targeted animation classification includes 18025, 18026, 21903, 21931, and 21922-21947 and reports renderSetReference=YES/NO without assigning unverified boss-mechanic names.
63. Confirm the GFX correlation section prints only explicit targeted/render-animation matches or direct known-RoTS-model matches. It must not flood the output with per-definition exceptions; if decoding failures occur, confirm it reports a total failure count, at most five examples, and marks a zero-match result as incomplete rather than evidence.
64. Run the normal Scan RoTS immediately after Deep Scan and confirm the original focused scan still works unchanged; no gameplay/server command path should execute during either scan.
65. Resize Owner from a wide console to the narrowest supported console width. Confirm Account values, all four RoTS buttons, output box, and informational text remain inside the visible viewport with no right-side clipping.
66. Confirm the Quick actions and RoTS informational text wrap within their cards instead of increasing the Owner panel's preferred width.
67. Start a fresh client session without first triggering any visible GFX. After Matrix3 finishes constructing its normal definition systems, click Deep Scan and confirm `GFX loader: ready`; the RoTS bridge must be using the authentic live `Class639` loader registered from the `Class452_Sub1` factory, not a fabricated loader built from another definition archive.
68. Click the Settings icon on the rail. Confirm Settings is lazy-created, remains usable at the narrowest supported console width, and exposes independent Legacy combat and Legacy interface toggles plus Apply saved combination.
69. Set Legacy combat ON and Legacy interface OFF, then Apply. Confirm the server reports Legacy combat and NIS separately, the modern resizable NIS interface remains active/editable, and combat uses Legacy behavior/special attack rather than EoC abilities.
70. Set Legacy combat OFF and Legacy interface ON, then Apply. Confirm EoC manual combat remains selected while the known Legacy interface/gameframe restrictions are applied. Record any interface element that does not match the original Legacy UI because this combination is verified-static only until runtime confirmation.
71. Test both matched combinations: ON/ON should produce Legacy combat plus Legacy interface state; OFF/OFF should produce EoC manual combat plus NIS. Confirm changing one toggle does not silently change the other selection.
72. Enable Matrix3's original in-game Legacy Mode checkbox, then return to Client Console Settings and Apply Legacy combat ON / Legacy interface OFF. Confirm the split control normalizes the original combined master state and returns to Legacy combat with NIS.
73. Save a non-default combat/interface combination, close Matrix3 normally, relaunch, log in, and open Settings. Confirm both toggle selections persist locally and the saved pair is queued once the logged-in player is available.
74. Attempt to apply Settings before login and with a non-admin account. Confirm the UI keeps the local selection but no unauthorized server mode change occurs; owner/admin live-player validation remains authoritative.
75. After Settings tests, switch Home/Owner/Commands/Items/Settings repeatedly, run one safe command and one Item Browser Inventory spawn, and confirm no Client Console panel or normal Commands.java path regressed.
76. Open the Home/shell panel and confirm a Cache development card with an Open RS3 CacheEditor button is visible and remains reachable without maximizing the client.
77. Click Open RS3 CacheEditor from a normal Eclipse/Java 8 client launch. Confirm the Client remains responsive while a separate CacheEditor process starts and presents its normal cache-directory chooser.
78. With CacheEditor still open, click Open RS3 CacheEditor again. Confirm the shell reports that CacheEditor is already running and no duplicate editor process is started.
79. Close CacheEditor and click Open RS3 CacheEditor again. Confirm it relaunches successfully, and verify Commands, Item Browser, Settings, BossLabs, normal gameplay input, and server state are unaffected by the external tool process.
80. Launch the Client with its working directory set to Matrix3/Client, then click Open RS3 CacheEditor. Confirm Gradle reports root project Matrix3-Server and runs runCacheEditor; Matrix3-Client must never be selected as the CacheEditor launch project.
81. On Windows, confirm the launcher prints `[CacheEditor] Server project:` pointing at Matrix3/Server and `[CacheEditor] Gradle wrapper:` pointing at Matrix3/Server/gradlew.bat before Gradle starts. Confirm the command uses the Server wrapper with `-p Matrix3/Server`, so Gradle cannot fall back to Matrix3-Client even when the client working directory is Client.
82. With Item Browser in All mode and the search field empty, confirm the visible result model remains capped at 160 entries and the game/client stays responsive while browsing and scrolling.
83. Type a broad partial item name that matches more than 160 definitions. Confirm the list does not rebuild on each keystroke, waits about 500ms after typing stops, then shows every matching definition in ascending item-ID order. Clear the search and confirm the empty browser returns to the 160-result cap after the same debounce.
84. Click the Player icon on the rail before login. Confirm Player is lazy-created, becomes the active panel, shows waiting placeholders for world state, and does not send a command or mutate gameplay.
85. Log in while leaving Player visible. Confirm Display name, Rights, Client state, Tile, Plane, and Region populate automatically without switching panels.
86. Walk several tiles with Player visible. Confirm Tile updates to the live world X/Y and Region remains stable until crossing a 64x64 region boundary; Plane should match the actual player plane.
87. Teleport across a region boundary using an existing normal Matrix3 path. Confirm the Player panel updates world tile and region ID/X/Y without requiring the panel to be recreated.
88. Switch away from Player, move in game, then return to Player. Confirm values refresh immediately on reopen and no background Player-panel refresh causes visible client/render hitching while the panel is hidden.
89. Leave Player selected, close Matrix3 normally, and relaunch. Confirm Player restores as the active panel through the existing Client Console workspace preference path.
90. Rapidly switch Home/Owner/Commands/Player/Items/Settings while moving in game. Confirm only the selected rail icon is highlighted, Player remains read-only, and normal renderer/input/Commands/Item Browser behavior remains unchanged.
91. Inspect all six rail icons at normal client size. Confirm Home, Owner, Commands, Player, Items, and Settings are recognizable, centered inside the existing 48x44 rail targets, and no old C/O/>_/P/I/S text placeholders remain.
92. Hover, press, select, disable where applicable, and switch between rail icons. Confirm the Java2D glyphs follow the existing muted/hover/selected theme treatment and the selected panel remains obvious without changing panel IDs or collapse behavior.
93. Verify the icon rail at 100%, 125%, and 150% Windows display scaling where available. Confirm the 22px vector-drawn glyphs remain crisp/readable, tooltips identify every icon, and no resource-loading or missing-glyph issue occurs.
94. After compiling the Phase 4A Client Console changes, rebuild the static Client Atlas index once so the generated fingerprint matches the current compiled client.
95. Click the new Client Atlas globe icon. Confirm Atlas is lazy-created on first open, becomes the active panel, and normal game rendering/input remains responsive while the first index is loaded off the Swing EDT.
96. Search `Class1`, then use an exact canonical Atlas ID from the results. Confirm the exact ID, kind, owner, name, descriptor, and available source/signature detail are shown without renaming the obfuscated symbol.
97. Select a relationship whose target/source resolves to an exact Atlas symbol and click Open selected relation (or double-click). Confirm the exact symbol opens. Select a constant/type/value row and confirm it stays visible but does not navigate as a guessed symbol.
98. On one harmless test symbol, save HYPOTHESIS evidence with alias `Atlas UI test`, note `Phase 4A runtime UI test`, and reference `runtime:phase4a-ui`. Confirm freshness reports CURRENT and the saved record reappears after selecting another symbol and returning.
99. Delete the temporary `Atlas UI test` evidence. Confirm the editor returns to No curated evidence and no generated Atlas symbol/relationship data changes.
100. Resize the console to the narrowest supported width with Atlas open. Confirm search, result selection, exact detail, relationship browsing, and evidence controls remain reachable without requiring fullscreen/maximized mode.
101. Move/interact in game while repeatedly searching and navigating Atlas. Confirm there is no visible render/input hitch and no Swing/client-thread exception.
102. Leave Atlas selected, close Matrix3 normally, and relaunch. Confirm Atlas restores as the active panel through the existing generic Client Console active-panel preference path.
103. Final Phase 4A regression: switch Atlas/Home/Owner/Commands/Player/Items/Settings, perform one safe normal game interaction, and confirm Atlas remains tooling-only with no gameplay/server command/packet behavior change.
