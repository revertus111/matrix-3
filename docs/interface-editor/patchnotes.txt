2026-09-06
- Added `ClientConsoleInterfaceBridge` as the client-thread authority for Interface Editor reads/writes; Swing only queues requests and consumes immutable snapshots.
- Added generic interface/component snapshots with component boundary detection, type/parent/item/child counts, base geometry, runtime geometry, alignment, text, sprite ID, and live-override state.
- Added reversible live overrides for base X/Y/W/H, runtime X/Y/W/H, alignment bytes, text, and sprite ID.
- Added pre-editor value capture so Reset Selected and Reset Interface restore the component state that existed before the first live override.
- Added `InterfaceEditorPanel` with `interface` / `interface:component` targeting, searchable component list, professional dark Client Console styling, Apply Live, Reset Selected, Reset Interface, and Copy Values.
- Added lazy Interface Editor rail navigation with a dedicated Java2D icon and generic active-panel persistence through the existing Client Console shell.
- Added Interface Editor request flushing to the existing logged-in client-cycle path in `Class514.method6093(...)`.
- Removed the temporary hardcoded Backpack `671:9` width repair so it cannot compete with manual live editor values; Backpack storage/server routing was not changed.
- Protected dirty inspector fields from the 350ms live snapshot refresh so values being typed are not replaced while the component list refreshes.
- V1 implementation is `NEEDS TEST` under Eclipse/Java 8.

2026-09-08
- Replaced geometry-only text-box tuning with eight live slider controls for base/runtime X, Y, Width, and Height while preserving exact numeric entry.
- Added one-pixel `-` / `+` nudges for each geometry value and shared Fine +/-64, Normal +/-256, and Wide +/-1024 slider ranges.
- Slider/nudge changes apply automatically when Live geometry is enabled; exact geometry values also apply on Enter or focus loss.
- Live drag traffic is coalesced by the Swing panel to at most one queued client-thread override every 50ms, preventing slider motion from flooding the existing Interface Editor action queue.
- Width/height controls reject negative exact values and clamp slider ranges at zero; X/Y remain signed.
- Renamed the manual action to `Apply Exact / Visual` so text/sprite/alignment or exact full-state application remains explicit alongside live geometry tuning.
- Reset Selected / Reset Interface cancel any pending live slider write before restoring pre-editor values.
- Preserved dirty-field protection for all eight exact geometry boxes so the 350ms snapshot refresh cannot overwrite a value while it is being typed.
- Programmatic component-list refresh/reselection no longer cancels a pending live slider write; only an actual user target/component change or reset does.
- Added `ClientConsoleInterfaceOverlay` as a read-only wire-mesh visualization over the Matrix3 game Canvas using immutable Interface Editor snapshots rather than direct Swing-side `InterfaceDefinitions` reads.
- Added Wire Mesh controls for all-components/selected-only display, component IDs, dimensions, and parent-link lines; the selected component receives a stronger diagnostic outline and position/size label.
- Added one-shot `Pick Component` mode. Only the armed diagnostic left-click is consumed; normal game input remains untouched outside picker mode. Overlapping hits prefer the deepest/smallest component so child widgets win over large parent containers.
- Wire Mesh automatically disables when Interface Editor is hidden and follows the currently loaded interface and selected component while sliders are tuning live geometry.
- V1.2 wire-mesh/picker behavior is `NEEDS TEST`, including OpenGL Canvas visibility and parent-relative overlay alignment.
