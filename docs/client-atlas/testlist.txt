Client Atlas - Phase 1 test list

Runtime-confirmed foundation
[x] Eclipse/Java 8 can compile/run Atlas after Gradle -> Refresh Gradle Project resolves ASM.
[x] Run scanner against compiled Matrix3 Client classes.
[x] Confirm scan reports non-zero counts.
    VERIFIED: 1221 class files / 33742 symbols / 34053 relationships.
[x] Reopen metadata with status.
[x] Confirm `Current fingerprint: true`.
    VERIFIED fingerprint: 41be330f2baa1044db8da56ddc160447b1cc3db7e7bdcd4c1c5cfc955973fc26

Standalone Client Atlas Control gate
[ ] Pull current main.
[ ] Run `game.atlas.ClientAtlasMain` with NO program arguments.
[ ] Confirm the `Client Atlas Control` window opens.
[ ] Confirm the status panel shows the existing index/counts and CURRENT fingerprint state.
[ ] Click `Run Phase 1 Check`.
[ ] Confirm output ends with `PHASE 1 AUTOMATED CHECK: PASS`.
[ ] Confirm automated check verifies CLASS/FIELD/METHOD/CONSTRUCTOR symbol coverage.
[ ] Confirm automated check verifies DECLARES/EXTENDS/IMPLEMENTS relationship coverage.
[ ] Confirm automated check verifies generated symbols exclude `game/atlas/**`.
[ ] Confirm automated check exercises an exact class query and a METHOD/CONSTRUCTOR query where available.
[ ] Confirm `.client-atlas/phase1-check.json` is written and non-empty.
[ ] Search `Class1` in the UI and confirm readable JSON output is shown.
[ ] Click `Export Last Result` and confirm a file is written under `.client-atlas/exports/`.
[ ] Click `Open Workspace` and confirm the local Atlas workspace opens.

Remaining guard/regression checks
[ ] On a later real client source change + rebuild, confirm query/export refuses the stale index until `Scan / Rebuild Index` runs again.
[ ] If a queried symbol owns more than 200 immediate relationships, confirm output is capped while full relationshipCount/truncation state remains accurate.
[ ] Confirm normal `game.RS3Applet` launch ownership/configuration remains unchanged.

Notes
- Do not intentionally mutate compiled classes solely to prove stale-index rejection if the one-click Phase 1 check passes; verify that guard on the next natural client rebuild/change.
- Normal developer use should no longer require editing Eclipse program arguments between scan/status/query/export commands.
