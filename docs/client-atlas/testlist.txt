Client Atlas test list

Phase 1 - runtime verified
[x] Eclipse/Java 8 compiles/runs Atlas after Gradle -> Refresh Gradle Project resolves ASM.
[x] Scanner completed against compiled Matrix3 Client classes.
[x] VERIFIED: 1221 class files / 33742 symbols / 34053 relationships.
[x] Metadata reopened without rebuild.
[x] `Current fingerprint: true`.
[x] VERIFIED fingerprint: 41be330f2baa1044db8da56ddc160447b1cc3db7e7bdcd4c1c5cfc955973fc26
[x] Standalone `Client Atlas Control` opens through no-argument `ClientAtlasMain`.
[x] `Run Phase 1 Check` ended with `PHASE 1 AUTOMATED CHECK: PASS`.
[x] UI exact class search worked.
[x] UI export worked for `CLASS:game/Class1`.
[x] Export reported schema 1, current fingerprint, 15 immediate relationships, and no truncation before the Phase 2 schema bump.

Phase 2 / 2A.1 schema-v2 checks - batch with structural scanner verification
[ ] Clean/build Client after pulling schema v2.
[ ] Open Client Atlas Control and confirm the old schema-v1 generated index reports non-current/stale until rebuilt.
[ ] Click `Scan / Rebuild Index` and confirm metadata becomes schema version 2/current.
[ ] Confirm a symbol JSONL record contains separate `compiledPath` and `sourcePath` fields.
[ ] Confirm a normal `game/Class1` symbol resolves `compiledPath` to its `.class` and `sourcePath` to its real `src/main/java/...java` file.
[ ] Confirm a relationship JSONL record contains `sourcePath`, `sourceLine`, `opcode`, `occurrenceCount`, and `detail`.
[ ] Confirm Phase-1 structural DECLARES/EXTENDS/IMPLEMENTS records use `occurrenceCount:1` with null line/opcode.
[ ] Confirm exact query/export still works after the schema-v2 rebuild.
[ ] Confirm evidence/traces remain preserved across the generated-index rebuild.

Phase 2 / upcoming 2A.2-2A.4 checks
[ ] Known outgoing `CALLS` edge and corresponding incoming/called-by path.
[ ] Known `GETFIELD`/`GETSTATIC` -> `READS_FIELD` edge.
[ ] Known `PUTFIELD`/`PUTSTATIC` -> `WRITES_FIELD` edge.
[ ] Repeated same-method edges aggregate occurrence count instead of flooding JSONL.
[ ] Source line/opcode evidence is present where bytecode/debug data supports it.
[ ] `invokedynamic`, if present, uses `DYNAMIC_CALL` rather than `CALLS`.
[ ] Type references remain structural facts without semantic renaming.
[ ] Typed constants do not become interface/animation/opcode/etc. IDs without evidence.
[ ] Measure generated relationship count/file size/query time before changing persistence or graph caps.

Carryover regression checks
[ ] On a natural future client source change + rebuild, confirm query/export refuses the stale index until rebuilt.
[ ] If a queried symbol owns more than 200 immediate relationships, confirm output stays capped while full count/truncation remains accurate.
[ ] Confirm normal `game.RS3Applet` launch ownership/configuration remains unchanged after meaningful Atlas changes.
