2026-09-05

Phase 1
- Added isolated `game.atlas` workspace, stable symbol IDs, deterministic client fingerprinting, ASM declaration scanner, JSONL persistence, exact query/export, standalone Atlas Control, and one-click verification.
- Runtime verified against 1221 compiled client classes / 33742 symbols / 34053 initial relationships.

Phase 2 structural map
- Added schema-v2 source locators plus CALLS, DYNAMIC_CALL, READS_FIELD, WRITES_FIELD, REFERENCES_TYPE, and typed CONSTANT relationships.
- Added deterministic malformed-string JSON escaping and consolidated structural verification.
- Runtime verified 33742 symbols / 325826 relationships; full scan ~1.28 s, symbols ~8.5 MiB, relationships ~74.4 MiB.

Phase 2 investigation/search
- Added immutable in-memory investigation index, ranked/friendly search, ambiguity safety, relationship commands, bounded depth-1/2 neighborhoods, and consolidated search verification.
- Added assistant export v2 and safe domain correlation (`interface 762`, `component 7`, `762:7`, animation/model/etc.) over typed constants only.
- Domain requests remain hints with semantic status UNKNOWN and no automatic LITERAL_ID promotion.
- Final combined Phase 2 investigation gate runtime-verified by user report.

Phase 3 / 3A.0
- Documented targeted runtime hook ownership for normalized keyboard input, outgoing packet enqueue, incoming packet decode, interface packet activity, and Class639 definition/cache misses.
- Chosen one-way `game.AtlasRuntimeBridge` -> `game.atlas` recorder ownership with tracing off by default and no event-path disk I/O.

Phase 3 / 3A-Core foundation
- Added `AtlasTraceRecorder`: bounded 10000-event process-wide sessions, start/stop/name/save lifecycle, dropped-event accounting, failure isolation, atomic JSONL trace persistence, and current compiled-client fingerprint capture on save.
- Added `AtlasTraceControl`: atomic file-backed cross-process START/STOP/SAVE/STOP_SAVE commands plus runtime heartbeat/status so the standalone tooling JVM can control the running client JVM without Client Console ownership.
- Added `ClientAtlasTraceControl`, a small dark trace-control window showing runtime connection, active session, event count, dropped count, and last saved trace.
- Added `ClientAtlasMain` trace-control/status/command CLI paths.
- Added `game.AtlasRuntimeBridge` APIs for normalized keyboard facts, outgoing/incoming packet metadata, named interface packet classification, and definition-load observations; packet payload bytes are never copied.
- Wired the existing `DevDefinitionBridge` definition observer into the Atlas runtime seam so the trace-control daemon is lazily initialized from a real client path.
- Runtime verification intentionally batched until the 3A-Correlation/final gate; no user launch required for the foundation patch.

Phase 3 / 3A-Core bundled hooks
- Resolved the high-level menu execution path as verified-static: `Class25.method728(...)` selects the menu entry, `Class319.method4094(...)` normalizes/dispatches the action, and its existing `DevModeBridge.handleMenuAction(...)` seam is called before normal Matrix3 action handling.
- Added menu-action tracing through that existing narrow Dev Mode seam without modifying the giant `Class319` dispatcher; records normalized action/local coordinates only and does not capture menu text.
- Added the outgoing packet hook at `Class195.method2929(...)` after encoded/enqueued length is known and before buffer reset; the audited decompiled-file diff is exactly one inserted line.
- Added incoming packet tracing through `MaterialInformation.method1605(...)` immediately after `PacketsDecoder.method3031(...)` successfully processes a packet; this preserves the decoder as authority and avoids rewriting the giant decoder. Records processed packet identity/final length only.
- Named incoming interface packets are classified as interface activity by `AtlasRuntimeBridge`; component payload values remain targeted carryover instead of blanket decoder instrumentation.
- Added the ID-bearing `Class639.method7568(...)` cache-miss hook after definition decode/finalization; the audited decompiled-file diff is exactly one inserted line.
- Confirmed GFX definitions are `Class639`-backed where its `Class452_Sub1` factory registers the graphics loader, so the generic definition hook already covers GFX cache-miss loads.
- Added `AtlasKeyboardObserver` as a failure-isolated AWT observer that mirrors Matrix3's verified `Class549_Sub1` key normalization/filtering without consuming events or rewriting the large decompiled input class. Keyboard trace records include the verified Matrix3 owner symbol as an explicit correlation hint.
- A direct attempted `Class549_Sub1` one-line rewrite was rejected because audit showed unrelated array-line churn; the branch was rolled back before using the safer observer implementation.
- Exact model/animation loader instrumentation remains CARRYOVER until real ownership is established; no guessed asset hook was added.
- Trace events now distinguish the literal hook `sourceSymbol` from the verified logical `ownerSymbol` for keyboard, menu, and incoming packet observations so later correlation does not blur evidence provenance.
- All Core runtime hooks remain opt-in through the recorder, payload-minimal, bounded, and failure-isolated. Runtime verification remains deliberately consolidated with 3A-Correlation and the final Bundle 3A gate.

Phase 3 / 3A.6 runtime correlation
- Added `AtlasTraceCorrelationEngine` to resolve saved runtime trace `sourceSymbol` / `ownerSymbol` IDs against the already-current `AtlasInvestigationIndex`.
- Correlation refuses stale generated Atlas data through the existing index load gate and separately checks the trace fingerprint against the current Atlas fingerprint.
- Correlation is accepted as current only when fingerprint, event count, source IDs, and owner IDs all validate; mismatch/unknown/unresolved states remain explicit diagnostics.
- Trace input remains bounded to 10000 events; assistant JSON exports at most 1000 events and 100 unresolved IDs per list.
- Correlation output includes category counts, dropped count, resolution totals, exact symbol metadata, safe original fields, and truncation state without creating semantic evidence or `LITERAL_ID` claims.
- Added `latest` trace lookup plus atomic correlation export.
- Added CLI commands `trace-correlate <trace|latest>` and `trace-correlate-export <trace|latest> <file>`.
- 3A.6 implementation is verified-static; runtime validation remains bundled with the single final Bundle 3A gate.
- Fixed the Java 8/Eclipse compile blocker by importing `DirectoryStream` from `java.nio.file` instead of the nonexistent `java.io` location; no runtime behavior changed.
