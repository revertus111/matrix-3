2026-09-05
- Added the isolated `game.atlas` offline foundation without changing normal `game.RS3Applet` startup ownership.
- Added stable Atlas symbol/relationship/evidence/metadata models preserving original obfuscated identifiers.
- Added deterministic SHA-256 fingerprinting over sorted compiled client `.class` inputs while excluding `game/atlas/**`.
- Added `.client-atlas` workspace initialization, metadata persistence/reopen support, stale/current fingerprint checks, and reserved JSONL data files.
- Added a minimal offline `ClientAtlasMain` init/status entry point for Phase 1 validation.
- Ignored `Client/.client-atlas/` so generated Atlas data survives build cleaning without polluting Git.
- Pinned ASM 9.7.1 and added the Phase 1 bytecode declaration scanner.
- Scanner records classes, interfaces, enums, annotations, fields, methods, constructors, descriptors/signatures, access flags, compiled paths, and EXTENDS/IMPLEMENTS/DECLARES relationships.
- Added deterministic JSONL serialization for generated symbol/relationship records without renaming obfuscated symbols.
- Added `ClientAtlasMain scan`, temporary-file publishing, metadata count updates, and before/after fingerprint protection against class changes during a scan.
- Kept method-body analysis out of Phase 1 so callers/callees and field read/write tracing remain isolated to Phase 2.
- Added exact canonical Atlas-ID lookup through `ClientAtlasMain query` with stale-index/schema validation before results are returned.
- Query output now includes the exact symbol plus its immediate stored relationships as compact JSON, capped at 200 relationship records with an explicit truncation flag/count.
- Added `ClientAtlasMain export` for atomic UTF-8 machine-readable investigation snapshots without adding another JSON/database dependency.
- Kept ranked/fuzzy/shorthand search out of Phase 1; those remain Phase 2 investigation-search ownership.
- Runtime-confirmed Eclipse/Java 8 scanner output: 1221 class files, 33742 symbols, 34053 relationships; persisted fingerprint reopened current.
- Confirmed Eclipse ASM classpath issue was resolved by Gradle -> Refresh Gradle Project; no dependency workaround was required.
- Added standalone dark `ClientAtlasControl` Swing UI with automatic Client/class-root discovery, current/stale status, scan, search, export, workspace access, and background tasks.
- Added one-click `Run Phase 1 Check` to scan, validate symbol/relationship coverage, verify Atlas exclusion/current fingerprint, exercise exact class/member query, and write a compact verification export.
- `ClientAtlasMain` with no program arguments now opens the control UI; explicit CLI commands remain available for automation.
