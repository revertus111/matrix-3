2026-09-05
- Added the isolated `game.atlas` offline foundation without changing normal `game.RS3Applet` startup ownership.
- Added stable Atlas symbol/relationship/evidence/metadata models preserving original obfuscated identifiers.
- Added deterministic SHA-256 fingerprinting over sorted compiled client `.class` inputs while excluding `game/atlas/**`.
- Added `.client-atlas` workspace initialization, metadata persistence/reopen support, stale/current fingerprint checks, and reserved JSONL data files.
- Added a minimal offline `ClientAtlasMain` init/status entry point for Phase 1 validation.
- Ignored `Client/.client-atlas/` so generated Atlas data survives build cleaning without polluting Git.
- Pinned ASM 9.7.1 and added the Phase 1 bytecode declaration scanner.
- Scanner records classes, interfaces, enums, annotations, fields, methods, constructors, descriptors/signatures, access flags, compiled paths, and EXTENDS/IMPLEMENTS/DECLARES relationships.
- Added deterministic JSONL serialization for generated symbol/relationship records without renaming obfuscated symbols.
- Added `ClientAtlasMain scan`, temporary-file publishing, metadata count updates, and before/after fingerprint protection against class changes during a scan.
- Kept method-body analysis out of Phase 1 so callers/callees and field read/write tracing remain isolated to Phase 2.
- Added exact canonical Atlas-ID lookup through `ClientAtlasMain query` with stale-index/schema validation before results are returned.
- Query output now includes the exact symbol plus its immediate stored relationships as compact JSON, capped at 200 relationship records with an explicit truncation flag/count.
- Added `ClientAtlasMain export` for atomic UTF-8 machine-readable investigation snapshots without adding another JSON/database dependency.
- Kept ranked/fuzzy/shorthand search out of Phase 1; those remain Phase 2 investigation-search ownership.
- Runtime-confirmed Eclipse/Java 8 scanner output: 1221 class files, 33742 symbols, 34053 relationships; persisted fingerprint reopened current.
- Confirmed Eclipse ASM classpath issue was resolved by Gradle -> Refresh Gradle Project; no dependency workaround was required.
- Added standalone dark `ClientAtlasControl` Swing UI with automatic Client/class-root discovery, current/stale status, scan, search, export, workspace access, and background tasks.
- Added one-click `Run Phase 1 Check` to scan, validate symbol/relationship coverage, verify Atlas exclusion/current fingerprint, exercise exact class/member query, and write a compact verification export.
- `ClientAtlasMain` with no program arguments now opens the control UI; explicit CLI commands remain available for automation.
- Runtime-confirmed the standalone UI Phase 1 automated check passes and UI search/export successfully produced `CLASS:game/Class1` with a current fingerprint and 15 immediate relationships.
- Closed Phase 1 and activated Phase 2 structural relationship work.
- Added Atlas schema v2: relationship records now carry `sourcePath`, nullable `sourceLine`, nullable `opcode`, `occurrenceCount`, and optional `detail`.
- Added distinct `DYNAMIC_CALL` relationship type so future `invokedynamic` evidence cannot be mislabeled as a proven direct call.
- Split symbol location into `compiledPath` and `sourcePath`; declaration scanning resolves Java source files from original internal owners while preserving compiled class paths.
- Existing EXTENDS/IMPLEMENTS/DECLARES records now persist source-path evidence with occurrence count 1; line/opcode evidence remains reserved for method-body scanning.
- Bumped Atlas workspace schema to 2 and treat older schema indexes as stale so query/export requires an explicit rebuild instead of silently reusing incompatible generated data.
- Added Phase 2 method-body scanning with `ClassReader.SKIP_FRAMES` so code/debug line information is available without frame processing.
- Added exact `CALLS` relationships for method/constructor bytecode targets and distinct `DYNAMIC_CALL` records for `invokedynamic` bootstrap sites.
- Added `READS_FIELD` for GETFIELD/GETSTATIC and `WRITES_FIELD` for PUTFIELD/PUTSTATIC using exact stable field IDs.
- Added per-method relationship aggregation so repeated identical targets emit one record with `occurrenceCount`; source line and opcode evidence are retained when consistent.
- Dynamic-call targets include bootstrap owner/name/descriptor while tag/interface facts remain in detail.
- Added `REFERENCES_TYPE` indexing across field/method descriptors, declared exceptions, generic signatures, type instructions, field/method owners and descriptors, LDC `Type` values, bootstrap handles, and constant-dynamic structure.
- Type references use neutral `TYPE:<internal-name>` targets so structural facts do not guess class/interface/domain semantics.
- Object arrays resolve to referenced object element types; primitive-only descriptors do not generate fake object references.
- Added typed `CONSTANT` relationships from field constant values, LDC literals, ICONST/LCONST/FCONST/DCONST, BIPUSH/SIPUSH, and reliable bootstrap arguments.
- Constant targets preserve literal type (`int:`, `long:`, `float:`, `double:`, `string:`); raw numeric constants never auto-promote to `LITERAL_ID` or domain IDs.
- `Type`, `Handle`, and `ConstantDynamic` values are treated structurally rather than flattened into misleading string constants.
- Generic inner-class signatures now retain exact JVM inner-type references (`Outer$Inner`) in addition to outer type evidence.
- Added `AtlasStructuralVerifier` for the 2A.4 consolidated gate: one run rebuilds schema v2, verifies structural relationship families, checks evidence/trace preservation, exercises incoming/exact query + export, and records scan/query timing plus generated JSONL sizes.
- Added `Run Phase 2 Check` to Client Atlas Control so the full Bundle 2A verification no longer requires manual JSONL inspection.
- Atlas Control now labels incompatible generated metadata as `REBUILD REQUIRED (schema old -> current)` instead of generic stale state.
- Added `ClientAtlasMain verify-structural` as the automation/CLI front end to the same verifier; the UI and CLI do not duplicate verification logic.
- Phase 2 structural verification writes `.client-atlas/phase2-structural-check.txt` plus a compact query regression export for easy reporting.
- Fixed the Phase 2 verifier report newline separator so `AtlasStructuralVerifier` compiles cleanly instead of producing Eclipse `Invalid character constant`.
- Fixed Phase 2 JSONL encoding for obfuscated/malformed client strings by escaping all UTF-16 surrogate code units as deterministic `\\uXXXX` JSON sequences instead of letting the UTF-8 writer fail with `Input length = 1`.
- Added `AtlasInvestigationIndex` as the Phase 2B in-memory acceleration layer over current schema-v2 JSONL; scanner/JSONL ownership is unchanged.
- Investigation index validates schema/fingerprint before and after load, verifies loaded symbol/relationship totals against metadata, and refuses stale/inconsistent generated data.
- Added immutable lookup maps for exact symbol ID, normalized owner/name candidates, outgoing relationships, incoming relationships, and typed-constant referrers.
- Added compact parsed symbol/relationship entries preserving source path, source line, opcode, occurrence count, and detail for later bounded investigation queries.
- Added a temporary string canonicalization pool while loading the ~325k relationship dataset to reduce duplicate String memory without introducing a database or persistent secondary index.
- 2B.1 local load/memory verification is intentionally batched with the first user-facing Bundle 2B investigation check so the user does not need a standalone runtime session for a non-user-facing loader.
- Added `AtlasSearchEngine` for ranked/friendly symbol resolution over the in-memory investigation index without changing authoritative obfuscated IDs.
- Friendly search resolves exact Atlas IDs, exact class paths, exact class/member names, optional method descriptors, and plain exact names; ambiguous overloads/names return ranked candidates instead of being silently chosen.
- Added deterministic prefix/contains fallback ranking capped at 50 results; fuzzy/partial matches never auto-resolve even when only one candidate remains.
- Added `ClientAtlasMain search "<friendly-query>"` so the assistant/developer automation path can search `Class387`, `Class387.method4844`, canonical IDs, and partial names while reporting index-load/search timing.
- Standalone Atlas Control search-box integration is intentionally bundled with 2B.3 relationship commands so the UI search surface is rewritten once instead of twice.
- Added `AtlasRelationshipQueryEngine` for `calls`, `called-by`, `reads`, `written-by`, `references`, typed/raw `constant`, and depth-1/2 neighborhood investigation over the in-memory Atlas index.
- Class-level call/read queries expand through declared methods so class searches return useful bytecode relationships instead of empty class-record results.
- Relationship output is capped at 500 edges; neighborhoods are capped at 100 nodes / 500 edges and report truncation instead of dumping unbounded graphs.
- Added numeric constant normalization for raw integer/decimal searches while preserving typed constant authority and avoiding domain-ID guesses.
- Integrated friendly search and relationship commands into the standalone Atlas Control search box, with Enter-to-search and cached current-snapshot investigation indexes.
- Renamed the old `Run Phase 2 Check` UI action to `Run Structural Check` because it verifies Bundle 2A structural data, not all of Phase 2.
- Added `AtlasInvestigationVerifier`, `Run Search Check`, and `ClientAtlasMain verify-search` for one-click Bundle 2B verification without rescanning compiled client classes.
- Search verification covers current-index load/counts, exact/friendly/member resolution, ambiguity/fuzzy safety, call/read/write/type/constant directions, and depth-2 node/edge caps; report writes to `.client-atlas/phase2-investigation-check.txt`.
- Hardened investigation verification sample selection so hot methods/fields/types above the 500-edge display cap cannot cause false failures from choosing a known edge that would be intentionally truncated.
- Runtime-confirmed `PHASE 2 INVESTIGATION CHECK: PASS` on the real 33742-symbol / 325826-relationship dataset without rescanning; investigation-index load was 946.649 ms with ~181.5 MiB approximate used-memory delta.
- Runtime-confirmed exact/friendly Class1 resolution, ambiguity safety (`<init>` 1294 candidates), fuzzy-result capping (`Clas` 29421 matches / 50 shown), all relationship directions/types, and depth-2 neighborhood bounds (28 nodes / 40 relationships).
- Added `AtlasAssistantExportEngine` for compact bounded assistant/code investigation packages over the current in-memory index; JSONL remains the persistence authority.
- Assistant packages include schema/fingerprint/snapshot metadata, exact resolution or bounded candidates, source-bearing symbol records, relevant relationships, neighborhood context for resolved plain searches, and explicit truncation/cap state.
- Added `assistant-json` and `assistant-export` CLI paths so automation can consume machine JSON without parsing human search output.
- Replaced the standalone control's legacy last-result export with `Export Assistant JSON`; any successful friendly search or relationship command can now be exported from the cached investigation index.
- Removed the UI search-time exact streaming export preparation, avoiding an unnecessary symbols/relationships JSONL pass after every resolved friendly search.
- Extended `Run Search Check` / `verify-search` to validate assistant metadata/source fields, plain-search context, relationship-command export, ambiguity safety, caps, and atomic verification-file output without rescanning.
- 2B.4 implementation is complete as verified-static; local runtime verification is pending through the updated one-click search check.
