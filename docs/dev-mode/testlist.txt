Matrix3 Dev Mode - targeted runtime test list

[ ] Start the client and confirm Dev Mode is OFF by default.
[ ] Open Client Console > Settings, enable Dev Mode, and confirm the toggle reports ON.
[ ] With an Admin+ account, right-click an ordinary world tile and confirm exactly one `Dev > Spawn...` and one `Dev > Edit Tile` entry appear in addition to normal Matrix3 options.
[ ] Verify ordinary tile right-click reaches the action-23 scene movement path; Dev entries must not depend on the action-60 staff/admin teleport-debug path.
[ ] Disable Dev Mode and confirm both Dev entries disappear on newly built tile menus.
[ ] Re-enable Dev Mode, choose `Dev > Spawn...`, and confirm the dark Spawn Browser opens with the exact selected world X/Y/plane shown.
[ ] Confirm normal Walk Here still works when Dev Mode is ON.

NPC / Object targeting + Inspector
[ ] Right-click a normal NPC with Dev Mode ON and confirm the Dev target actions appear without replacing normal NPC options.
[ ] Choose `Dev > Inspect NPC` and confirm the shared Inspector shows the NPC type, cache name, exact definition ID, world tile/plane, and live NPC runtime index.
[ ] Choose `Dev > Edit NPC` and confirm the same Inspector window retargets/reopens with runtime world manipulation controls available; no NPC definition/persistent spawn data is changed by opening Edit.
[ ] Choose `Dev > Copy NPC ID` and confirm the clipboard receives the exact NPC definition ID.
[ ] Choose `Dev > Copy NPC Tile` and confirm the clipboard receives `x, y, plane` for the live NPC tile.
[ ] Right-click a normal object with Dev Mode ON and confirm the Dev target actions appear without replacing normal object options.
[ ] Choose `Dev > Inspect Object` and confirm the same shared Inspector retargets to the object and shows the exact object definition ID and selected world tile/plane.
[ ] Choose `Dev > Edit Object` and confirm runtime world manipulation controls are available; no object definition/map persistence data is changed by opening Edit.
[ ] Choose both object Copy actions and confirm the clipboard receives the exact object ID and selected `x, y, plane`.
[ ] Right-click a second NPC/object while the Inspector is open and confirm the same window retargets instead of creating duplicate Inspector frames.
[ ] Disable Dev Mode and confirm NPC/object Dev entries disappear from newly built menus while normal interactions remain unchanged.
[ ] Test a tile with multiple/stacked interactable entities and confirm each target keeps its own Dev actions/selection instead of cross-selecting another entity.

World Manipulation - consolidated Bundle 1.2 gate
[ ] Eclipse/Java 8: clean/build Client and Server and confirm no compilation errors before runtime checks.
[ ] Spawn an NPC through Dev Spawn, inspect it, choose Move, then right-click another tile and select `Dev > Move Here`; confirm that exact NPC moves to the chosen tile.
[ ] Move a normal non-Dev NPC and confirm the move is runtime-only; its persistent/original spawn authority is not rewritten.
[ ] Put two identical NPCs close/stacked, select one, move it, and confirm the exact runtime-index target moves rather than the other NPC.
[ ] Select a normal NPC, arm Duplicate, choose `Dev > Duplicate Here`, and confirm the source remains untouched and a new matching NPC appears on the destination tile.
[ ] Inspect the duplicated NPC and use `Delete Development Spawn`; confirm the duplicate disappears.
[ ] Attempt `Delete Development Spawn` on an ordinary non-Dev NPC and confirm the server blocks deletion and the NPC remains.
[ ] Spawn an object through Dev Spawn and confirm it is eligible for Dev-owned Move/Rotate/Delete behavior.
[ ] Move the Dev-spawned object with `Dev > Move Here`, then reselect it on the destination tile and confirm its ID/type/rotation are preserved.
[ ] Rotate the Dev-spawned object left and right; confirm rotation wraps safely through 0-3 and the object remains on the same tile.
[ ] Delete the Dev-spawned object and confirm it is removed without altering unrelated map objects.
[ ] Select an ordinary map object, arm Duplicate, choose `Dev > Duplicate Here`, and confirm the original remains and the new copy appears at the destination with the source type/rotation.
[ ] Reselect the duplicated object and confirm Move, Rotate, and Delete Development Spawn work on the Dev-owned copy.
[ ] Attempt Move on an ordinary map object and confirm the server blocks it with a Dev Mode message and the original object remains unchanged.
[ ] Attempt Rotate Left/Right on an ordinary map object and confirm the server blocks it and the object remains unchanged.
[ ] Attempt Delete Development Spawn on an ordinary map object and confirm the server blocks deletion and the map object remains.
[ ] Arm Move or Duplicate, right-click a destination tile, and confirm exactly one contextual `Dev > Move Here` or `Dev > Duplicate Here` entry appears in addition to normal tile actions.
[ ] Click `Cancel placement`, build a fresh tile menu, and confirm Move Here/Duplicate Here no longer appears.
[ ] Re-arm a placement after cancelling and confirm the new selected target replaces the previous pending placement cleanly.
[ ] Deeper check: move a Dev-owned object across a chunk/region boundary and confirm it disappears from the old location and appears correctly at the destination without clipping/map corruption.
[ ] Restart the server after manipulation testing and confirm Dev-created NPC/object placements do not persist as source/map data.

Tile Editor
[ ] Choose `Dev > Edit Tile` and confirm the dark Tile Editor opens for the exact tile that was right-clicked.
[ ] Confirm world X/Y and plane match the selected tile.
[ ] Confirm Region ID is `(worldX >> 6 << 8) | (worldY >> 6)` and region-local X/Y are each 0-63.
[ ] Confirm world chunk X/Y are `worldX >> 3` / `worldY >> 3` and chunk-local X/Y are each 0-7.
[ ] Click `Open Spawn Browser for this tile` and confirm Dev Spawn opens with the same X/Y/plane target.
[ ] Click `Copy tile coordinates` and confirm the clipboard receives `x, y, plane` without affecting the client.
[ ] Right-click a second tile while the Tile Editor is open and confirm reopening `Dev > Edit Tile` retargets the same window instead of creating duplicates.
[ ] Confirm Terrain / clipping is informational only; no height, overlay, underlay, movement clipping, or projectile clipping mutation is performed by this slice.

Spawn Browser search
[ ] NPC: type an exact NPC ID and confirm the matching cache name appears without waiting for full name indexing.
[ ] NPC: search a partial NPC name and confirm lazy indexing begins off the Swing UI thread and matching IDs populate as indexing progresses.
[ ] Object: type an exact object ID and confirm the matching cache name appears.
[ ] Object: search a partial object name and confirm lazy indexing populates matching object IDs without freezing the client/console UI.
[ ] Item: search by exact ID and partial name; confirm results match the existing Item Browser definitions.
[ ] Item: select a result and confirm a real item thumbnail loads when the Item Browser render context is ready.
[ ] NPC/object result cards must show cache-backed names/IDs only; do not mistake the N/O placeholder for a real model thumbnail.
[ ] Select a search result and confirm its ID is copied into the existing spawn field while manual ID editing still works.

Placement
[ ] NPC: choose a known NPC result/ID, spawn it, and confirm it appears on the selected tile using normal Matrix3 NPC behavior.
[ ] Object: choose a known object result/ID, type, and rotation; confirm it appears on the selected tile with the requested orientation.
[ ] Ground item: choose a known item result/ID and amount; confirm the owner can see the temporary item on the selected tile.
[ ] Enter invalid/unknown IDs and malformed values; confirm they are rejected without server/client exceptions.
[ ] Confirm an account below Admin rights cannot use the server Dev spawn/edit bridge and does not receive client Dev entries.
[ ] Restart the client and confirm Dev Mode returns to OFF.

Regression checks
[ ] Right-click NPCs, objects, ground items, and normal tiles; confirm their existing Matrix3 menu options are unchanged.
[ ] Walk several tiles and interact with normal objects after using Dev Spawn/Tile Editor/Inspector/manipulation tools.
[ ] Use the normal Client Console Item Browser after Dev Spawn search and confirm its item indexing/thumbnails still work.
[ ] Confirm normal NPC/object interaction packets still target the same live entities after Dev manipulation testing.
[ ] Spawn/manipulate then restart the server; confirm runtime placement is not mistaken for persistent source/map data.
