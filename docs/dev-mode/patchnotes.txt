2026-09-08 - Dev Mode Continuous / Paint Placement bundle
- Added shared client-side Dev Spawn placement state with Once, Continuous, and Paint modes while reusing the existing owner-only server `devspawn` authority for every actual placement.
- Continuous mode stays armed and adds a contextual tile placement entry for repeated NPC/object/item placement.
- Paint mode observes Matrix3's verified normal tile action (23), queues the selected Dev spawn, and returns control so normal Walk Here still executes.
- Added session last-used spawn target with contextual `Dev > Place Last ...` tile placement.
- Added explicit tile/browser cancellation plus a non-consuming AWT Escape listener that cancels armed Move/Duplicate/Continuous/Paint placement without taking over normal Escape behavior.
- Added object repeated-placement rotation behavior: Fixed, Cycle from the selected starting rotation, or Random 0-3.
- Placement modes are mutually exclusive with Move/Duplicate to prevent stale overlapping editor state; disabling Dev Mode clears active placement while last-used spawn remains session-only.
- No new server/world/map ownership was added; this bundle is client tooling layered over the existing validated runtime spawn path.

2026-09-07 - Dev Mode World Manipulation bundle
- Added server-side runtime ownership tracking for exact NPC/Object instances created through Dev Mode; tracking is session-only and does not persist world data.
- Added Move and Duplicate placement modes that use a right-clicked destination tile (`Dev > Move Here` / `Dev > Duplicate Here`) instead of coordinate-entry forms.
- Added object Rotate Left/Right plus guarded Delete Development Spawn actions in right-click menus and the shared Inspector.
- NPC Move resolves the exact Matrix3 NPC runtime index; ordinary NPC moves are runtime-only while Dev-owned NPC moves also update their runtime respawn tile.
- NPC Delete and object Move/Rotate/Delete require server-side proof of Dev ownership. Ordinary map objects/NPCs are protected from destructive deletion; ordinary targets may still be duplicated into Dev-owned copies.
- Reused native Matrix3 World/NPC/WorldObject runtime APIs and the existing Admin+ Item Browser command bridge; no map, clipping, definition, or persistent spawn ownership was changed.
- Normalized Dev Mode into `docs/dev-mode/PROJECT.md` and registered the workstream for persistent phase/bundle/Resume tracking.

2026-09-06 - Dev Mode NPC/object target + Inspector foundation
- Added owner-only Dev actions to normal NPC/object right-click targets: Inspect, Edit route, Copy ID, and Copy Tile.
- Added one shared contextual Inspector window that retargets to the selected live NPC/object instead of opening separate editor windows.
- Reused Matrix3's already-resolved NPC/object menu targets; object IDs come from the normal object menu UID and NPC IDs come from the live NPC definition ID field.
- Edit currently routes into the same read-only Inspector shell only; no NPC/object definition, spawn, map, clipping, or persistence mutation was added in this slice.
- Added shared current-target context for later Move/Rotate/Duplicate/editor slices without changing Matrix3 interaction ownership.

2026-09-03 - Dev Mode creation-studio design expansion
- Reframed Dev Mode as the main live-world Matrix3 creation environment instead of a collection of generic admin/cache panels.
- Added the planned World/Scene, contextual Inspector, universal browser, and History concepts plus Select/Move/Rotate/Spawn/Paint/Erase/Inspect tool modes.
- Added continuous/persistent placement workflows, inventory/equipment/bank Dev item coverage, full NPC stat/HP/drop editing direction, visual drop editing, tile gameplay-zone painting, contextual asset relationships, live-vs-saved comparison, workspaces, and command-palette concepts.
- Moved cache-facing work under contextual Assets and revised the roadmap around the already-working NPC/object/item spawn and Tile Editor foundation.

2026-09-03 - Dev Mode Tile Editor foundation
- Added owner-only `Dev > Edit Tile` beside the existing tile Spawn action.
- Added a dark Tile Editor window targeted from the exact live world tile right-click.
- Tile Editor shows world X/Y/plane, region ID/region-local coordinates, and world chunk/chunk-local coordinates.
- Added direct access from Tile Editor into the existing Spawn Browser for the same selected tile plus a copy-coordinate action.
- Terrain height, overlays/underlays, movement clipping, and projectile clipping remain intentionally non-editable until Matrix3 mutation paths are verified.
- Did not add speculative NPC/object thumbnail rendering; no existing safe off-screen Matrix3 model preview helper was found in the targeted renderer/tool path.

2026-09-03 - Dev Mode searchable Spawn Browser
- Added lazy NPC/object/item search by exact ID or name inside the tile Spawn Browser.
- Added a narrow client definition bridge that observes Matrix3's normal Class639 decode path instead of duplicating cache parsing.
- NPC/object name indexes build off-thread only after a text search starts; direct numeric ID lookup does not require a full index.
- Reused the existing Client Console item thumbnail renderer for real item previews in Dev Spawn.
- NPC/object model thumbnails remain a separate render-bridge task; this slice shows verified cache names/IDs without inventing visual previews.
- Existing runtime spawn commands and Matrix3 world placement authority were left unchanged.

2026-09-03 - Dev Mode tile right-click hook correction
- Corrected the normal Matrix3 world-tile menu source from action 60 to action 23.
- VERIFIED: action 23 is the normal scene-tile movement path carrying clicked local X/Y into Class319.method4094; action 60 is the staff/admin teleport-debug path.
- Kept the existing Dev Mode menu injection and Spawn Browser dispatch unchanged; no server placement behavior changed.

2026-09-03 - Dev Mode Phase 1 foundation
- Added a session-only Dev Mode toggle to Client Console Settings; defaults OFF on client launch.
- Mirrored Matrix3's existing scene-tile movement menu coordinates into an owner-only `Dev > Spawn...` action without replacing normal menu behavior.
- Added a dark Dev Spawn window with NPC, object, and ground-item runtime placement by ID on the selected tile.
- Added owner-only server validation/placement through the existing Item Browser command bridge and native World.spawnNPC, World.spawnObject, and World.addGroundItem APIs.
- Live placements are runtime edits only; persistent save/edit workflows remain future Dev Mode phases.
- Name search and NPC/object/item thumbnail browsing remain the next spawn-browser slice.
