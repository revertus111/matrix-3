2026-09-02
- Added read-only `RotsCacheScanner` for revision-830 RoTS asset discovery.
- Scanner finds all six brother name variants and dumps NPC/model/render-animation metadata.
- Added model-fingerprint correlation to identify likely normal variants related to observed Santa-hat models.
- Added initial RoTS 1:1 reverse-engineering documentation structure.
- Added Owner Console RoTS cache research output so future focused scans no longer require running a standalone Java main.
- Added one-click Copy All and Clear controls around a scrollable read-only dump; scanning is off the Swing event-dispatch thread.
- Recorded NPC 18539 as VERIFIED Ahrim flying state from runtime testing.
- Recorded NPC 18542 as VERIFIED Guthan spearless/Impale state from runtime testing.
- Initially tracked NPCs 18546-18551 as HYPOTHESIS subdued/downed states pending runtime confirmation.
- Recorded that the currently identified RoTS combat-family models render with Santa hats in this revision-830 cache/runtime.

2026-09-03
- Added Owner Console RoTS Deep Scan for focused BAS/render-set and GFX correlation without assigning unverified mechanic names.
- Deep Scan resolves render sets 2687, 2689, 2690, 2982, and 2987 into raw cache opcode animation references and classifies targeted animation candidates by render-set membership.
- Added read-only GFX-definition correlation against targeted/render-set animation IDs and known RoTS NPC model IDs.
- Kept all Deep Scan work off the Swing event-dispatch thread and reused the existing Copy All workflow.
- Added a read-only GFX bootstrap using the live Matrix3 config archive and spot-animation config group before Deep Scan, so GFX correlation no longer depends on a visible GFX having already loaded at runtime.
- Fixed Owner research-card width tracking/wrapping so Deep Scan controls and copied-output workflow remain reachable at narrow supported console widths.
- Audited the current Matrix3-native RoTS implementation and reconciled RoTS documentation with the actual encounter owners and implemented mechanics.
- Updated `ROTS_MASTER.md` from the old from-scratch discovery plan to the current 1:1 parity workflow around `RiseOfTheSixInstance`, `RiseOfTheSixBrother`, and `RiseOfTheSixCombat`.
- Recorded the implemented encounter foundation: daily 20-rotation side assignment, logical subdue, survivor healing, revive timer/bar, all-six completion, empty-side empowerment foundation, and dormant Brother Throw execution.
- Corrected 18546-18551 documentation: these remain body-only/inactive cache definitions with unproven authentic role, and current revision-830 runtime testing found them invisible when used as subdued models; the live encounter intentionally keeps active brother models visible at 1 HP instead.
- Recorded current implementation animation mappings including revive 21914, flying Ahrim auto 21925, Torag 21933/21934/21935/21938, Dharok charge 21940, and Guthan 21944/21945/21947.
- Recorded current Guthan/side-hop GFX mappings and kept donor Dharok GFX 4406 explicitly runtime-rejected for Greatest Axe in this cache.
- Refocused the parity workflow around authentic source/video evidence instead of assigning mechanic names from animation appearance alone.
- Recorded animation 21941 as VERIFIED runtime visual evidence of a sustained Dharok spin while keeping its Hurricane/Spinning Attack assignment as a strong HYPOTHESIS.
- Added period RoTS documentary evidence that all four melee brothers use a shared Spinning Attack and that Verac's Helicopter is a separate move, preventing those animations from being conflated.
- Changed the immediate parity checkpoint to cross-test 21941 on Guthan, Torag, and Verac before any Hurricane gameplay patch.
- Noted that targeted animation research should extend through at least 21947 because known RoTS/current-implementation special mappings now continue beyond the previous 21940 cutoff.
- Runtime cross-testing confirmed animation 21941 produces the sustained shared weapon-spin presentation on Dharok, Guthan, Torag, and Verac.
- Updated `RiseOfTheSixBrother` Hurricane to use animation 21941 instead of replaying each brother's normal attack emote.
- Removed the fake per-pulse cardinal facing rotation from Hurricane; the verified spin animation now owns the visual rotation while the existing 10-pulse tracking/damage mechanic remains unchanged.
- Starts 21941 once at Hurricane entry instead of restarting it every damage pulse, preserving the sequence for runtime duration verification.
- Updated RoTS master status, animation evidence, and runtime test checklist around the integrated 21941 Hurricane slice.
- Added a stable main-goal status roadmap to `ROTS_MASTER.md` anchored to 100% / 1:1 Rise of the Six, so future status updates keep the original encounter goal visible instead of being replaced by the latest subtask.
- Updated the repository-wide `AGENTS.md` status rule so every chat/post-patch status remains anchored to the original/main project goal, reports the latest subtask separately, and ends with the next meaningful main-roadmap step.
- Traced the Owner Deep Scan GFX failures blocking Dharok Greatest Axe asset research to the synthetic loader path that reused the render-set archive for the split spot-animation config group.
- Changed the research bridge to capture Matrix3's authentic live `Class639` GFX loader when it is constructed with `Class452_Sub1`; the old bootstrap no longer fabricates a loader or guesses an archive.
- Extended focused animation research through 21947 and corrected the live research label for the runtime-invisible 18546-18551 body-only/inactive definitions.
- GFX correlation now summarizes decode failures and refuses to treat a zero-match result as evidence when any definitions failed to decode.
- No RoTS server combat behavior changed in this GFX-research unblock; Greatest Axe animation 21940, timing, and missing blue-energy visual remain pending runtime/cache evidence.
- Added `RiseOfTheSixDebugLog`, a development-only per-instance RoTS Fight Recorder writing unique logs under `data/logs/rots/`.
- Recorder disk I/O is isolated to a bounded-queue daemon writer; the world/combat thread only builds log strings and performs non-blocking queue offers.
- Recorder starts from `RiseOfTheSixInstance.loadMapInstance()` before brother spawning and records the active daily rotation, all six spawn events, broadcasts, side-hop state/movement, survivor heals, subdue/all-six completion, revive timer/progress/revival, Torag cross-brother callback, and instance cleanup.
- Added one full runtime snapshot on every `WorldTasksManager.processTasks()` cadence for the instance, every current player, and every brother, including tiles, HP, side ownership, current target, subdued/special state, and RoTS-owned internal special counters/victims used for debugging.
- Added automatic player enter/leave/side-change and player/brother HP-change events so logs can be reconstructed without relying only on raw snapshots.
- `RiseOfTheSixCombat` now records every RoTS normal melee/magic/ranged attack with animation/projectile/GFX, max hit, rolled damage, hit delay, return delay, Guthan heal proc, and Ahrim Strength drain when triggered.
- Brother-owned special logging now records Dharok Greatest Axe start/21940/absorbed damage/stored release, Torag Whack/Pummel/rescue/release, Guthan Impale projectile/transformation/bleeds/retrieval/auto-return, Hurricane 21941 start/pulses/hits/end, Wall Slam phases/hits, special resets, subdue/revive, and brother finish.
- Fight Recorder lines include elapsed milliseconds plus a recorder tick incremented on every world-task processing cadence; queue overflow is fail-soft and reported as a dropped-line count instead of blocking combat.
- Verification hardened recorder attachment so delayed RoTS tasks cannot create a second log after an instance has already finished; one encounter remains one recorder file.
- Verification corrected the snapshot schedule from period 1 to period 0 because Matrix3 period 1 skips one `processTasks()` pass between executions; the recorder now captures every server task tick as intended.
- First full Fight Recorder analysis identified three concrete runtime-correctness defects: revive-bar progress ran at half the actual revival cadence, the empty-side monitor ran at half the intended cadence, and subdued 1-HP brother shells inherited Matrix3 natural HP restoration.
- Fixed revive-bar progression to run every world-task processing cadence (`period=0`) so the 50-step bar advances alongside the shared revive timer instead of reaching only about half-full before revival.
- Fixed empty-side monitoring to run every world-task processing cadence (`period=0`) so `EMPTY_SIDE_WARNING_TICKS=40` again represents about 24 seconds at the observed ~600 ms tick cadence.
- Added a RoTS-only `RiseOfTheSixBrother.restoreHitPoints()` override that suppresses ordinary Matrix3 natural HP restoration while `subdued=true`, keeping logical subdued shells at exactly 1 HP without changing global NPC regeneration.
- Added `docs/rots/updated.md` as the persistent cross-chat runtime issue/continuation tracker. It records recorder-proven defects, patched-but-needing-runtime-verification items, open side-targeting/Guthan/Greatest-Axe issues, and the continuation order back to the main 1:1 roadmap.
- Second Fight Recorder verification (`rots_2026-09-03_21-42-48-058_1861806.log`) promoted all three original runtime fixes to VERIFIED FIXED: empty-side warning fires on the corrected every-tick cadence, revive generation 6 reaches `bar=255/255` on the same tick as revival, and every observed subdued snapshot remains exactly 1 HP.
- The same verification log established a broader west/east side-isolation defect: East brothers can acquire/attack/follow a West-side player before empowerment, and West-side player damage can land on East brothers before their side ownership changes.
- Recorded two successful single-player Guthan Impale throw/impact/retrieve sequences in the second log; bleed cadence remains untested because the only victim was already close enough for immediate retrieval.
- Recorded exact one-shot timing evidence for later parity work: tick 41 side-hop warning -> tick 47 movement and tick 131 revive-generation start -> tick 182 revival. These observations are evidence of Matrix3 scheduling behavior, not yet proof of the authentic 2014 delay constants.
